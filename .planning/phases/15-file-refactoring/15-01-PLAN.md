---
phase: 15-file-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mastra/workflow/workflow.ts
  - src/mastra/workflow/steps/01-extract.ts
  - src/mastra/workflow/steps/02-hypothesize.ts
  - src/mastra/workflow/steps/03-answer.ts
autonomous: true
requirements: [REFAC-01, REFAC-04]

must_haves:
  truths:
    - "workflow.ts is a short composition file (~30 lines) that imports step definitions and wires them with .then()/.map()"
    - "Each step definition lives in its own file under src/mastra/workflow/steps/"
    - "The solverWorkflow export in workflow.ts is unchanged and still imported by src/mastra/index.ts"
    - "npx tsc --noEmit passes with no new errors after the split"
  artifacts:
    - path: "src/mastra/workflow/steps/01-extract.ts"
      provides: "extractionStep definition"
      exports: ["extractionStep"]
    - path: "src/mastra/workflow/steps/02-hypothesize.ts"
      provides: "multiPerspectiveHypothesisStep definition"
      exports: ["multiPerspectiveHypothesisStep"]
    - path: "src/mastra/workflow/steps/03-answer.ts"
      provides: "answerQuestionsStep definition"
      exports: ["answerQuestionsStep"]
    - path: "src/mastra/workflow/workflow.ts"
      provides: "Composition chain importing steps and exporting solverWorkflow"
      exports: ["solverWorkflow"]
  key_links:
    - from: "src/mastra/workflow/workflow.ts"
      to: "src/mastra/workflow/steps/01-extract.ts"
      via: "import { extractionStep }"
      pattern: "import.*extractionStep.*steps/01-extract"
    - from: "src/mastra/workflow/workflow.ts"
      to: "src/mastra/workflow/steps/02-hypothesize.ts"
      via: "import { multiPerspectiveHypothesisStep }"
      pattern: "import.*multiPerspectiveHypothesisStep.*steps/02-hypothesize"
    - from: "src/mastra/workflow/workflow.ts"
      to: "src/mastra/workflow/steps/03-answer.ts"
      via: "import { answerQuestionsStep }"
      pattern: "import.*answerQuestionsStep.*steps/03-answer"
    - from: "src/mastra/index.ts"
      to: "src/mastra/workflow/workflow.ts"
      via: "import { solverWorkflow }"
      pattern: "import.*solverWorkflow.*workflow/workflow"
---

<objective>
Extract the 3 step definitions from workflow.ts into individual files under src/mastra/workflow/steps/, leaving workflow.ts as a ~30-line composition file.

Purpose: Reduce workflow.ts from 1,448 lines to ~30 lines by moving each createStep() call to its own file, following the project's one-definition-per-file convention.
Output: 3 new step files in steps/, slimmed workflow.ts, passing type-check.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-file-refactoring/15-CONTEXT.md
@.planning/phases/15-file-refactoring/15-RESEARCH.md
@src/mastra/workflow/workflow.ts
@src/mastra/index.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From src/mastra/index.ts (the sole external consumer):
```typescript
import { solverWorkflow } from './workflow/workflow';
```
This import MUST continue to work after the split. The `solverWorkflow` named export MUST remain in workflow.ts.

From workflow.ts, the 3 step definitions are:
- `extractionStep` (lines 48-176, ~128 lines)
- `multiPerspectiveHypothesisStep` (lines 180-1294, ~1114 lines)
- `answerQuestionsStep` (lines 1297-1433, ~136 lines)
- Composition chain (lines 1435-1448, ~13 lines)
- Imports (lines 1-46, ~46 lines)

Each step uses `createStep` from `@mastra/core/workflows` and imports from:
- `../workflow-schemas` (Zod schemas and types)
- `../request-context-helpers` and `../request-context-types` (RequestContext helpers)
- `@/lib/workflow-events` (event types)
- `../logging-utils` (logging helpers)
- `../agent-utils` (streamWithRetry, generateWithRetry)
- Various agent files via mastra.getAgentById()

Steps do NOT import from each other. Each step is self-contained.

The project uses `verbatimModuleSyntax: true` — type-only imports MUST use `import type`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract step definitions to individual files</name>
  <files>
    src/mastra/workflow/steps/01-extract.ts,
    src/mastra/workflow/steps/02-hypothesize.ts,
    src/mastra/workflow/steps/03-answer.ts,
    src/mastra/workflow/workflow.ts
  </files>
  <action>
Create the `src/mastra/workflow/steps/` directory.

For each of the 3 steps, create a file that:
1. Contains the full `createStep()` call as a named export (e.g., `export const extractionStep = createStep({...})`)
2. Includes only the imports needed by that specific step
3. Uses `import type` for type-only imports (required by `verbatimModuleSyntax`)
4. Uses `../` prefix for sibling imports within the workflow directory (e.g., `import { streamWithRetry } from '../agent-utils'`)
5. Uses `@/` prefix for imports from outside the workflow directory (e.g., `import type { WorkflowTraceEvent } from '@/lib/workflow-events'`)

**File mapping (from 15-RESEARCH.md line ranges):**
- `steps/01-extract.ts`: extractionStep (lines 48-176 of workflow.ts)
- `steps/02-hypothesize.ts`: multiPerspectiveHypothesisStep (lines 180-1294 of workflow.ts)
- `steps/03-answer.ts`: answerQuestionsStep (lines 1297-1433 of workflow.ts)

**Then rewrite workflow.ts** to contain only:
1. Imports of the 3 step definitions from `./steps/01-extract`, `./steps/02-hypothesize`, `./steps/03-answer`
2. Imports of `createWorkflow` from `@mastra/core/workflows`
3. Imports of schemas from `./workflow-schemas` (only the ones needed for the workflow definition itself: `rawProblemInputSchema`, `questionsAnsweredSchema`, `workflowStateSchema`)
4. The `solverWorkflow` export with the `.then()/.map()/.then()/.then()/.commit()` composition chain

The resulting workflow.ts should be approximately 25-35 lines.

**Important constraints:**
- No re-export of steps in `src/mastra/workflow/index.ts` — steps are internal to the workflow definition
- Boilerplate (emit events, call agent, validate, log, bail) stays inline in each step file — no shared step helper abstraction
- No behavior changes — the step code must be identical to the original, just in new files
- Do not change any logic, variable names, or runtime behavior
  </action>
  <verify>
    <automated>cd /home/cervo/Code/LO-Solver && npx tsc --noEmit 2>&1 | grep -v "globals.css" | head -20</automated>
  </verify>
  <done>
    - 3 step files exist in src/mastra/workflow/steps/ with correct exports
    - workflow.ts is ~30 lines containing only imports and the composition chain
    - solverWorkflow export unchanged
    - npx tsc --noEmit shows no new errors (only pre-existing globals.css error)
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit workflow.ts split</name>
  <files>
    src/mastra/workflow/steps/01-extract.ts,
    src/mastra/workflow/steps/02-hypothesize.ts,
    src/mastra/workflow/steps/03-answer.ts,
    src/mastra/workflow/workflow.ts
  </files>
  <action>
Stage and commit all 4 files (3 new step files + modified workflow.ts) as one atomic commit.

Commit message: "Extract workflow step definitions to individual files"

Verify the commit includes exactly the expected files and no others.
  </action>
  <verify>
    <automated>cd /home/cervo/Code/LO-Solver && git log --oneline -1 && git diff --stat HEAD~1</automated>
  </verify>
  <done>
    - One atomic commit containing the 3 new step files and the slimmed workflow.ts
    - No other files included in the commit
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with only the pre-existing globals.css error
2. `wc -l src/mastra/workflow/workflow.ts` shows ~25-35 lines
3. `wc -l src/mastra/workflow/steps/*.ts` shows 3 files totaling ~1,380 lines
4. `grep "solverWorkflow" src/mastra/index.ts` confirms the import still exists
5. `git log --oneline -1` shows the atomic commit
</verification>

<success_criteria>
- workflow.ts reduced from 1,448 lines to ~30 lines
- 3 step files created with correct named exports
- No new TypeScript errors
- Single atomic commit
- src/mastra/index.ts import of solverWorkflow still works
</success_criteria>

<output>
After completion, create `.planning/phases/15-file-refactoring/15-01-SUMMARY.md`
</output>
