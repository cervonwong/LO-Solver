---
phase: 33-provider-foundation
plan: 05
type: execute
wave: 3
depends_on: ["33-04"]
files_modified:
  - src/mastra/workflow/01-structured-problem-extractor-agent.ts
  - src/mastra/workflow/02-dispatcher-agent.ts
  - src/mastra/workflow/02-improver-dispatcher-agent.ts
  - src/mastra/workflow/02-initial-hypothesizer-agent.ts
  - src/mastra/workflow/02-synthesizer-agent.ts
  - src/mastra/workflow/03a-rule-tester-agent.ts
  - src/mastra/workflow/03a-sentence-tester-agent.ts
  - src/mastra/workflow/03a-verifier-orchestrator-agent.ts
  - src/mastra/workflow/03a2-verifier-feedback-extractor-agent.ts
  - src/mastra/workflow/03b-rules-improver-agent.ts
  - src/mastra/workflow/03b2-rules-improvement-extractor-agent.ts
  - src/mastra/workflow/04-question-answerer-agent.ts
autonomous: false
requirements:
  - PROV-04

must_haves:
  truths:
    - "Each of the 8 tool-free agents has a claudeCodeModel assigned (sonnet for extractors/testers, opus for dispatchers/answerer)"
    - "The 4 tool-using agents also have claudeCodeModel assigned (prepared for Phase 34)"
    - "All 8 tool-free agents produce correct structured output through Claude Code provider"
  artifacts:
    - path: "src/mastra/workflow/01-structured-problem-extractor-agent.ts"
      provides: "First agent with claudeCodeModel"
      contains: "claudeCodeModel"
    - path: "src/mastra/workflow/04-question-answerer-agent.ts"
      provides: "Answerer agent with opus model"
      contains: "claude-opus-4-6"
  key_links:
    - from: "src/mastra/workflow/01-structured-problem-extractor-agent.ts"
      to: "src/mastra/workflow/agent-factory.ts"
      via: "createWorkflowAgent with claudeCodeModel config"
      pattern: "claudeCodeModel.*claude-sonnet"
---

<objective>
Add claudeCodeModel to all 12 agent files and validate structured output through a real solve.

Purpose: Configure every agent for Claude Code model resolution and validate that the 8 tool-free agents produce correct structured JSON output. The 4 tool-using agents get prepared for Phase 34.

Output: All 12 agents configured for Claude Code, runtime-validated structured output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-provider-foundation/33-CONTEXT.md
@.planning/phases/33-provider-foundation/33-RESEARCH.md
@.planning/phases/33-provider-foundation/33-04-SUMMARY.md

@src/mastra/workflow/agent-factory.ts
@src/mastra/workflow/01-structured-problem-extractor-agent.ts
@src/mastra/workflow/02-dispatcher-agent.ts
@src/mastra/workflow/02-improver-dispatcher-agent.ts
@src/mastra/workflow/02-initial-hypothesizer-agent.ts
@src/mastra/workflow/02-synthesizer-agent.ts
@src/mastra/workflow/03a-rule-tester-agent.ts
@src/mastra/workflow/03a-sentence-tester-agent.ts
@src/mastra/workflow/03a-verifier-orchestrator-agent.ts
@src/mastra/workflow/03a2-verifier-feedback-extractor-agent.ts
@src/mastra/workflow/03b-rules-improver-agent.ts
@src/mastra/workflow/03b2-rules-improvement-extractor-agent.ts
@src/mastra/workflow/04-question-answerer-agent.ts

<interfaces>
<!-- From Plan 04 output. -->

From src/mastra/workflow/agent-factory.ts (after Plan 04):
```typescript
export interface WorkflowAgentConfig {
  id: string;
  name: string;
  instructions: string | { role: 'system'; content: string };
  productionModel: string;
  testingModel?: string;
  claudeCodeModel?: string;  // Added in Plan 04
  tools?: ToolsInput;
  useUnicodeNormalizer?: boolean;
  requestContextSchema?: import('zod').ZodType<any>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add claudeCodeModel to all 12 agent files</name>
  <files>src/mastra/workflow/01-structured-problem-extractor-agent.ts, src/mastra/workflow/02-dispatcher-agent.ts, src/mastra/workflow/02-improver-dispatcher-agent.ts, src/mastra/workflow/02-initial-hypothesizer-agent.ts, src/mastra/workflow/02-synthesizer-agent.ts, src/mastra/workflow/03a-rule-tester-agent.ts, src/mastra/workflow/03a-sentence-tester-agent.ts, src/mastra/workflow/03a-verifier-orchestrator-agent.ts, src/mastra/workflow/03a2-verifier-feedback-extractor-agent.ts, src/mastra/workflow/03b-rules-improver-agent.ts, src/mastra/workflow/03b2-rules-improvement-extractor-agent.ts, src/mastra/workflow/04-question-answerer-agent.ts</files>
  <action>
Mechanical addition of `claudeCodeModel` field to each agent's `createWorkflowAgent()` config object. Each file requires adding a single line. The field goes next to the existing `productionModel` and `testingModel` fields.

**Extraction agents (use `'claude-sonnet-4-6'` per user decision):**
- `01-structured-problem-extractor-agent.ts`: add `claudeCodeModel: 'claude-sonnet-4-6',`
- `03a2-verifier-feedback-extractor-agent.ts`: add `claudeCodeModel: 'claude-sonnet-4-6',`
- `03b2-rules-improvement-extractor-agent.ts`: add `claudeCodeModel: 'claude-sonnet-4-6',`

**Tester agents (use `'claude-sonnet-4-6'` -- extraction-class work):**
- `03a-rule-tester-agent.ts`: add `claudeCodeModel: 'claude-sonnet-4-6',`
- `03a-sentence-tester-agent.ts`: add `claudeCodeModel: 'claude-sonnet-4-6',`

**Reasoning/dispatcher agents (use `'claude-opus-4-6'` per user decision):**
- `02-dispatcher-agent.ts`: add `claudeCodeModel: 'claude-opus-4-6',`
- `02-improver-dispatcher-agent.ts`: add `claudeCodeModel: 'claude-opus-4-6',`
- `04-question-answerer-agent.ts`: add `claudeCodeModel: 'claude-opus-4-6',`

**Tool-using agents (prepared for Phase 34, use reasoning-class model):**
- `02-initial-hypothesizer-agent.ts`: add `claudeCodeModel: 'claude-opus-4-6',`
- `02-synthesizer-agent.ts`: add `claudeCodeModel: 'claude-opus-4-6',`
- `03a-verifier-orchestrator-agent.ts`: add `claudeCodeModel: 'claude-opus-4-6',`
- `03b-rules-improver-agent.ts`: add `claudeCodeModel: 'claude-opus-4-6',`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | tail -5 && echo "---" && grep -r 'claudeCodeModel' src/mastra/workflow/*-agent.ts | wc -l</automated>
  </verify>
  <done>All 12 agent files have claudeCodeModel configured (grep count = 12). npx tsc --noEmit passes. Sonnet assigned to extractors/testers, opus assigned to dispatchers/reasoners/answerer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate Claude Code structured output with a real solve</name>
  <files>none</files>
  <action>
CHECKPOINT: Human-verify that Claude Code provider produces correct structured output.

What was built: Claude Code provider integration with 3-way model resolution, auth gate, and error handling. The 8 tool-free agents should produce correct structured JSON output through the Claude Code provider. The RESEARCH.md flags .catchall() schema on structuredProblemDataSchema as a potential silent fallback risk where Claude Code's constrained decoding may return prose instead of structured JSON.

Steps to verify:
1. Ensure the dev server is running (`npm run dev`)
2. In the UI, switch the provider toggle to "Claude Code" (if Plan 06 is done) OR manually send a POST to `/api/solve` with `providerMode: 'claude-code'` in the request body
3. Submit a simple linguistics problem (e.g., one from the examples/ folder)
4. Watch for:
   - Auth gate: Should succeed if Claude Code CLI is authenticated (run `claude auth login` first if needed)
   - Step 1 (extraction): The structured-problem-extractor-agent must return a valid JSON object, NOT prose text. This is the highest-risk agent due to the `.catchall()` schema.
   - If extraction returns prose/null instead of structured JSON, the `.catchall()` workaround is needed: either remove `.catchall()` from the schema or add `jsonPromptInjection: true` to the provider config
5. The solve will fail at Step 2 (tool-using agents not yet configured for Claude Code -- that is Phase 34). This is expected. The key validation is that Step 1 extraction produces structured output.

Resume signal: Type "approved" if Step 1 extraction returned structured JSON. If it returned prose text, describe the failure so we can add a .catchall() fix as a gap closure task.
  </action>
  <verify>
    <automated>echo "Manual checkpoint -- user verifies Step 1 extraction returns structured JSON through Claude Code provider"</automated>
  </verify>
  <done>Step 1 extraction agent produces structured JSON output (not prose text) through Claude Code provider, confirming PROV-04 and ruling out .catchall() schema silent fallback.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (only pre-existing globals.css error)
2. `grep -r 'claudeCodeModel' src/mastra/workflow/*-agent.ts | wc -l` returns 12 (all agents configured)
3. Runtime validation: Step 1 extraction produces structured JSON through Claude Code provider
</verification>

<success_criteria>
- All 12 agents have claudeCodeModel field set
- Sonnet for extractors/testers, opus for dispatchers/reasoners/answerer
- Step 1 extraction agent produces structured JSON output through Claude Code (runtime-validated)
</success_criteria>

<output>
After completion, create `.planning/phases/33-provider-foundation/33-05-SUMMARY.md`
</output>
