---
phase: 33-provider-foundation
plan: 03
type: execute
wave: 1
depends_on: ["33-01"]
files_modified:
  - src/evals/run.ts
  - src/evals/storage.ts
  - src/evals/zero-shot-solver.ts
  - src/app/evals/page.tsx
autonomous: true
requirements:
  - PROV-01

must_haves:
  truths:
    - "No references to modelMode or model-mode remain in src/evals/ or src/app/evals/"
    - "Old eval JSON files with modelMode are backward-compatible (migrated on read)"
    - "Full codebase compiles with zero new type errors"
  artifacts:
    - path: "src/evals/storage.ts"
      provides: "EvalRunResult with providerMode field and backward compat"
      contains: "providerMode"
    - path: "src/evals/run.ts"
      provides: "CLI maps --mode flag to providerMode internally"
      contains: "providerMode"
  key_links:
    - from: "src/evals/storage.ts"
      to: "src/mastra/openrouter.ts"
      via: "imports ProviderMode type"
      pattern: "import.*ProviderMode"
    - from: "src/evals/run.ts"
      to: "src/evals/storage.ts"
      via: "writes providerMode to result"
      pattern: "providerMode"
---

<objective>
Rename modelMode to providerMode in eval files and evals UI, with backward-compatible JSON loading.

Purpose: Complete the codebase-wide rename. After this plan, no references to modelMode or model-mode remain anywhere in src/. Runs in parallel with Plan 02 (both depend only on Plan 01's core type changes).

Output: All eval files compiling with providerMode, old eval JSON files backward-compatible.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-provider-foundation/33-CONTEXT.md
@.planning/phases/33-provider-foundation/33-01-SUMMARY.md

@src/mastra/openrouter.ts
@src/evals/run.ts
@src/evals/storage.ts
@src/evals/zero-shot-solver.ts
@src/app/evals/page.tsx

<interfaces>
<!-- Post Plan 01 types. -->

From src/mastra/openrouter.ts (after Plan 01):
```typescript
export type ProviderMode = 'openrouter-testing' | 'openrouter-production' | 'claude-code';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename modelMode to providerMode in eval files and evals UI</name>
  <files>src/evals/run.ts, src/evals/storage.ts, src/evals/zero-shot-solver.ts, src/app/evals/page.tsx</files>
  <action>
Update eval system files to use the new ProviderMode type. Keep backward-compatible where possible -- defer full --provider flag to Phase 36.

**src/evals/storage.ts:**
- Rename `modelMode` field in the `EvalRunResult` interface to `providerMode` with type `ProviderMode` (import from openrouter.ts)
- Keep backward compat: if loading old JSON files with `modelMode`, map them ('testing' -> 'openrouter-testing', 'production' -> 'openrouter-production')

**src/evals/run.ts:**
- Rename internal `modelMode` references to `providerMode`
- The CLI `--mode testing|production` flag stays as-is but maps to `'openrouter-testing'` / `'openrouter-production'` internally
- Update the object that writes `modelMode:` to write `providerMode:` instead

**src/evals/zero-shot-solver.ts:**
- Rename `model-mode` to `provider-mode` in RequestContext usage
- Update `ModelMode` import to `ProviderMode`

**src/app/evals/page.tsx:**
- Rename `modelMode` references in type and display to `providerMode`
- Display value can show the raw string ('openrouter-testing', etc.)

After all changes, run `npx tsc --noEmit` and verify the full codebase compiles (assuming Plans 01 and 02 are also done). Search for any remaining `modelMode` or `model-mode` references in `src/` (excluding `node_modules`) and fix them.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | tail -5 && echo "---" && grep -r 'modelMode\|model-mode' src/ --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v '.d.ts' | wc -l</automated>
  </verify>
  <done>Zero new type errors from npx tsc --noEmit (only the pre-existing globals.css error). No references to `modelMode` or `model-mode` remain anywhere in src/ (grep returns 0). Eval files updated with backward-compatible providerMode.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (only pre-existing globals.css error) — requires Plans 01+02+03 all complete
2. `grep -r 'modelMode\|model-mode' src/ --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v '.d.ts'` returns 0 results
3. `grep 'providerMode' src/evals/storage.ts` shows the new field name
</verification>

<success_criteria>
- All eval files compile with providerMode
- Old eval JSON files with modelMode are loaded with backward compat migration
- No references to old modelMode/model-mode remain in src/
</success_criteria>

<output>
After completion, create `.planning/phases/33-provider-foundation/33-03-SUMMARY.md`
</output>
