---
phase: 33-provider-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/mastra/claude-code-provider.ts
  - src/mastra/openrouter.ts
  - src/mastra/workflow/workflow-schemas.ts
  - src/mastra/workflow/request-context-types.ts
  - src/mastra/workflow/request-context-helpers.ts
  - src/mastra/workflow/agent-factory.ts
  - src/mastra/workflow/steps/01-extract.ts
  - src/mastra/workflow/steps/02-hypothesize.ts
  - src/mastra/workflow/steps/02a-dispatch.ts
  - src/mastra/workflow/steps/02b-hypothesize.ts
  - src/mastra/workflow/steps/02c-verify.ts
  - src/mastra/workflow/steps/02d-synthesize.ts
  - src/mastra/workflow/steps/03-answer.ts
  - src/mastra/workflow/03a-rule-tester-agent.ts
  - src/mastra/workflow/03a-sentence-tester-agent.ts
  - src/evals/run.ts
  - src/evals/storage.ts
  - src/evals/zero-shot-solver.ts
  - src/app/evals/page.tsx
autonomous: true
requirements:
  - PROV-01
  - PROV-05

must_haves:
  truths:
    - "providerMode enum with three values exists and compiles"
    - "Existing OpenRouter modes continue to work unchanged (openrouter-testing maps to old testing, openrouter-production maps to old production)"
    - "Claude Code provider module exists with comprehensive disallowedTools"
    - "npm packages ai-sdk-provider-claude-code and @anthropic-ai/claude-agent-sdk are installed"
  artifacts:
    - path: "src/mastra/claude-code-provider.ts"
      provides: "Claude Code provider singleton with security sandbox"
      exports: ["claudeCode", "CLAUDE_CODE_DISALLOWED_TOOLS"]
    - path: "src/mastra/openrouter.ts"
      provides: "ProviderMode type and activeModelId with claude-code branch"
      exports: ["ProviderMode", "activeModelId"]
      contains: "ProviderMode"
    - path: "src/mastra/workflow/workflow-schemas.ts"
      provides: "providerMode field in workflow state and input schemas"
      contains: "providerMode"
  key_links:
    - from: "src/mastra/workflow/workflow-schemas.ts"
      to: "src/mastra/openrouter.ts"
      via: "ProviderMode type import"
      pattern: "providerMode.*enum"
    - from: "src/mastra/workflow/request-context-types.ts"
      to: "src/mastra/openrouter.ts"
      via: "provider-mode key using ProviderMode type"
      pattern: "'provider-mode'.*ProviderMode"
    - from: "src/mastra/workflow/steps/01-extract.ts"
      to: "src/mastra/workflow/workflow-schemas.ts"
      via: "reads providerMode from inputData"
      pattern: "providerMode"
---

<objective>
Install Claude Code provider packages, create the provider module, and rename modelMode to providerMode across all backend files.

Purpose: Establish the foundational types and provider that all subsequent plans depend on. The rename from 2-value modelMode to 3-value providerMode is the single most cross-cutting change and must happen first.

Output: Claude Code provider module, ProviderMode type system, all backend files compiling with the rename.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-provider-foundation/33-CONTEXT.md
@.planning/phases/33-provider-foundation/33-RESEARCH.md

@src/mastra/openrouter.ts
@src/mastra/workflow/workflow-schemas.ts
@src/mastra/workflow/request-context-types.ts
@src/mastra/workflow/request-context-helpers.ts
@src/mastra/workflow/agent-factory.ts
@src/mastra/workflow/agent-utils.ts
@src/mastra/workflow/steps/01-extract.ts
@src/mastra/workflow/steps/02-hypothesize.ts
@src/mastra/workflow/steps/02a-dispatch.ts
@src/mastra/workflow/steps/02b-hypothesize.ts
@src/mastra/workflow/steps/02c-verify.ts
@src/mastra/workflow/steps/02d-synthesize.ts
@src/mastra/workflow/steps/03-answer.ts
@src/mastra/workflow/03a-rule-tester-agent.ts
@src/mastra/workflow/03a-sentence-tester-agent.ts
@src/evals/run.ts
@src/evals/storage.ts
@src/evals/zero-shot-solver.ts
@src/app/evals/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/mastra/openrouter.ts:
```typescript
export type ModelMode = 'testing' | 'production';
export const TESTING_MODEL = 'openai/gpt-oss-120b';
export function activeModelId(mode: ModelMode, productionModel: string): string;
export function createOpenRouterProvider(apiKey: string): OpenRouterProvider;
export const openrouter: OpenRouterProvider;
```

From src/mastra/workflow/workflow-schemas.ts:
```typescript
export const workflowStateSchema = z.object({
  modelMode: z.enum(['testing', 'production']).default('testing'),
  // ...other fields
});
export const rawProblemInputSchema = z.object({
  modelMode: z.enum(['testing', 'production']).default('testing'),
  // ...other fields
});
```

From src/mastra/workflow/request-context-types.ts:
```typescript
export interface WorkflowRequestContext {
  'model-mode': ModelMode;
  'openrouter-provider'?: OpenRouterProvider;
  // ...other fields
}
```

From src/mastra/workflow/agent-factory.ts:
```typescript
export interface WorkflowAgentConfig {
  testingModel?: string;
  productionModel: string;
  // ...other fields
}
// model callback reads: requestContext?.get('model-mode') as ModelMode
```

From src/mastra/workflow/steps/02-hypothesize.ts:
```typescript
export interface HypothesizeContext {
  modelMode: ModelMode;
  // ...other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and create Claude Code provider module</name>
  <files>package.json, src/mastra/claude-code-provider.ts</files>
  <action>
1. Install packages: `npm install ai-sdk-provider-claude-code @anthropic-ai/claude-agent-sdk`

2. Create `src/mastra/claude-code-provider.ts` as a singleton provider module mirroring the `openrouter.ts` pattern:
   - Import `createClaudeCode` from `ai-sdk-provider-claude-code`
   - Export `CLAUDE_CODE_DISALLOWED_TOOLS` as a readonly array containing ALL built-in Claude Code tools: `Bash`, `Read`, `Write`, `Edit`, `Glob`, `Grep`, `NotebookEdit`, `WebSearch`, `WebFetch`, `Agent`, `Task`, `TaskOutput`, `TaskStop`, `TodoRead`, `TodoWrite`, `AskUserQuestion`, `EnterPlanMode`, `ExitPlanMode`, `Config`
   - Export `claudeCode` as the configured singleton: `createClaudeCode({ disallowedTools: [...CLAUDE_CODE_DISALLOWED_TOOLS], permissionMode: 'bypassPermissions', allowDangerouslySkipPermissions: true })`
   - Singleton is correct because Claude Code uses CLI-level auth, not per-request API keys
   - Do NOT set `settingSources` or `systemPrompt` in the config (solver agents have their own instructions)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | head -20</automated>
  </verify>
  <done>Package installed and provider module compiles. CLAUDE_CODE_DISALLOWED_TOOLS contains all 20 built-in tool names. Provider uses bypassPermissions for server-side execution.</done>
</task>

<task type="auto">
  <name>Task 2: Rename modelMode to providerMode in workflow files</name>
  <files>src/mastra/openrouter.ts, src/mastra/workflow/workflow-schemas.ts, src/mastra/workflow/request-context-types.ts, src/mastra/workflow/request-context-helpers.ts, src/mastra/workflow/agent-factory.ts, src/mastra/workflow/steps/01-extract.ts, src/mastra/workflow/steps/02-hypothesize.ts, src/mastra/workflow/steps/02a-dispatch.ts, src/mastra/workflow/steps/02b-hypothesize.ts, src/mastra/workflow/steps/02c-verify.ts, src/mastra/workflow/steps/02d-synthesize.ts, src/mastra/workflow/steps/03-answer.ts, src/mastra/workflow/03a-rule-tester-agent.ts, src/mastra/workflow/03a-sentence-tester-agent.ts</files>
  <action>
Rename `ModelMode` to `ProviderMode` with 3 values across the workflow codebase. Strategy: change the type first, then fix all compilation errors.

**src/mastra/openrouter.ts:**
- Replace `export type ModelMode = 'testing' | 'production'` with `export type ProviderMode = 'openrouter-testing' | 'openrouter-production' | 'claude-code'`
- Update `activeModelId(mode: ModelMode, productionModel: string)` signature to `activeModelId(providerMode: ProviderMode, productionModel: string, claudeCodeModel?: string)`:
  - If `providerMode === 'claude-code'`: return `'claude-code/' + (claudeCodeModel ?? 'sonnet')` (for trace event display)
  - If `providerMode === 'openrouter-production'`: return `productionModel`
  - Otherwise: return `TESTING_MODEL`
- Keep all other exports (openrouter, createOpenRouterProvider, TESTING_MODEL) unchanged

**src/mastra/workflow/workflow-schemas.ts:**
- Replace `modelMode: z.enum(['testing', 'production']).default('testing')` with `providerMode: z.enum(['openrouter-testing', 'openrouter-production', 'claude-code']).default('openrouter-testing')` in both `workflowStateSchema` and `rawProblemInputSchema`
- Update `initializeWorkflowState()`: change `modelMode: 'testing' as const` to `providerMode: 'openrouter-testing' as const`

**src/mastra/workflow/request-context-types.ts:**
- Change import from `ModelMode` to `ProviderMode`
- Rename key `'model-mode': ModelMode` to `'provider-mode': ProviderMode`
- Update the comment accordingly

**src/mastra/workflow/request-context-helpers.ts:**
- `extractCostFromResult`: Since this function doesn't have access to providerMode, keep it as-is (Claude Code's `providerMetadata?.openrouter` will be undefined so it naturally returns 0).

**src/mastra/workflow/agent-factory.ts:**
- Change import from `ModelMode` to `ProviderMode`
- Update model callback: replace `requestContext?.get('model-mode') as ModelMode` with `requestContext?.get('provider-mode') as ProviderMode`
- Replace mode logic:
  - Old: `mode === 'production' ? productionModel : testingModel`
  - New: `providerMode === 'openrouter-production' ? productionModel : testingModel` (the claude-code branch will be added in Plan 02)

**src/mastra/workflow/03a-rule-tester-agent.ts and 03a-sentence-tester-agent.ts:**
- Update requestContextSchema: `'model-mode': z.enum(['testing', 'production'])` becomes `'provider-mode': z.enum(['openrouter-testing', 'openrouter-production', 'claude-code'])`

**All step files (01-extract.ts, 02-hypothesize.ts, 02a-dispatch.ts, 02b-hypothesize.ts, 02c-verify.ts, 02d-synthesize.ts, 03-answer.ts):**
- Replace all `import type { ModelMode }` with `import type { ProviderMode }`
- Replace `import { activeModelId }` (already imported, just signature changed)
- Replace all `ctx.modelMode` / `state.modelMode` / `inputData.modelMode` references with `ctx.providerMode` / `state.providerMode` / `inputData.providerMode`
- Replace all `requestContext.set('model-mode', ...)` with `requestContext.set('provider-mode', ...)`
- Replace all type casts `as ModelMode` with `as ProviderMode`
- Update `activeModelId()` calls: old signature was `activeModelId(mode, productionModel)`, new is `activeModelId(providerMode, productionModel)` (the third arg claudeCodeModel is optional, not needed yet)
- In `02-hypothesize.ts`: update `HypothesizeContext.modelMode: ModelMode` to `providerMode: ProviderMode`, and the testing-mode guards `state.modelMode === 'testing'` become `state.providerMode === 'openrouter-testing'`

After all changes, run `npx tsc --noEmit` and fix any remaining type errors in these files.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | tail -5</automated>
  </verify>
  <done>Zero new type errors from npx tsc --noEmit (only the pre-existing globals.css error, plus expected errors from not-yet-updated eval files). No references to `modelMode` or `model-mode` remain in src/mastra/ (grep confirms). providerMode default is 'openrouter-testing'.</done>
</task>

<task type="auto">
  <name>Task 3: Rename modelMode to providerMode in eval files and evals UI</name>
  <files>src/evals/run.ts, src/evals/storage.ts, src/evals/zero-shot-solver.ts, src/app/evals/page.tsx</files>
  <action>
Update eval system files to use the new ProviderMode type. Keep backward-compatible where possible -- defer full --provider flag to Phase 36.

**src/evals/storage.ts:**
- Rename `modelMode` field in the `EvalRunResult` interface to `providerMode` with type `ProviderMode` (import from openrouter.ts)
- Keep backward compat: if loading old JSON files with `modelMode`, map them ('testing' -> 'openrouter-testing', 'production' -> 'openrouter-production')

**src/evals/run.ts:**
- Rename internal `modelMode` references to `providerMode`
- The CLI `--mode testing|production` flag stays as-is but maps to `'openrouter-testing'` / `'openrouter-production'` internally
- Update the object that writes `modelMode:` to write `providerMode:` instead

**src/evals/zero-shot-solver.ts:**
- Rename `model-mode` to `provider-mode` in RequestContext usage
- Update `ModelMode` import to `ProviderMode`

**src/app/evals/page.tsx:**
- Rename `modelMode` references in type and display to `providerMode`
- Display value can show the raw string ('openrouter-testing', etc.)

After all changes, run `npx tsc --noEmit` and verify the full codebase compiles. Search for any remaining `modelMode` or `model-mode` references in `src/` (excluding `node_modules`) and fix them.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | tail -5 && echo "---" && grep -r 'modelMode\|model-mode' src/ --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v '.d.ts' | wc -l</automated>
  </verify>
  <done>Zero new type errors from npx tsc --noEmit (only the pre-existing globals.css error). No references to `modelMode` or `model-mode` remain anywhere in src/ (grep returns 0). Eval files updated with backward-compatible providerMode. All existing OpenRouter functionality preserved.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (only pre-existing globals.css error)
2. `grep -r 'modelMode\|model-mode' src/ --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v '.d.ts'` returns 0 results
3. `grep -r 'providerMode' src/mastra/workflow/workflow-schemas.ts` shows the new enum
4. `cat src/mastra/claude-code-provider.ts` shows provider with disallowedTools
</verification>

<success_criteria>
- ProviderMode type with 3 values replaces ModelMode everywhere
- Claude Code provider module exists with 20 disallowed tools
- All backend files compile with the rename
- Eval files updated with backward-compatible providerMode
- No references to old modelMode/model-mode remain in src/
</success_criteria>

<output>
After completion, create `.planning/phases/33-provider-foundation/33-01-SUMMARY.md`
</output>
