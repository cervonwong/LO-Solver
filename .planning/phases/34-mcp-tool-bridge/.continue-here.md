---
phase: 34-mcp-tool-bridge
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-13T03:33:40.009Z
---

<current_state>
Plan 34-01 (MCP tool bridge infrastructure) is complete with SUMMARY.md.
Plan 34-02 (wire MCP into workflow steps + E2E validation) has task 1 done (wiring), task 2 (E2E validation) is blocked on runtime issues being debugged.

Two bug-fix commits have been made on top of the original plan work:
1. Transport reuse fix — changed `claude-code-provider` to a factory function so each agent.generate() gets a fresh MCP server/transport (was causing "Already connected to a transport" errors)
2. Performance fix — tester sub-agents (rule-tester, sentence-tester) now use a stripped RequestContext without the MCP provider factory, falling back to singleton Claude Code (no MCP overhead). Also increased timeout from 10min to 20min for Claude Code mode.

E2E validation has NOT yet passed — user reported timeout at 30min during multi-perspective-hypothesis step. The transport reuse error is fixed but Claude Code mode is inherently slow due to subprocess spawning per agent call.
</current_state>

<completed_work>

- Plan 34-01: Full MCP tool bridge infrastructure — COMPLETE (SUMMARY.md exists)
- Plan 34-02 Task 1: Wire MCP server creation into workflow step files — COMPLETE (commits f153484, 973e6ea)
- Bug fix: MCP transport reuse error — COMPLETE (commit 3fc0bb6)
  - Changed `claude-code-provider` to `claude-code-provider-factory` in request-context-types.ts
  - `attachMcpProvider` now stores a factory function, not a provider instance
  - agent-factory.ts calls the factory per model() invocation
- Bug fix: Tester sub-agent performance — COMPLETE (commit 3e3d46b)
  - Tester sub-agents get stripped RequestContext (no claude-code-provider-factory)
  - Fall back to singleton Claude Code provider (no MCP overhead)
  - Timeout increased from 600s to 1200s for Claude Code mode
- Observability: Added [CLAUDE] console logging throughout (commit 3fc0bb6)
  - MCP server creation, provider creation, tool calls with duration, agent stream start/finish
</completed_work>

<remaining_work>

- Plan 34-02 Task 2: E2E validation — user must re-test with latest fixes
  - Previous attempt timed out at 30min (before performance fixes were applied)
  - Need to verify: no transport errors, faster tester calls, full solve completes
  - Then create 34-02-SUMMARY.md, update STATE.md and ROADMAP.md
- Phase verification via gsd-verifier after task 2 passes
</remaining_work>

<decisions_made>

- Used factory pattern for claude-code-provider instead of storing a single instance, because MCP server transports can only be connected once — each agent.generate() needs its own fresh transport
- Tester sub-agents (rule-tester, sentence-tester) use singleton Claude Code (no MCP) because they don't need tools, just reasoning + structured output
- Timeout for Claude Code mode set to 20min (vs 10min for OpenRouter) because Claude Code spawns subprocesses per call which is inherently slower
- Perspectives are parallelized (Promise.all) but verifier tool calls within each perspective remain sequential — this is a fundamental constraint of how Claude Code processes tool calls
</decisions_made>

<blockers>
- E2E validation not yet passed — need user to re-test with latest fixes applied
- Claude Code mode is inherently slower than OpenRouter due to subprocess spawning per agent call — this is a fundamental limitation, not a bug
</blockers>

<context>
The key architectural insight: in Claude Code mode, every agent.generate() spawns a Claude Code CLI subprocess. The verifier orchestrator agent makes ~10-20 sequential tool calls (testRule, testSentence), and each tool call runs a tester sub-agent. Before the fix, each tester sub-agent ALSO created a full MCP server + Claude Code provider. After the fix, testers use the lightweight singleton provider.

The RequestContext key was renamed from 'claude-code-provider' to 'claude-code-provider-factory' to reflect the new factory pattern. This required changes in request-context-types.ts, 02-hypothesize.ts (attachMcpProvider), and agent-factory.ts (model resolver).

Files modified across all commits in this session:
- src/mastra/mcp/mcp-tool-bridge.ts (logging, tool name param)
- src/mastra/workflow/agent-factory.ts (factory pattern, logging)
- src/mastra/workflow/agent-utils.ts (timeout, logging)
- src/mastra/workflow/request-context-types.ts (factory type)
- src/mastra/workflow/steps/02-hypothesize.ts (attachMcpProvider factory)
- src/mastra/workflow/03a-rule-tester-tool.ts (stripped RequestContext)
- src/mastra/workflow/03a-sentence-tester-tool.ts (stripped RequestContext)
</context>

<next_action>
Start with: User re-tests E2E solve in Claude Code mode. If it passes, create 34-02-SUMMARY.md, update STATE.md and ROADMAP.md, then run phase verification. If it still times out, investigate which specific agent call is the bottleneck using the [CLAUDE] console logs.
</next_action>
