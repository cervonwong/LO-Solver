---
phase: 27-dead-code-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mastra/workflow/index.ts
  - src/mastra/workflow/README.md
  - src/mastra/workflow/shared-memory.ts
  - src/mastra/workflow/02a-initial-hypothesis-extractor-agent.ts
  - src/mastra/workflow/02a-initial-hypothesis-extractor-instructions.ts
  - package.json
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03]

must_haves:
  truths:
    - "No deprecated 02a-initial-hypothesis-extractor files exist in the workflow directory"
    - "No shared-memory.ts file exists in the workflow directory"
    - "index.ts does not import or re-export the deleted agent"
    - "README.md file tree and agent table do not reference deleted files"
    - "Running Knip reports zero actionable unused exports (only documented false positives remain)"
    - "An npm run audit script runs Knip for future audits"
  artifacts:
    - path: "src/mastra/workflow/index.ts"
      provides: "Agent/tool registration without deprecated agent"
      contains: "workflowAgents"
    - path: "package.json"
      provides: "Knip devDependency and audit script"
      contains: "knip"
    - path: "src/mastra/workflow/README.md"
      provides: "Accurate file tree and agent table"
  key_links:
    - from: "src/mastra/workflow/index.ts"
      to: "src/mastra/index.ts"
      via: "workflowAgents spread import"
      pattern: "workflowAgents"
---

<objective>
Remove deprecated files and dead code, install Knip for unused export detection, and clean all dead exports from the codebase.

Purpose: Eliminate known dead code (deprecated agent files, shared-memory.ts) and establish automated dead code detection (Knip) for ongoing maintenance.
Output: A codebase with no deprecated files, no unused exports, and a permanent `npm run audit` script.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dead-code-type-safety/27-CONTEXT.md
@.planning/phases/27-dead-code-type-safety/27-RESEARCH.md

@src/mastra/workflow/index.ts
@src/mastra/workflow/README.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete deprecated files and clean references</name>
  <files>
    src/mastra/workflow/02a-initial-hypothesis-extractor-agent.ts
    src/mastra/workflow/02a-initial-hypothesis-extractor-instructions.ts
    src/mastra/workflow/shared-memory.ts
    src/mastra/workflow/index.ts
    src/mastra/workflow/README.md
  </files>
  <action>
1. Delete the 3 deprecated files:
   - `src/mastra/workflow/02a-initial-hypothesis-extractor-agent.ts`
   - `src/mastra/workflow/02a-initial-hypothesis-extractor-instructions.ts`
   - `src/mastra/workflow/shared-memory.ts`

2. Edit `src/mastra/workflow/index.ts`:
   - Remove line 2: `import { initialHypothesisExtractorAgent } from './02a-initial-hypothesis-extractor-agent';`
   - Remove line 23: `initialHypothesisExtractorAgent, // DEPRECATED: kept for backward compat with verify-improve loop`
   - Leave all other imports and exports unchanged

3. Edit `src/mastra/workflow/README.md`:
   - In the Agents table (around line 50): remove the "Initial Hypothesis Extractor" row entirely
   - In the File Structure section (around line 134): remove line `├── shared-memory.ts                      # Shared memory utilities`
   - In the File Structure section (around line 138): remove line `├── 02a-initial-hypothesis-extractor-*.ts # Step 2: Extract rules JSON`
   - Also update the Mermaid diagram in Step 2 (lines 24-26): remove the `IHE[Initial Hypothesis Extractor Agent]` node and the `IH --> IHE` and `IHE --> RULES1` edges. Connect directly: `IH --> RULES1[Rules + Vocabulary]`
   - Update the "Dual-Agent Pattern" section description if it references the Initial Hypothesis Extractor as an example

4. Before each deletion, verify no remaining references exist:
   - grep src/ for `02a-initial-hypothesis-extractor` (should only find index.ts and README.md at this point)
   - grep src/ for `shared-memory` (should only find README.md)
   - grep src/ for `generateWorkflowIds` (should find zero matches)
  </action>
  <verify>
    <automated>ls src/mastra/workflow/02a-initial-hypothesis-extractor-*.ts src/mastra/workflow/shared-memory.ts 2>&1 | grep -c "No such file" && grep -rc "02a-initial-hypothesis-extractor\|shared-memory\|generateWorkflowIds" src/mastra/workflow/index.ts src/mastra/workflow/README.md; echo "Exit: $?"</automated>
  </verify>
  <done>Three files deleted. index.ts has no import or export of the deprecated agent. README.md file tree, agent table, and Mermaid diagram no longer reference deleted files. grep confirms zero remaining references to deleted artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Install Knip, run audit, clean unused exports</name>
  <files>
    package.json
  </files>
  <action>
1. Install Knip as devDependency:
   ```
   npm install --save-dev knip@5.86.0
   ```

2. Add audit script to package.json scripts:
   ```json
   "audit": "knip"
   ```

3. Run `npx knip` and capture the full report.

4. Analyze the Knip output:
   - Mastra agent/tool registration exports from `index.ts` (like `structuredProblemExtractorAgent`, `initialHypothesizerAgent`, etc.) ARE used -- they are consumed via `...workflowAgents` spread in `src/mastra/index.ts`. If Knip flags individual named exports from `index.ts`, these are false positives because the object IS imported.
   - For ANY other flagged unused export: before deleting, run `grep -r "exportName" src/` to verify no static or dynamic usage exists. Also check for `requestContext.get('key-name')` patterns if the export is a context key.

5. For each confirmed unused export:
   - If it is the only export from a file: delete the file
   - If it is one of many exports: remove just the export

6. For Mastra registration false positives: add a comment block at the top of `src/mastra/workflow/index.ts`:
   ```typescript
   // Knip: workflowAgents and workflowTools are consumed by src/mastra/index.ts
   // via spread operator. Individual agent/tool exports may appear unused to
   // static analysis but are required for Mastra runtime registration.
   ```

7. Run `npx knip` again to confirm the report is clean (zero actionable findings).
  </action>
  <verify>
    <automated>cd /home/cervo/Code/LO-Solver && npx knip 2>&1 | tail -20</automated>
  </verify>
  <done>Knip is installed as devDependency. `npm run audit` script exists in package.json. Running `npx knip` reports either zero findings or only documented false positives. All actionable unused exports have been removed with grep verification before each deletion.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` produces the same pre-existing errors (no new errors introduced)
2. `npx knip` reports clean or only documented false positives
3. `ls src/mastra/workflow/02a-initial-hypothesis-extractor-*.ts src/mastra/workflow/shared-memory.ts` returns "No such file" for all three
4. `grep -r "initialHypothesisExtractorAgent" src/mastra/workflow/index.ts` returns zero matches
</verification>

<success_criteria>
- Three deprecated files deleted (02a-initial-hypothesis-extractor-agent.ts, 02a-initial-hypothesis-extractor-instructions.ts, shared-memory.ts)
- index.ts cleaned of deprecated import and export
- README.md updated (agent table, file tree, Mermaid diagram)
- Knip installed as devDependency, audit script added
- All Knip-flagged unused exports removed (with grep verification)
- npx tsc --noEmit shows no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-dead-code-type-safety/27-01-SUMMARY.md`
</output>
