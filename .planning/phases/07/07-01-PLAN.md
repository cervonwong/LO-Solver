---
phase: 07-hierarchical-trace-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mastra/workflow/workflow-schemas.ts
  - src/mastra/workflow/04-question-answerer-instructions.ts
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "questionAnswerSchema includes a rulesApplied field: array of strings (rule titles)"
    - "Answer agent instructions tell the agent to populate rulesApplied with the titles of rules used for each answer"
    - "Existing answer fields (questionId, answer, workingSteps, confidence, confidenceReasoning) are unchanged"
    - "questionsAnsweredSchema still wraps the array of answers with success/explanation"
  artifacts:
    - path: "src/mastra/workflow/workflow-schemas.ts"
      provides: "questionAnswerSchema with rulesApplied field"
      contains: "rulesApplied"
    - path: "src/mastra/workflow/04-question-answerer-instructions.ts"
      provides: "Updated instructions telling agent to output rulesApplied"
      contains: "rulesApplied"
  key_links:
    - from: "src/mastra/workflow/workflow-schemas.ts"
      to: "src/components/results-panel.tsx"
      via: "questionAnswerSchema shape consumed by ResultsPanel Answer interface"
      pattern: "rulesApplied"
---

<objective>
Add a `rulesApplied` field to the answer schema so the answer agent reports which rule titles it used for each answer. Update the agent instructions to populate this field. This provides the data needed for rule tag chips in the results panel (Plan 07-03).

Purpose: Backend data foundation for UI-05 rule tags. Small, isolated change that enables Plan 07-03.

Output: Updated schema and instructions. No frontend changes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07/07-CONTEXT.md
@src/mastra/workflow/workflow-schemas.ts
@src/mastra/workflow/04-question-answerer-instructions.ts
@src/mastra/workflow/04-question-answerer-agent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rulesApplied field to questionAnswerSchema</name>
  <files>src/mastra/workflow/workflow-schemas.ts</files>
  <action>
In `workflow-schemas.ts`, find the `questionAnswerSchema` z.object definition. Add a new field after `confidenceReasoning`:

```typescript
rulesApplied: z
  .array(z.string())
  .describe('Titles of the rules that were applied to derive this answer'),
```

This field is required (not optional) because every answer should reference at least one rule. The values should match the `title` field from the `ruleSchema` objects passed to the agent.

Do NOT change any other schema fields. The questionAnswerSchema currently has: questionId, answer, workingSteps, confidence, confidenceReasoning. After this change it has those five plus rulesApplied.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | head -20</automated>
  </verify>
  <done>
    - questionAnswerSchema has rulesApplied: z.array(z.string()) field
    - Type check passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Update answer agent instructions to populate rulesApplied</name>
  <files>src/mastra/workflow/04-question-answerer-instructions.ts</files>
  <action>
Update the QUESTION_ANSWERER_INSTRUCTIONS to tell the agent about the rulesApplied field.

**2a. Update the JSON output format examples.**

In the "Scenario 1: Success" JSON example, add the rulesApplied field:
```json
{
  "questionId": "Q1",
  "answer": "The final translated phrase or answer",
  "workingSteps": "Step-by-step breakdown showing how the answer was derived...",
  "confidence": "HIGH",
  "confidenceReasoning": "All morphemes found in vocabulary, all rules applied unambiguously.",
  "rulesApplied": ["Verb agreement", "Noun cases", "Sentence syntax"]
}
```

**2b. Add a brief note to the "Quality Guidelines" section.**

After guideline 4 ("Reference Rules"), add or update:
```
5. **Track Rules Applied**: For each answer, record the titles of every rule you used in the `rulesApplied` array. Use the exact rule titles as provided in the input rules list.
```

Renumber subsequent guidelines (current 5 "Show Alternatives" becomes 6, current 6 "Assign Confidence" becomes 7, current 7 "Check Your Work" becomes 8).

**2c. Update the "For Each Question" methodology.**

In Step 3 ("Apply Rules Systematically"), add a bullet:
```
- Record the title of each rule you apply in the rulesApplied list for this question.
```

Do not change the fundamental structure or tone of the instructions. Keep the changes minimal and focused on the new field.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | head -20</automated>
  </verify>
  <done>
    - Instructions mention rulesApplied field in output format examples
    - Instructions tell agent to track rule titles in rulesApplied array
    - Quality guidelines include "Track Rules Applied" guideline
    - Type check passes
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with only the pre-existing CSS module error
2. `questionAnswerSchema` includes `rulesApplied: z.array(z.string())` field
3. Answer agent instructions reference `rulesApplied` in output format and quality guidelines
4. No other schemas or agent files are modified
</verification>

<success_criteria>
- UI-05 (partial): Backend data model supports rule tag display — answer schema includes rulesApplied titles
- Answer agent is instructed to populate the field with exact rule titles from input
</success_criteria>

<output>
After completion, create `.planning/phases/07/07-01-SUMMARY.md`
</output>
