---
phase: 28-agent-factory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mastra/workflow/agent-factory.ts
  - src/mastra/workflow/03a-rule-tester-instructions.ts
  - src/mastra/workflow/03a-sentence-tester-instructions.ts
autonomous: true
requirements:
  - STR-04
  - STR-06

must_haves:
  truths:
    - "A factory function exists that creates workflow agents with dynamic model resolution"
    - "Tester agent instructions exist as separate files following the *-instructions.ts convention"
    - "The factory handles all three agent variants: standard (UnicodeNormalizer), tester (requestContextSchema, no normalizer), and tool-equipped"
  artifacts:
    - path: "src/mastra/workflow/agent-factory.ts"
      provides: "createWorkflowAgent() factory function and WorkflowAgentConfig interface"
      exports: ["createWorkflowAgent", "WorkflowAgentConfig"]
      min_lines: 40
    - path: "src/mastra/workflow/03a-rule-tester-instructions.ts"
      provides: "Rule tester agent system prompt"
      exports: ["RULE_TESTER_INSTRUCTIONS"]
      min_lines: 10
    - path: "src/mastra/workflow/03a-sentence-tester-instructions.ts"
      provides: "Sentence tester agent system prompt"
      exports: ["SENTENCE_TESTER_INSTRUCTIONS"]
      min_lines: 10
  key_links:
    - from: "src/mastra/workflow/agent-factory.ts"
      to: "src/mastra/openrouter.ts"
      via: "imports TESTING_MODEL and ModelMode"
      pattern: "import.*TESTING_MODEL.*from.*openrouter"
    - from: "src/mastra/workflow/agent-factory.ts"
      to: "src/mastra/workflow/request-context-helpers.ts"
      via: "imports getOpenRouterProvider for dynamic model resolution"
      pattern: "import.*getOpenRouterProvider.*from.*request-context-helpers"
---

<objective>
Create the `createWorkflowAgent()` factory function and extract tester agent instructions into separate files.

Purpose: Establish the factory that all 12 agent files will migrate to in Plan 02, plus extract inline tester instructions to follow the `*-instructions.ts` convention.
Output: `agent-factory.ts` with exported factory + config type, `03a-rule-tester-instructions.ts`, `03a-sentence-tester-instructions.ts`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-agent-factory/28-CONTEXT.md
@.planning/phases/28-agent-factory/28-RESEARCH.md

@src/mastra/openrouter.ts
@src/mastra/workflow/request-context-helpers.ts
@src/mastra/workflow/01-structured-problem-extractor-agent.ts
@src/mastra/workflow/03a-rule-tester-agent.ts
@src/mastra/workflow/03a-sentence-tester-agent.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/mastra/openrouter.ts:
```typescript
export type ModelMode = 'testing' | 'production';
export const TESTING_MODEL = 'openai/gpt-oss-120b';
export function activeModelId(mode: ModelMode, productionModel: string): string;
```

From src/mastra/workflow/request-context-helpers.ts:
```typescript
export function getOpenRouterProvider(requestContext: ...): OpenRouterProvider;
```

Current Agent constructor pattern (all 12 agents):
```typescript
new Agent({
  id: string,
  name: string,
  instructions: string | { role: string; content: string },
  model: ({ requestContext }) => getOpenRouterProvider(requestContext)(modelId),
  tools: {},
  inputProcessors?: [new UnicodeNormalizer({...})],
  requestContextSchema?: z.object({...}),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent factory function and config type</name>
  <files>src/mastra/workflow/agent-factory.ts</files>
  <action>
Create `src/mastra/workflow/agent-factory.ts` with:

1. A `WorkflowAgentConfig` interface (exported) with these fields:
   - `id: string` — agent identifier
   - `name: string` — display name
   - `instructions: string | { role: string; content: string }` — passed through to Agent
   - `productionModel: string` — model ID for production mode
   - `testingModel?: string` — defaults to `TESTING_MODEL`
   - `tools?: ToolsInput` — defaults to `{}`
   - `useUnicodeNormalizer?: boolean` — defaults to `true`
   - `requestContextSchema?: import('zod').ZodType<any>` — opaque Zod type, no Zod import

2. A `createWorkflowAgent(config: WorkflowAgentConfig): Agent` function (exported) that:
   - Destructures config with defaults: `testingModel = TESTING_MODEL`, `tools = {}`, `useUnicodeNormalizer = true`
   - Builds model resolver: `({ requestContext }) => { const mode = requestContext?.get('model-mode') as ModelMode | undefined; const modelId = mode === 'production' ? productionModel : testingModel; return getOpenRouterProvider(requestContext)(modelId); }`
   - Conditionally creates `new UnicodeNormalizer({ stripControlChars: false, preserveEmojis: true, collapseWhitespace: true, trim: true })` when `useUnicodeNormalizer` is true
   - Passes `requestContextSchema` through when provided
   - Returns `new Agent({...})` with all properties

Import `Agent` from `@mastra/core/agent`, `UnicodeNormalizer` from `@mastra/core/processors`, `TESTING_MODEL` and `ModelMode` from `../openrouter`, `getOpenRouterProvider` from `./request-context-helpers`. For `ToolsInput`, try importing from `@mastra/core/tools`; if that type is not available, use `Record<string, any>` and note it in a comment.

Do NOT import Zod. The `requestContextSchema` type should use `import('zod').ZodType<any>` (inline import type) to avoid a runtime Zod dependency.

The factory does NOT handle template injection (`{{placeholder}}` replacement) — that stays in agent files per user decision.
  </action>
  <verify>
    <automated>cd /home/cervo/Code/LO-Solver && npx tsc --noEmit 2>&1 | grep -v "globals.css" | head -20</automated>
  </verify>
  <done>agent-factory.ts exists with exported createWorkflowAgent function and WorkflowAgentConfig interface, type-checks cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Extract tester agent instructions to separate files</name>
  <files>src/mastra/workflow/03a-rule-tester-instructions.ts, src/mastra/workflow/03a-sentence-tester-instructions.ts</files>
  <action>
Extract the inline instruction strings from the two tester agent files into separate `*-instructions.ts` files, following the existing project convention (e.g., `01-structured-problem-extractor-instructions.ts`).

1. Create `src/mastra/workflow/03a-rule-tester-instructions.ts`:
   - Copy the `RULE_TESTER_SYSTEM_PROMPT` string from `03a-rule-tester-agent.ts` (lines 6-45)
   - Export it as `export const RULE_TESTER_INSTRUCTIONS = \`...\`.trim();`
   - The content is the full system prompt from "You are a specialized linguistic rule validator..." through the example output

2. Create `src/mastra/workflow/03a-sentence-tester-instructions.ts`:
   - Copy the `SENTENCE_TESTER_SYSTEM_PROMPT` string from `03a-sentence-tester-agent.ts` (lines 6-75)
   - Export it as `export const SENTENCE_TESTER_INSTRUCTIONS = \`...\`.trim();`
   - The content is the full system prompt from "You are a specialized linguistic sentence translator..." through the example output

Do NOT modify the tester agent files yet — that happens in Plan 02 during migration.

These files have no imports. They are plain string exports.
  </action>
  <verify>
    <automated>cd /home/cervo/Code/LO-Solver && npx tsc --noEmit 2>&1 | grep -v "globals.css" | head -20</automated>
  </verify>
  <done>Both instruction files exist with exported constants matching the inline prompts from the tester agent files, type-checks cleanly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (only pre-existing CSS module error)
- `agent-factory.ts` exports `createWorkflowAgent` and `WorkflowAgentConfig`
- `03a-rule-tester-instructions.ts` exports `RULE_TESTER_INSTRUCTIONS`
- `03a-sentence-tester-instructions.ts` exports `SENTENCE_TESTER_INSTRUCTIONS`
- No Zod import in `agent-factory.ts`
- Factory creates UnicodeNormalizer with `{ stripControlChars: false, preserveEmojis: true, collapseWhitespace: true, trim: true }`
</verification>

<success_criteria>
- Factory function type-checks and is ready for agent migration
- Tester instructions extracted to separate files ready for import in Plan 02
- No behavioral changes to any existing code
</success_criteria>

<output>
After completion, create `.planning/phases/28-agent-factory/28-01-SUMMARY.md`
</output>
