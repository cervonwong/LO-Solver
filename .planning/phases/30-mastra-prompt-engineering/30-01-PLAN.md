---
phase: 30-mastra-prompt-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mastra/workflow/01-structured-problem-extractor-instructions.ts
  - src/mastra/workflow/03a-rule-tester-instructions.ts
  - src/mastra/workflow/03a-sentence-tester-instructions.ts
  - src/mastra/workflow/03a2-verifier-feedback-extractor-instructions.ts
  - src/mastra/workflow/03b2-rules-improvement-extractor-instructions.ts
autonomous: true
requirements: [PE-02, PE-05]

must_haves:
  truths:
    - "All 5 GPT-5-mini instruction files use XML section tags for structural boundaries instead of markdown headers"
    - "Extractor prompts lead with JSON output schema before any processing instructions"
    - "Tester prompts have crisp 1-2 sentence tool descriptions with clear success/failure criteria"
    - "Agents that deal with confidence use the 6-level evidence-based scale (well-supported through unsupported) mapped to HIGH/MEDIUM/LOW Zod enum"
    - "Hedged assertion style replaces any unqualified confidence language"
  artifacts:
    - path: "src/mastra/workflow/01-structured-problem-extractor-instructions.ts"
      provides: "GPT-5-mini extraction prompt with XML structure and grounding principle"
      exports: ["STRUCTURED_PROBLEM_EXTRACTOR_INSTRUCTIONS"]
    - path: "src/mastra/workflow/03a-rule-tester-instructions.ts"
      provides: "GPT-5-mini tester prompt with tool descriptions and status criteria"
      exports: ["RULE_TESTER_INSTRUCTIONS"]
    - path: "src/mastra/workflow/03a-sentence-tester-instructions.ts"
      provides: "GPT-5-mini tester prompt with tool descriptions and translation criteria"
      exports: ["SENTENCE_TESTER_INSTRUCTIONS"]
    - path: "src/mastra/workflow/03a2-verifier-feedback-extractor-instructions.ts"
      provides: "GPT-5-mini extraction prompt with output schema and extraction rules"
      exports: ["VERIFIER_FEEDBACK_EXTRACTOR_INSTRUCTIONS"]
    - path: "src/mastra/workflow/03b2-rules-improvement-extractor-instructions.ts"
      provides: "GPT-5-mini extraction prompt with confidence scale integration"
      exports: ["RULES_IMPROVEMENT_EXTRACTOR_INSTRUCTIONS"]
  key_links:
    - from: "src/mastra/workflow/03b2-rules-improvement-extractor-instructions.ts"
      to: "workflow-schemas.ts ruleSchema"
      via: "6-level confidence scale mapped to HIGH/MEDIUM/LOW enum"
      pattern: "well-supported.*HIGH|supported.*HIGH|plausible.*MEDIUM|tentative.*MEDIUM|speculative.*LOW|unsupported.*LOW"
---

<objective>
Rewrite all 5 GPT-5-mini agent instruction files using OpenAI GPT-5 prompting guide best practices, role-adapted per agent function.

Purpose: GPT-5-mini agents (extractors and testers) need XML-structured, schema-first, literal-following prompts per vendor guide. Extractor prompts emphasize JSON schema clarity and grounding. Tester prompts emphasize tool use and success/failure criteria. The 6-level confidence scale and hedged assertion style are integrated where applicable.

Output: 5 rewritten instruction files with consistent structure and vocabulary.
</objective>

<execution_context>
@/home/cervon/.claude/get-shit-done/workflows/execute-plan.md
@/home/cervon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-mastra-prompt-engineering/30-CONTEXT.md
@.planning/phases/30-mastra-prompt-engineering/30-RESEARCH.md

Source files to read and rewrite:
@src/mastra/workflow/01-structured-problem-extractor-instructions.ts
@src/mastra/workflow/03a-rule-tester-instructions.ts
@src/mastra/workflow/03a-sentence-tester-instructions.ts
@src/mastra/workflow/03a2-verifier-feedback-extractor-instructions.ts
@src/mastra/workflow/03b2-rules-improvement-extractor-instructions.ts

Also read for Zod schema context (do NOT modify):
@src/mastra/workflow/workflow-schemas.ts

<interfaces>
<!-- Each file exports a single const string. The export name MUST be preserved exactly. -->

From 01-structured-problem-extractor-instructions.ts:
```typescript
export const STRUCTURED_PROBLEM_EXTRACTOR_INSTRUCTIONS = `...`.trim();
```

From 03a-rule-tester-instructions.ts:
```typescript
export const RULE_TESTER_INSTRUCTIONS = `...`.trim();
```

From 03a-sentence-tester-instructions.ts:
```typescript
export const SENTENCE_TESTER_INSTRUCTIONS = `...`.trim();
```

From 03a2-verifier-feedback-extractor-instructions.ts:
```typescript
export const VERIFIER_FEEDBACK_EXTRACTOR_INSTRUCTIONS = `...`.trim();
```

From 03b2-rules-improvement-extractor-instructions.ts:
```typescript
export const RULES_IMPROVEMENT_EXTRACTOR_INSTRUCTIONS = `...`.trim();
```

<!-- Zod confidence enum (immutable — prompts must map TO these values): -->
<!-- z.enum(['HIGH', 'MEDIUM', 'LOW']) in ruleSchema.confidence and questionAnswerSchema.confidence -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 3 GPT-5-mini extractor prompts</name>
  <files>
    src/mastra/workflow/01-structured-problem-extractor-instructions.ts
    src/mastra/workflow/03a2-verifier-feedback-extractor-instructions.ts
    src/mastra/workflow/03b2-rules-improvement-extractor-instructions.ts
  </files>
  <action>
Read each file fully, then rewrite the instruction string using GPT-5-mini extractor patterns from 30-RESEARCH.md.

For ALL 3 extractor prompts, apply these GPT-5 guide strategies:
1. Replace all markdown headers (# / ## / ###) with XML section tags
2. Use CTCO structure: `<role>` -> `<grounding>` -> `<output_schema>` -> `<extraction_rules>` -> `<constraints>`
3. Lead with the JSON output schema BEFORE any processing instructions (schema-first)
4. Include grounding principle: "Extract ONLY information explicitly stated in the input. Do not add, interpret, or hallucinate content."
5. Include extraction completeness directive: "Re-scan the input for omissions before finalizing output"
6. For missing/optional fields: "Set missing fields to null rather than guessing"
7. End with `<constraints>` section containing critical rules (placed LAST per GPT-5 guide)
8. Keep the `.trim()` call on the template literal

For `03b2-rules-improvement-extractor-instructions.ts` specifically:
- This extractor handles rules with confidence levels. Add the 6-level confidence scale with Zod mapping:
  ```
  When extracting confidence levels from the reasoning text, use this evidence-based scale:
  - well-supported / supported -> Output as HIGH
  - plausible / tentative -> Output as MEDIUM
  - speculative / unsupported -> Output as LOW
  ```
- Use hedged assertion style in any example language: "X appears to hold based on..." not "X is correct"

For `01-structured-problem-extractor-instructions.ts`:
- This parses raw problem text into structured JSON. No confidence fields. Focus on schema clarity and grounding.

For `03a2-verifier-feedback-extractor-instructions.ts`:
- This parses verification feedback into structured JSON. The conclusion field uses `ALL_RULES_PASS | NEEDS_IMPROVEMENT | MAJOR_ISSUES` enum. Focus on faithful extraction of tool outputs and evidence.

CRITICAL: Preserve the exact export const name. Do NOT change the variable name. Do NOT add any imports. Each file must remain a single exported const string with `.trim()`.

Anti-patterns to AVOID (from research):
- Do NOT add elaborate `<process>` sections to extractors (over-structuring causes reasoning waste)
- Do NOT use narrative instruction style (prefer concrete scoped directives)
- Do NOT add contradictory instructions (GPT-5 wastes reasoning tokens reconciling them)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | grep -c "error" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
All 3 extractor instruction files use XML section tags, lead with output schema, include grounding principle, and the rules-improvement extractor includes the 6-level confidence scale with Zod mapping. Export names unchanged. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite 2 GPT-5-mini tester prompts</name>
  <files>
    src/mastra/workflow/03a-rule-tester-instructions.ts
    src/mastra/workflow/03a-sentence-tester-instructions.ts
  </files>
  <action>
Read each file fully, then rewrite the instruction string using GPT-5-mini tester patterns from 30-RESEARCH.md.

For BOTH tester prompts, apply these GPT-5 guide strategies:
1. Replace all markdown headers with XML section tags
2. Use structure: `<role>` -> `<task>` -> `<tools>` -> `<output_format>` -> `<constraints>`
3. Crisp tool descriptions: 1-2 sentences for what the tool does + when to use it
4. Clear success/failure criteria with enum-style statuses
5. "Quote exact evidence" directive for grounding results
6. End with `<constraints>` section (placed LAST)
7. Keep the `.trim()` call

For `03a-rule-tester-instructions.ts`:
- Tests a single rule against the dataset. Tool-calling agent.
- Status output uses an enum (PASS/FAIL/ERROR or similar). Preserve whatever status values exist.
- Add directive: "Cite specific dataset items (by ID/number) that support or contradict the rule"

For `03a-sentence-tester-instructions.ts`:
- Tests a single sentence translation. Tool-calling agent.
- Has suggestion likelihood field (HIGH/MEDIUM/LOW). Adapt 6-level vocabulary for suggestion likelihood if appropriate, OR leave as 3-level if "likelihood" is a different concept from "confidence" (use judgment per Claude's Discretion).
- Add directive: "Quote exact translations from tool output. Do not paraphrase."

CRITICAL: Preserve the exact export const name. No imports. Single exported const string with `.trim()`.

Anti-patterns to AVOID:
- Do NOT add verbose process sections (testers need to be fast and direct)
- Do NOT over-encourage context gathering (triggers unnecessary exploration in GPT-5)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | grep -c "error" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
Both tester instruction files use XML section tags, have crisp tool descriptions, clear status criteria, and evidence-quoting directives. Export names unchanged. TypeScript compiles.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. All 5 files compile: `npx tsc --noEmit` (only pre-existing globals.css error)
2. All 5 files still export their original const names: `grep "export const" src/mastra/workflow/01-structured-problem-extractor-instructions.ts src/mastra/workflow/03a-rule-tester-instructions.ts src/mastra/workflow/03a-sentence-tester-instructions.ts src/mastra/workflow/03a2-verifier-feedback-extractor-instructions.ts src/mastra/workflow/03b2-rules-improvement-extractor-instructions.ts`
3. No markdown headers remain in GPT-5-mini files: `grep "^#" src/mastra/workflow/01-structured-problem-extractor-instructions.ts src/mastra/workflow/03a-rule-tester-instructions.ts src/mastra/workflow/03a-sentence-tester-instructions.ts src/mastra/workflow/03a2-verifier-feedback-extractor-instructions.ts src/mastra/workflow/03b2-rules-improvement-extractor-instructions.ts` should return empty
4. XML tags present in all 5 files: `grep "<role>" src/mastra/workflow/01-structured-problem-extractor-instructions.ts src/mastra/workflow/03a-rule-tester-instructions.ts src/mastra/workflow/03a-sentence-tester-instructions.ts src/mastra/workflow/03a2-verifier-feedback-extractor-instructions.ts src/mastra/workflow/03b2-rules-improvement-extractor-instructions.ts`
</verification>

<success_criteria>
- All 5 GPT-5-mini instruction files use XML section tags (no markdown headers for structure)
- Extractor prompts have schema-first ordering with grounding principle
- Tester prompts have crisp tool descriptions with evidence-quoting directives
- Rules improvement extractor includes 6-level confidence scale mapped to HIGH/MEDIUM/LOW
- All export names preserved, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/30-mastra-prompt-engineering/30-01-SUMMARY.md`
</output>
