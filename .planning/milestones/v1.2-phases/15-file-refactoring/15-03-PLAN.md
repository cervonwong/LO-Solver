---
phase: 15-file-refactoring
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - src/app/page.tsx
  - src/hooks/use-solver-workflow.ts
  - src/hooks/use-workflow-progress.ts
  - src/hooks/use-workflow-data.ts
  - src/hooks/use-examples.ts
autonomous: true
requirements: [REFAC-03, REFAC-04]

must_haves:
  truths:
    - "page.tsx is smaller with extracted hooks, serving as layout orchestration + hook wiring"
    - "4 domain hooks exist in src/hooks/ covering solver workflow, progress, data, and examples"
    - "The solver page works identically to before — default export SolverPage remains"
    - "npx tsc --noEmit passes with no new errors after the split"
  artifacts:
    - path: "src/hooks/use-solver-workflow.ts"
      provides: "Chat integration, transport, abort handling, send/stop/reset actions"
      exports: ["useSolverWorkflow"]
    - path: "src/hooks/use-workflow-progress.ts"
      provides: "Progress steps, step status derivation"
      exports: ["useWorkflowProgress"]
    - path: "src/hooks/use-workflow-data.ts"
      provides: "Vocabulary, rules, trace events derived from raw event stream"
      exports: ["useWorkflowData"]
    - path: "src/hooks/use-examples.ts"
      provides: "Example problem fetching"
      exports: ["useExamples"]
    - path: "src/app/page.tsx"
      provides: "Layout orchestration, hook wiring, JSX structure"
      exports: ["default (SolverPage)"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/hooks/use-solver-workflow.ts"
      via: "import { useSolverWorkflow }"
      pattern: "import.*useSolverWorkflow.*hooks/use-solver-workflow"
    - from: "src/app/page.tsx"
      to: "src/hooks/use-workflow-progress.ts"
      via: "import { useWorkflowProgress }"
      pattern: "import.*useWorkflowProgress.*hooks/use-workflow-progress"
    - from: "src/app/page.tsx"
      to: "src/hooks/use-workflow-data.ts"
      via: "import { useWorkflowData }"
      pattern: "import.*useWorkflowData.*hooks/use-workflow-data"
    - from: "src/app/page.tsx"
      to: "src/hooks/use-examples.ts"
      via: "import { useExamples }"
      pattern: "import.*useExamples.*hooks/use-examples"
---

<objective>
Extract 4 domain hooks from page.tsx into dedicated hook files in src/hooks/, leaving page.tsx as a layout orchestration component wiring hooks to JSX.

Purpose: Reduce page.tsx from 842 lines to ~200-300 lines by extracting state logic into reusable, testable hooks that follow the project's existing hook pattern.
Output: 4 new hook files in src/hooks/, slimmed page.tsx, passing type-check.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-file-refactoring/15-CONTEXT.md
@.planning/phases/15-file-refactoring/15-RESEARCH.md
@src/app/page.tsx
@src/hooks/use-model-mode.ts
@src/hooks/use-workflow-settings.ts

<interfaces>
<!-- Key contracts the executor needs. -->

page.tsx is the Next.js page component with `export default function SolverPage()`. This default export MUST remain.

**Hook domain analysis from 15-RESEARCH.md:**

**useSolverWorkflow** — Chat lifecycle and abort handling:
```
State: hasStarted, isAborting, hasSent (ref), problemText, inputOpen
Deps: useModelMode, useWorkflowSettings, useChat, DefaultChatTransport
Returns: messages, status, hasStarted, isAborting, isComplete, isRunning, isFailed, isAborted
         handleSolve, handleStop, handleReset, setMessages
         problemText, setProblemText, inputOpen, setInputOpen
```

**useWorkflowProgress** — Progress bar step list:
```
Input: allParts (from messages), steps (from workflowData)
State: none (pure useMemo derivation)
Returns: progressSteps, displaySteps, statusMessage, activeStep
```

**useWorkflowData** — Vocabulary, rules, trace events:
```
Input: allParts (from messages)
State: none (pure useMemo derivations)
Returns: vocabulary, rules, rulesWithTestStatus, vocabActivityEvents, rulesActivityEvents, traceEvents
```

**useExamples** — Example problem fetching:
```
State: examples (array)
Returns: examples
```

**Critical dependency flow — hooks do NOT call each other:**
```
useExamples (independent)
useSolverWorkflow → messages, status, control functions
  ↓ messages
derive allParts, workflowData, steps (inline in page.tsx)
  ↓ allParts, steps
useWorkflowProgress(allParts, steps) → progressSteps
useWorkflowData(allParts) → vocabulary, rules, traceEvents
```

The page component derives `allParts` and intermediate data from `messages`, then passes them as parameters to `useWorkflowProgress` and `useWorkflowData`. This keeps hooks independent of each other.

**What stays in page.tsx (from 15-RESEARCH.md):**
- MascotProvider / useMascotSync
- useRegisterWorkflowControl
- useGroupRef, useMediaQuery
- hasAnimated (ref), isTransitioning
- auto-scroll effect, handleRuleClick, handleStepClick
- All JSX layout (ResizablePanelGroup, panels, sections)

**Existing hook pattern from src/hooks/use-model-mode.ts:**
- `'use client'` at top
- Named export function (e.g., `export function useModelMode()`)
- kebab-case filename

**Claude's discretion items:**
- Exact hook boundaries — which useState/useMemo/useCallback goes into which hook
- Whether to merge small utility functions inline or keep in utility files
- Which JSX sections become sub-components vs stay inline (recommendation: keep inline, size reduction comes from hook extraction)
- Exact return type shapes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract hooks and slim page.tsx</name>
  <files>
    src/hooks/use-solver-workflow.ts,
    src/hooks/use-workflow-progress.ts,
    src/hooks/use-workflow-data.ts,
    src/hooks/use-examples.ts,
    src/app/page.tsx
  </files>
  <action>
Create 4 new hook files and rewrite page.tsx to use them. All hook files need `'use client'` as the first line.

**1. src/hooks/use-examples.ts** (simplest, independent):
   - Extract the examples fetch logic (useState + useEffect with fetch to /api/examples)
   - Return `{ examples }` (the array of example objects)

**2. src/hooks/use-solver-workflow.ts** (core workflow lifecycle):
   - Extract: `hasStarted`, `isAborting`, `hasSent` ref, `problemText`, `inputOpen` state
   - Extract: `useModelMode`, `useWorkflowSettings` hook calls
   - Extract: `transport` useMemo (DefaultChatTransport construction)
   - Extract: `useChat` call with the transport
   - Extract: `handleSolve`, `handleStop`, `handleReset` callbacks
   - Extract: `isComplete`, `isRunning`, `isFailed`, `isAborted` derived state
   - Do NOT include `useMascotSync` or `setMascotState` — those stay in page.tsx
   - The `handleReset` callback needs to call `setMascotState('idle')` from the mascot context. Handle this by accepting an optional `onReset` callback parameter that page.tsx can provide (e.g., `useSolverWorkflow({ onReset: () => setMascotState('idle') })`), or by having `handleReset` returned from the hook and having page.tsx wrap it with the mascot call. Use whichever approach is cleanest — this is at your discretion.
   - Return all state, derived values, and control functions needed by page.tsx

**3. src/hooks/use-workflow-data.ts** (event stream processing):
   - Accept `allParts` as a parameter (array of data parts from messages)
   - Extract: the useMemo derivations for vocabulary, rules, rulesWithTestStatus, vocabActivityEvents, rulesActivityEvents, traceEvents, and the `steps` derivation
   - These are pure useMemo computations — no useState
   - Return all derived data objects

**4. src/hooks/use-workflow-progress.ts** (progress bar logic):
   - Accept parameters needed for progress derivation (allParts and/or the step data from useWorkflowData)
   - Extract: the useMemo derivations for progressSteps, displaySteps, statusMessage, activeStep
   - These are pure useMemo computations — no useState
   - Return progress-related data

**5. Rewrite src/app/page.tsx:**
   - Import and call the 4 new hooks
   - Derive `allParts` from `messages` (this intermediate derivation stays in page.tsx to avoid hooks depending on each other)
   - Pass `allParts` to `useWorkflowData` and `useWorkflowProgress`
   - Keep in page.tsx: MascotProvider, useMascotSync, useRegisterWorkflowControl, useGroupRef, useMediaQuery, hasAnimated ref, isTransitioning state, auto-scroll effect, handleRuleClick, handleStepClick, all JSX
   - The default export `SolverPage` must remain

**Important constraints:**
- Hooks do NOT import from each other — page.tsx wires them together
- No behavior changes — the page must work identically to before
- Follow existing hook file pattern: `'use client'`, named export, kebab-case
- Use `import type` for type-only imports (verbatimModuleSyntax)
- Preserve all existing `eslint-disable` comments in their new locations
- Panel section JSX stays inline in page.tsx — hook extraction is the main size reduction
  </action>
  <verify>
    <automated>cd /home/cervo/Code/LO-Solver && npx tsc --noEmit 2>&1 | grep -v "globals.css" | head -20</automated>
  </verify>
  <done>
    - 4 hook files exist in src/hooks/ with correct exports
    - page.tsx is ~200-300 lines (down from 842)
    - Hooks do not import from each other
    - SolverPage default export intact
    - npx tsc --noEmit shows no new errors (only pre-existing globals.css error)
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit page.tsx hook extraction</name>
  <files>
    src/hooks/use-solver-workflow.ts,
    src/hooks/use-workflow-progress.ts,
    src/hooks/use-workflow-data.ts,
    src/hooks/use-examples.ts,
    src/app/page.tsx
  </files>
  <action>
Stage and commit all 5 files (4 new hook files + modified page.tsx) as one atomic commit.

Commit message: "Extract page.tsx hooks to dedicated files"

Verify the commit includes exactly the expected files and no others.
  </action>
  <verify>
    <automated>cd /home/cervo/Code/LO-Solver && git log --oneline -1 && git diff --stat HEAD~1</automated>
  </verify>
  <done>
    - One atomic commit containing the 4 new hook files and the slimmed page.tsx
    - No other files included in the commit
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with only the pre-existing globals.css error
2. `wc -l src/app/page.tsx` shows ~200-300 lines (down from 842)
3. `ls src/hooks/use-solver-workflow.ts src/hooks/use-workflow-progress.ts src/hooks/use-workflow-data.ts src/hooks/use-examples.ts` — all 4 files exist
4. `grep "import.*from.*hooks/use-" src/app/page.tsx` shows the 4 hook imports
5. `grep "export default" src/app/page.tsx` confirms SolverPage default export
6. `git log --oneline -1` shows the atomic commit
</verification>

<success_criteria>
- page.tsx reduced from 842 lines to ~200-300 lines
- 4 hook files created with correct named exports
- Hooks are independent (no cross-hook imports)
- page.tsx wires hooks together and handles layout
- No new TypeScript errors
- Single atomic commit
- Solver page works identically to before
</success_criteria>

<output>
After completion, create `.planning/phases/15-file-refactoring/15-03-SUMMARY.md`
</output>
