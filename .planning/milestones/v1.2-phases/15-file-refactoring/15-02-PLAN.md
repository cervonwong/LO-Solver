---
phase: 15-file-refactoring
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/components/trace-event-card.tsx
  - src/components/trace/trace-event-card.tsx
  - src/components/trace/tool-call-cards.tsx
  - src/components/trace/specialized-tools.tsx
  - src/components/trace/shared.tsx
  - src/components/trace/trace-utils.tsx
  - src/components/dev-trace-panel.tsx
autonomous: true
requirements: [REFAC-02, REFAC-04]

must_haves:
  truths:
    - "trace-event-card.tsx sub-components live in focused files under src/components/trace/"
    - "The original src/components/trace-event-card.tsx is deleted"
    - "dev-trace-panel.tsx imports TraceEventCard, ToolCallGroupCard, AgentCard from their new locations in components/trace/"
    - "npx tsc --noEmit passes with no new errors after the split"
  artifacts:
    - path: "src/components/trace/trace-event-card.tsx"
      provides: "Main TraceEventCard component"
      exports: ["TraceEventCard"]
    - path: "src/components/trace/tool-call-cards.tsx"
      provides: "ToolCallGroupCard, ToolCallDetail, ToolCallRenderer, RenderItem, AgentToolCallCard, AgentCard"
      exports: ["ToolCallGroupCard", "ToolCallDetail", "ToolCallRenderer", "AgentCard"]
    - path: "src/components/trace/specialized-tools.tsx"
      provides: "VocabularyToolCard, SentenceTestToolCard, BulkToolCallGroup, RuleTestCard"
      exports: ["VocabularyToolCard", "SentenceTestToolCard", "BulkToolCallGroup", "RuleTestCard"]
    - path: "src/components/trace/shared.tsx"
      provides: "ChevronIcon, RawJsonToggle, StructuredOutputSection, TRACE_SD_CLASS constant"
      exports: ["ChevronIcon", "RawJsonToggle", "StructuredOutputSection", "TRACE_SD_CLASS"]
    - path: "src/components/trace/trace-utils.tsx"
      provides: "Utility functions and types for trace rendering"
      exports: ["isRuleTestTool", "isSentenceTestTool", "hasVocabularyEntries", "isStartedStatus", "formatConclusion", "jsonMarkdown", "buildRenderItems"]
  key_links:
    - from: "src/components/dev-trace-panel.tsx"
      to: "src/components/trace/trace-event-card.tsx"
      via: "import { TraceEventCard }"
      pattern: "import.*TraceEventCard.*trace/trace-event-card"
    - from: "src/components/dev-trace-panel.tsx"
      to: "src/components/trace/tool-call-cards.tsx"
      via: "import { ToolCallGroupCard, AgentCard }"
      pattern: "import.*ToolCallGroupCard.*trace/tool-call-cards"
    - from: "src/components/trace/trace-event-card.tsx"
      to: "src/components/trace/tool-call-cards.tsx"
      via: "imports rendering components"
    - from: "src/components/trace/tool-call-cards.tsx"
      to: "src/components/trace/shared.tsx"
      via: "imports ChevronIcon, RawJsonToggle, TRACE_SD_CLASS"
    - from: "src/components/trace/tool-call-cards.tsx"
      to: "src/components/trace/trace-utils.tsx"
      via: "imports utility functions"
---

<objective>
Extract trace-event-card.tsx sub-components into focused files under src/components/trace/, then delete the original monolithic file and update the consumer import.

Purpose: Reduce the 898-line trace-event-card.tsx into 5 focused files organized by domain (shared utilities, tool call rendering, specialized tool cards, trace utilities, main card).
Output: 5 new files in trace/, deleted original, updated consumer, passing type-check.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-file-refactoring/15-CONTEXT.md
@.planning/phases/15-file-refactoring/15-RESEARCH.md
@src/components/trace-event-card.tsx
@src/components/dev-trace-panel.tsx

<interfaces>
<!-- Key contracts the executor needs. -->

From dev-trace-panel.tsx (the sole external consumer):
```typescript
import { TraceEventCard, ToolCallGroupCard, AgentCard } from '@/components/trace-event-card';
```
These 3 exports must be available from their new locations after the split. The import in dev-trace-panel.tsx must be updated to:
```typescript
import { TraceEventCard } from '@/components/trace/trace-event-card';
import { ToolCallGroupCard, AgentCard } from '@/components/trace/tool-call-cards';
```

Component inventory from 15-RESEARCH.md (line number = position in current trace-event-card.tsx):

**trace/trace-event-card.tsx** (main switch component):
  - TraceEventCard (line 40) — exported

**trace/tool-call-cards.tsx** (tool call rendering):
  - ToolCallDetail (line 217) — internal
  - ToolCallGroupCard (line 186) — exported
  - ToolCallRenderer (line 553) — internal
  - RenderItem (line 639) — internal
  - AgentCard (line 672) — exported
  - AgentToolCallCard (line 863) — internal
  - DEPTH_INDENT constant (line 656) — local
  - getIndentClass function (line 663) — local

**trace/specialized-tools.tsx** (domain-specific tool cards):
  - VocabularyToolCard (line 316) — internal
  - SentenceTestToolCard (line 423) — internal
  - BulkToolCallGroup (line 478) — internal
  - RuleTestCard (line 826) — internal

**trace/shared.tsx** (shared UI primitives):
  - ChevronIcon (line 15) — internal
  - RawJsonToggle (line 248) — internal
  - StructuredOutputSection (line 287) — internal
  - TRACE_SD_CLASS constant (line 34)

**trace/trace-utils.tsx** (utility functions and types):
  - jsonMarkdown (line 30) — internal
  - formatConclusion (line 169) — internal
  - isRuleTestTool (line 533) — internal
  - isSentenceTestTool (line 537) — internal
  - hasVocabularyEntries (line 541) — internal
  - isStartedStatus (line 549) — internal
  - RenderItemType (line 583) — type
  - buildRenderItems (line 588) — internal

**Critical notes:**
- Every .tsx file with React hooks MUST have `'use client'` as the first line
- trace-utils.tsx does NOT need `'use client'` (pure functions, no hooks)
- No barrel/index.ts file in trace/
- Use `import type` for type-only imports (verbatimModuleSyntax)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split trace-event-card.tsx into trace/ directory files</name>
  <files>
    src/components/trace/trace-event-card.tsx,
    src/components/trace/tool-call-cards.tsx,
    src/components/trace/specialized-tools.tsx,
    src/components/trace/shared.tsx,
    src/components/trace/trace-utils.tsx,
    src/components/dev-trace-panel.tsx,
    src/components/trace-event-card.tsx
  </files>
  <action>
Create the `src/components/trace/` directory.

**Build order matters** — create files bottom-up (dependencies first) to avoid referencing nonexistent modules:

1. **trace/trace-utils.tsx** — Pure utility functions. No `'use client'` needed.
   - Move: `jsonMarkdown`, `formatConclusion`, `isRuleTestTool`, `isSentenceTestTool`, `hasVocabularyEntries`, `isStartedStatus`, `RenderItemType` (type), `buildRenderItems`
   - Import types from `@/lib/workflow-events` and `@/lib/trace-utils` as needed

2. **trace/shared.tsx** — Shared UI primitives. Add `'use client'` at top.
   - Move: `ChevronIcon`, `RawJsonToggle`, `StructuredOutputSection`, `TRACE_SD_CLASS` constant
   - These are small UI components used across multiple trace files

3. **trace/specialized-tools.tsx** — Domain-specific tool cards. Add `'use client'` at top.
   - Move: `VocabularyToolCard`, `SentenceTestToolCard`, `BulkToolCallGroup`, `RuleTestCard`
   - Import from `./shared` and `./trace-utils` as needed

4. **trace/tool-call-cards.tsx** — Main tool call rendering. Add `'use client'` at top.
   - Move: `ToolCallGroupCard`, `ToolCallDetail`, `ToolCallRenderer`, `RenderItem`, `AgentCard`, `AgentToolCallCard`, `DEPTH_INDENT`, `getIndentClass`
   - Import from `./shared`, `./trace-utils`, `./specialized-tools` as needed

5. **trace/trace-event-card.tsx** — Main switch component. Add `'use client'` at top.
   - Move: `TraceEventCard`
   - Import from `./tool-call-cards`, `./shared` as needed

6. **Update dev-trace-panel.tsx** — Change the import from:
   ```typescript
   import { TraceEventCard, ToolCallGroupCard, AgentCard } from '@/components/trace-event-card';
   ```
   To:
   ```typescript
   import { TraceEventCard } from '@/components/trace/trace-event-card';
   import { ToolCallGroupCard, AgentCard } from '@/components/trace/tool-call-cards';
   ```

7. **Delete the original** `src/components/trace-event-card.tsx`.

**Important constraints:**
- No behavior changes — component code must be identical to the original
- Every .tsx file with React hooks/JSX MUST have `'use client'` as first line
- Use `import type` for type-only imports (verbatimModuleSyntax)
- No barrel/index.ts file in trace/
- Do not change any component names, props, or runtime behavior
  </action>
  <verify>
    <automated>cd /home/cervo/Code/LO-Solver && npx tsc --noEmit 2>&1 | grep -v "globals.css" | head -20</automated>
  </verify>
  <done>
    - 5 files exist in src/components/trace/ with correct exports
    - Original src/components/trace-event-card.tsx is deleted
    - dev-trace-panel.tsx imports updated to new paths
    - npx tsc --noEmit shows no new errors (only pre-existing globals.css error)
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit trace-event-card.tsx split</name>
  <files>
    src/components/trace/trace-event-card.tsx,
    src/components/trace/tool-call-cards.tsx,
    src/components/trace/specialized-tools.tsx,
    src/components/trace/shared.tsx,
    src/components/trace/trace-utils.tsx,
    src/components/dev-trace-panel.tsx,
    src/components/trace-event-card.tsx
  </files>
  <action>
Stage all 5 new trace/ files, the modified dev-trace-panel.tsx, and the deleted trace-event-card.tsx. Commit as one atomic commit.

Commit message: "Extract trace-event-card sub-components to trace/ directory"

Verify the commit includes the deletion of the original file via `git diff --stat HEAD~1` (should show the original as deleted and new files as added).
  </action>
  <verify>
    <automated>cd /home/cervo/Code/LO-Solver && git log --oneline -1 && git diff --stat HEAD~1</automated>
  </verify>
  <done>
    - One atomic commit containing 5 new files, 1 modified file, 1 deleted file
    - Original trace-event-card.tsx no longer on disk
    - No other files included in the commit
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with only the pre-existing globals.css error
2. `ls src/components/trace/` shows 5 files: trace-event-card.tsx, tool-call-cards.tsx, specialized-tools.tsx, shared.tsx, trace-utils.tsx
3. `ls src/components/trace-event-card.tsx` shows file does not exist (deleted)
4. `grep "trace-event-card" src/components/dev-trace-panel.tsx` returns nothing (old import gone)
5. `grep "trace/" src/components/dev-trace-panel.tsx` shows new imports
6. `git log --oneline -1` shows the atomic commit
</verification>

<success_criteria>
- Original 898-line trace-event-card.tsx deleted
- 5 focused files created in src/components/trace/
- dev-trace-panel.tsx imports updated to new paths
- No new TypeScript errors
- Single atomic commit
</success_criteria>

<output>
After completion, create `.planning/phases/15-file-refactoring/15-02-SUMMARY.md`
</output>
