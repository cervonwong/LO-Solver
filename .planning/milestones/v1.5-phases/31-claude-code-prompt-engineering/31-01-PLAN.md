---
phase: 31-claude-code-prompt-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/.claude/agents/extractor.md
  - claude-code/.claude/agents/verifier.md
  - claude-code/.claude/agents/hypothesizer.md
  - claude-code/.claude/agents/improver.md
  - claude-code/.claude/agents/synthesizer.md
  - claude-code/.claude/agents/answerer.md
autonomous: true
requirements: [PE-04]

must_haves:
  truths:
    - "All 6 agent prompts use XML-tagged sections for structural boundaries instead of markdown headings"
    - "Each agent prompt opens with a role definition as the first content after YAML frontmatter"
    - "Domain context and data format descriptions precede task instructions in every prompt"
    - "Tool use instructions use conditional phrasing, not blanket ALWAYS directives"
    - "5 of 6 agents use the 6-level evidence-based confidence scale (verifier exempt)"
    - "4 agents use hedged assertion style (extractor and verifier exempt)"
    - "Error handling sections are preserved functionally intact"
    - "No CRITICAL/MUST/ALWAYS aggressive emphasis language in any prompt"
  artifacts:
    - path: "claude-code/.claude/agents/extractor.md"
      provides: "Structural parser agent prompt with XML sections, no confidence scale, no hedging"
      contains: "<role>"
    - path: "claude-code/.claude/agents/verifier.md"
      provides: "Single-item tester agent prompt with XML sections, PASS/FAIL/NEEDS_UPDATE vocabulary"
      contains: "<role>"
    - path: "claude-code/.claude/agents/hypothesizer.md"
      provides: "Hypothesis generator prompt with XML sections, 6-level confidence, hedged assertions"
      contains: "well-supported"
    - path: "claude-code/.claude/agents/improver.md"
      provides: "Rule reviser prompt with XML sections, 6-level confidence, hedged assertions"
      contains: "well-supported"
    - path: "claude-code/.claude/agents/synthesizer.md"
      provides: "Ruleset merger prompt with XML sections, 6-level confidence, hedged assertions"
      contains: "well-supported"
    - path: "claude-code/.claude/agents/answerer.md"
      provides: "Translator prompt with XML sections, 6-level per-question confidence, hedged assertions"
      contains: "well-supported"
  key_links:
    - from: "claude-code/.claude/agents/hypothesizer.md"
      to: "claude-code/.claude/agents/verifier.md"
      via: "PASS/FAIL/NEEDS_UPDATE vocabulary in verifier matches what hypothesizer expects"
      pattern: "PASS|FAIL|NEEDS_UPDATE"
    - from: "claude-code/.claude/agents/improver.md"
      to: "6-level confidence scale"
      via: "Same scale wording as hypothesizer/synthesizer"
      pattern: "well-supported.*supported.*plausible.*tentative.*speculative.*unsupported"
---

<objective>
Rewrite all 6 Claude Code agent prompts per Anthropic Claude 4.6 best practices.

Purpose: Apply XML-tagged sections, role-first structure, data-first ordering, conditional tool guidance, the 6-level evidence-based confidence scale, and hedged assertion style to all Claude Code agents. This is the Claude-specific counterpart to the Mastra prompt engineering done in Phase 30.

Output: 6 rewritten agent markdown files in `claude-code/.claude/agents/`
</objective>

<execution_context>
@/home/cervon/.claude/get-shit-done/workflows/execute-plan.md
@/home/cervon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-claude-code-prompt-engineering/31-CONTEXT.md
@.planning/phases/31-claude-code-prompt-engineering/31-RESEARCH.md
@claude-code/CLAUDE.md
@claude-code/.claude/agents/extractor.md
@claude-code/.claude/agents/verifier.md
@claude-code/.claude/agents/hypothesizer.md
@claude-code/.claude/agents/improver.md
@claude-code/.claude/agents/synthesizer.md
@claude-code/.claude/agents/answerer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite extractor and verifier prompts (exempt agents)</name>
  <files>claude-code/.claude/agents/extractor.md, claude-code/.claude/agents/verifier.md</files>
  <action>
Read the Anthropic prompting best practices from 31-RESEARCH.md and both current agent files. Then deep-rewrite both prompts.

**Both agents** -- apply these structural changes:
- Replace all `## Heading` structural boundaries with XML tags: `<role>`, `<context>`, `<input>`, `<task>`, `<output_format>`, `<constraints>`, `<error_handling>`
- Open each prompt with `<role>` as the first content after YAML frontmatter (role definition as first sentence)
- Order sections data-first: `<role>` -> `<context>` -> `<input>` -> `<task>` -> `<output_format>` -> `<constraints>` -> `<error_handling>`
- Restructure "Do NOT" sections into `<constraints>` using the hybrid approach from 31-RESEARCH.md:
  - Positive reframing where natural (e.g., "Copy all text verbatim" instead of "Do NOT modify text")
  - Conditional guidance where context matters (explain WHY a constraint exists)
  - Brief negative guardrails where sharp scope boundaries are needed (e.g., "do not translate or analyze")
  - Always provide context/motivation for constraints
- Remove any "CRITICAL: You MUST" or "ALWAYS" aggressive emphasis language -- use normal phrasing per Opus 4.6 guidance
- Tool use: specify WHEN to use each tool and WHAT to expect, not blanket directives (e.g., "Use the Read tool to load the file before beginning" not "ALWAYS read using the Read tool")
- Update YAML frontmatter `description` field to match rewritten prompt style
- Preserve error handling sections functionally as-is (rewrap in `<error_handling>` XML tag but keep the same recovery procedures)
- Use markdown only inside content blocks (output format templates, tables), XML tags for prompt structure
- Keep 6-8 top-level XML tags maximum, avoid over-nesting

**Extractor-specific:**
- No confidence scale needed (extractor does not assess confidence)
- No hedged assertion style (mechanical parsing -- exempt per user decision)
- The Task section's 3 subsections (Context, Dataset, Questions) contain substantive extraction rules -- preserve ALL of them in substance within `<task>`, using nested structure if needed (e.g., markdown sub-headings inside `<task>` content)
- Reframe the 11 "Do NOT" items: many become positive instructions or scope boundaries in `<role>` and `<constraints>`. Follow the examples in 31-RESEARCH.md (e.g., "Do NOT assume a fixed two-column format" -> "Auto-detect the number of language columns")

**Verifier-specific:**
- No confidence scale (keeps PASS/FAIL/NEEDS_UPDATE status vocabulary -- exempt per user decision)
- No hedged assertion style (definitive testing with direct assertions -- exempt per user decision)
- Keep the two operational modes (Rule Test, Sentence Test) clearly separated within `<task>`
- Reframe the 8 "Do NOT" items: many are scope boundaries belonging in `<role>` or `<constraints>` (e.g., "Do NOT aggregate multiple test results" -> role: "You test ONE thing per invocation"; "Do NOT modify or suggest changes to rules" -> scope: "Modification is the improver agent's job")

After writing each file, verify the YAML frontmatter is valid (no unescaped special characters in description).
  </action>
  <verify>
    <automated>cd /home/cervon/Code/LO-Solver && grep -c '<role>' claude-code/.claude/agents/extractor.md claude-code/.claude/agents/verifier.md && grep -c '## Do NOT' claude-code/.claude/agents/extractor.md claude-code/.claude/agents/verifier.md && grep -c 'CRITICAL\|ALWAYS' claude-code/.claude/agents/extractor.md claude-code/.claude/agents/verifier.md</automated>
  </verify>
  <done>Both extractor.md and verifier.md have: (1) XML-tagged sections with `<role>` as first content after frontmatter, (2) no `## Do NOT` section (replaced by `<constraints>`), (3) no CRITICAL/ALWAYS aggressive language, (4) data-first ordering, (5) error handling preserved, (6) YAML frontmatter valid. Verifier retains PASS/FAIL/NEEDS_UPDATE vocabulary. Extractor has no confidence scale.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite hypothesizer and improver prompts (rule discovery agents)</name>
  <files>claude-code/.claude/agents/hypothesizer.md, claude-code/.claude/agents/improver.md</files>
  <action>
Read both current agent files and 31-RESEARCH.md. Then deep-rewrite both prompts.

**Both agents** -- apply the same structural changes as Task 1 (XML tags, role-first, data-first ordering, constraint reframing, no aggressive emphasis, conditional tool use, error handling preserved).

Additionally, both need:
- `<guidelines>` section containing the 6-level evidence-based confidence scale AND hedged assertion style guidance
- Confidence scale (include verbatim in `<guidelines>`):
  ```
  When assessing confidence, use this evidence-based scale:
  - well-supported: ALL examples work without exception, pattern is unambiguous and simple
  - supported: 2+ examples align with minor gaps acceptable
  - plausible: 1 clear example, or pattern works but involves complexity or exceptions
  - tentative: Partial pattern with gaps remaining
  - speculative: Inferred from analogy, no direct supporting example
  - unsupported: Contradicted by data or lacking any evidence

  Overclaiming guard: if even one example is ambiguous, the rule cannot be "well-supported."
  Complexity and exceptions reduce confidence -- a rule with multiple exceptions is "plausible" at best.
  ```
- Hedged assertion style guidance: qualify claims with evidence references (e.g., "X appears to hold based on examples 1-3, though example 4 is ambiguous"). Avoid unqualified assertions like "The answer is X" or "I found that X."
- Update output format template: replace `{HIGH|MEDIUM|LOW}` with `{well-supported|supported|plausible|tentative|speculative|unsupported}`
- Section ordering: `<role>` -> `<context>` -> `<input>` -> `<task>` -> `<output_format>` -> `<guidelines>` -> `<constraints>` -> `<error_handling>`

**Hypothesizer-specific:**
- The 7-step Analysis Process is detailed chain-of-thought scaffolding. Per Anthropic guidance, Opus 4.6's adaptive thinking makes prescriptive step-by-step less necessary. Condense to 4-5 high-level steps that name the key analytical concepts (segmentation, morphology, syntax, phonology, validation, competing hypotheses) without over-specifying the reasoning process. Preserve the unique high-value items: "actively seek counterexamples" (falsification), "generate competing hypotheses," and "Occam's Razor" guidance.
- Reframe the 10 "Do NOT" items into `<constraints>` using positive framing and conditional guidance. Follow the example in 31-RESEARCH.md for this agent specifically.
- Replace the 3-level Confidence Guidelines section with the 6-level scale in `<guidelines>`.

**Improver-specific:**
- The 7 sub-steps including "Root Cause Analysis" with 6 core reasoning principles are high-value instructions. Condense slightly per Anthropic's "prefer general instructions over prescriptive steps" but preserve the key concepts: logical order (vocab first), abductive reasoning, multiple hypotheses, adaptability, grounding, persistence. These can be presented as high-level principles rather than step-by-step instructions.
- The "multiple hypotheses" principle aligns with hedged assertion style -- integrate the two naturally in `<guidelines>`.
- Replace the 3-level Confidence Guidelines section with the 6-level scale in `<guidelines>`.
- Reframe the 8 "Do NOT" items into `<constraints>`.

After writing each file, verify YAML frontmatter validity.
  </action>
  <verify>
    <automated>cd /home/cervon/Code/LO-Solver && grep -c '<role>' claude-code/.claude/agents/hypothesizer.md claude-code/.claude/agents/improver.md && grep -c 'well-supported' claude-code/.claude/agents/hypothesizer.md claude-code/.claude/agents/improver.md && grep -c '## Do NOT\|## Confidence Guidelines' claude-code/.claude/agents/hypothesizer.md claude-code/.claude/agents/improver.md && grep -c 'CRITICAL\|ALWAYS' claude-code/.claude/agents/hypothesizer.md claude-code/.claude/agents/improver.md</automated>
  </verify>
  <done>Both hypothesizer.md and improver.md have: (1) XML-tagged sections with `<role>` first, (2) `<guidelines>` section with 6-level confidence scale (well-supported through unsupported) and hedged assertion guidance, (3) no `## Do NOT` or `## Confidence Guidelines` sections (replaced by `<constraints>` and `<guidelines>`), (4) no CRITICAL/ALWAYS language, (5) output format uses 6-level scale, (6) error handling preserved. Hypothesizer's analysis process condensed to high-level steps. Improver's root cause principles preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite synthesizer and answerer prompts (merge and answer agents)</name>
  <files>claude-code/.claude/agents/synthesizer.md, claude-code/.claude/agents/answerer.md</files>
  <action>
Read both current agent files and 31-RESEARCH.md. Then deep-rewrite both prompts.

**Both agents** -- apply the same structural changes as Tasks 1-2 (XML tags, role-first, data-first ordering, constraint reframing, no aggressive emphasis, conditional tool use, error handling preserved).

Additionally, both need:
- `<guidelines>` section with the same 6-level evidence-based confidence scale and hedged assertion style as Task 2 (identical scale text, both are hedging agents per user decision)
- Update output format templates: replace `{HIGH|MEDIUM|LOW}` with `{well-supported|supported|plausible|tentative|speculative|unsupported}`
- Section ordering: `<role>` -> `<context>` -> `<input>` -> `<task>` -> `<output_format>` -> `<guidelines>` -> `<constraints>` -> `<error_handling>`

**Synthesizer-specific:**
- Preserve the clear merge process structure (rank, base, add, resolve, merge vocab, sanity check) but present as high-level steps rather than prescriptive sub-procedures
- Confidence is currently embedded in the output format section (not a separate Confidence Guidelines section) -- extract to `<guidelines>` with the 6-level scale
- The synthesizer's confidence definitions are merge-specific (e.g., "Rule was PASS in at least one verification"). Integrate these as context within the 6-level scale: map "PASS in verification + consistent evidence" -> well-supported/supported, "revised during merging" -> plausible/tentative, "inferred from partial insights" -> speculative/tentative
- Reframe the 8 "Do NOT" items. Follow the examples in 31-RESEARCH.md: "Do NOT invent rules" -> "Only merge, select, and refine existing rules -- the perspectives are the evidence base"; "Do NOT discard all rules from a low-scoring perspective" -> "Low-scoring perspectives may still have complementary rules the best perspective lacks"

**Answerer-specific:**
- Has redundant confidence content: both a "Confidence and Best-Attempt Policy" subsection in Task AND a separate "Confidence Guidelines" section. Merge both into `<guidelines>` with the 6-level scale.
- The 6-level scale replaces HIGH/MEDIUM/LOW directly in output format (no Zod constraint for Claude Code agents, per 31-RESEARCH.md)
- Best-attempt policy is important -- preserve in `<guidelines>`: always produce a translation for every question even with low confidence
- Reframe the 7 "Do NOT" items. Follow 31-RESEARCH.md examples: "Do NOT use external linguistic knowledge" -> "Apply only the rules and vocabulary from the provided solution file"; "Do NOT skip any question" -> "Produce a best-attempt translation for every question, even with low confidence"

After writing each file, verify YAML frontmatter validity.

**Final cross-agent verification after both files are written:**
- Confirm all 6 agent files contain `<role>` as first content after frontmatter
- Confirm no `## Do NOT` sections remain in any agent file
- Confirm no `CRITICAL` or `ALWAYS` aggressive language remains
- Confirm the 5 non-verifier agents all use the exact same 6-level confidence scale wording
- Confirm the verifier still uses PASS/FAIL/NEEDS_UPDATE (not the 6-level scale)
- Confirm the 4 hedging agents (hypothesizer, improver, synthesizer, answerer) have hedged assertion guidance
- Confirm the 2 exempt agents (extractor, verifier) do not have hedging guidance
  </action>
  <verify>
    <automated>cd /home/cervon/Code/LO-Solver && echo "=== role tags ===" && grep -c '<role>' claude-code/.claude/agents/*.md && echo "=== Do NOT sections ===" && grep -c '## Do NOT' claude-code/.claude/agents/*.md && echo "=== CRITICAL/ALWAYS ===" && grep -ci 'CRITICAL\|ALWAYS' claude-code/.claude/agents/*.md && echo "=== 6-level scale (5 agents) ===" && grep -c 'well-supported' claude-code/.claude/agents/hypothesizer.md claude-code/.claude/agents/improver.md claude-code/.claude/agents/synthesizer.md claude-code/.claude/agents/answerer.md claude-code/.claude/agents/extractor.md && echo "=== verifier PASS/FAIL ===" && grep -c 'PASS|FAIL|NEEDS_UPDATE\|PASS/FAIL/NEEDS_UPDATE' claude-code/.claude/agents/verifier.md && echo "=== hedged assertion (4 agents) ===" && grep -c 'hedged\|evidence references\|appears to hold' claude-code/.claude/agents/hypothesizer.md claude-code/.claude/agents/improver.md claude-code/.claude/agents/synthesizer.md claude-code/.claude/agents/answerer.md</automated>
  </verify>
  <done>All 6 agent files rewritten: (1) XML-tagged sections throughout, (2) role-first ordering, (3) data-first ordering, (4) no aggressive emphasis, (5) constraints use positive framing with motivation. Synthesizer and answerer have 6-level confidence scale and hedged assertion style in `<guidelines>`. Cross-agent consistency verified: same scale wording across 5 agents (extractor excluded), verifier retains PASS/FAIL/NEEDS_UPDATE, 4 hedging agents have assertion guidance, 2 exempt agents do not.</done>
</task>

</tasks>

<verification>
Cross-agent consistency checks after all tasks complete:

1. **XML structure**: All 6 files use `<role>` as first content, `<context>` before `<task>`, `<error_handling>` last
2. **No markdown structural headings**: Zero `## Domain Context`, `## Do NOT`, `## Confidence Guidelines` headings remain
3. **Confidence scale consistency**: The exact 6-level scale text (well-supported/supported/plausible/tentative/speculative/unsupported) appears identically in hypothesizer, improver, synthesizer, answerer; absent from extractor; verifier uses PASS/FAIL/NEEDS_UPDATE
4. **Hedging consistency**: Hedged assertion guidance appears in hypothesizer, improver, synthesizer, answerer; absent from extractor, verifier
5. **No aggressive language**: Zero instances of "CRITICAL", "MUST" (in emphasis context), or blanket "ALWAYS" across all 6 files
6. **Error handling preserved**: All 6 files have `<error_handling>` sections with the same recovery procedures as the originals
7. **YAML frontmatter valid**: All 6 files have properly quoted description fields with no syntax-breaking characters
</verification>

<success_criteria>
- All 6 agent files in `claude-code/.claude/agents/` are rewritten with XML-tagged sections
- Each prompt opens with `<role>` as first content after YAML frontmatter
- Data-first ordering: `<context>` before `<task>` in all prompts
- Tool use is conditional ("Use X when Y"), not blanket ("ALWAYS use X")
- 5 of 6 agents use the 6-level evidence-based confidence scale (verifier exempt)
- 4 agents use hedged assertion style (extractor and verifier exempt)
- Error handling sections functionally identical to originals
- No "CRITICAL: You MUST" or blanket "ALWAYS" language in any prompt
- YAML frontmatter `description` fields updated and syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/31-claude-code-prompt-engineering/31-01-SUMMARY.md`
</output>
