---
phase: 30-mastra-prompt-engineering
plan: 03
type: execute
wave: 2
depends_on: [30-01, 30-02]
files_modified: []
autonomous: false
requirements: [PE-01, PE-06, PE-07]

must_haves:
  truths:
    - "A testing-mode eval run completes successfully after all 12 prompt rewrites"
    - "The eval run uses --comparison flag to show workflow vs zero-shot scores"
    - "No catastrophic regression: workflow scores are not significantly worse than pre-rewrite expectations"
  artifacts: []
  key_links:
    - from: "npm run eval -- --comparison"
      to: "all 12 rewritten instruction files"
      via: "workflow execution using rewritten prompts"
      pattern: "translation-accuracy|rule-quality"
---

<objective>
Run the final eval to verify no regression from the 12 prompt rewrites, and record results.

Purpose: All 12 Mastra agent prompts have been rewritten in Plans 01 and 02. This plan runs the single eval verification agreed upon in CONTEXT.md. PE-01 (baseline capture) and PE-07 (production mode eval) are recorded as SKIPPED per user decision. The eval run satisfies PE-06 (verification with eval).

Output: Eval results confirming non-regression (or identifying issues for follow-up).
</objective>

<execution_context>
@/home/cervon/.claude/get-shit-done/workflows/execute-plan.md
@/home/cervon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-mastra-prompt-engineering/30-CONTEXT.md
@.planning/phases/30-mastra-prompt-engineering/30-01-SUMMARY.md
@.planning/phases/30-mastra-prompt-engineering/30-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run eval with comparison</name>
  <files></files>
  <action>
Run the eval command in testing mode with comparison to verify no regression from the 12 prompt rewrites:

```bash
npm run eval -- --comparison
```

This runs the full eval suite in testing mode (default), comparing workflow results against zero-shot baseline. It tests against the ground-truth Linguini problems.

IMPORTANT: This command makes live API calls to OpenRouter and incurs cost. Run it exactly ONCE. Do not retry unless it fails due to a transient error (timeout, rate limit).

After the eval completes:
1. Record the translation-accuracy scores for each problem
2. Record the workflow vs zero-shot comparison (delta scores)
3. Note any problems where the workflow scored significantly worse than zero-shot

Record these requirement dispositions:
- PE-01: SKIPPED per user decision (no baseline capture before prompt changes)
- PE-06: SATISFIED by this eval run (verifies rewrites with eval)
- PE-07: SKIPPED per user decision (testing mode only, no production eval)

If the eval reveals a regression on any specific problem, note it in the summary but do NOT attempt to fix prompts in this plan. Fixing would be a separate gap-closure plan.
  </action>
  <verify>
    <automated>ls evals/results/*.json 2>/dev/null | tail -1 | xargs -I{} test -f {} && echo "PASS: eval results exist" || echo "FAIL: no eval results"</automated>
  </verify>
  <done>
Eval run completed. Results JSON exists in evals/results/. Translation-accuracy and comparison scores recorded in summary.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies eval results and prompt quality</name>
  <action>
Present eval results to user for review. All 12 Mastra agent prompts were rewritten with model-specific best practices (Plans 01 + 02) and verified with eval run (Task 1).
  </action>
  <verify>User approves results</verify>
  <done>User confirms eval scores are acceptable and Phase 30 is complete.</done>
  <what-built>All 12 Mastra agent prompts rewritten with model-specific best practices (Plans 01 + 02) and verified with eval run (Plan 03 Task 1).</what-built>
  <how-to-verify>
    1. Review the eval scores reported in Task 1 output
    2. Check that workflow scores are reasonable (not catastrophically worse than expected)
    3. If desired, run the solver manually via `npm run dev` at http://localhost:3000 with a Linguistics Olympiad problem to visually verify agent behavior
    4. Optionally inspect a few rewritten instruction files to confirm XML structure and confidence scale integration
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 30, or describe any issues to address</resume-signal>
</task>

</tasks>

<verification>
1. Eval results JSON exists in evals/results/
2. Eval completed without runtime errors (no Zod validation failures from confidence mismatch)
3. User approves results
</verification>

<success_criteria>
- Eval run completes successfully with no Zod validation errors
- Translation-accuracy scores recorded
- Workflow vs zero-shot comparison shows workflow is competitive or better
- PE-01 documented as SKIPPED, PE-06 SATISFIED, PE-07 documented as SKIPPED
- User approves phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/30-mastra-prompt-engineering/30-03-SUMMARY.md`
</output>
