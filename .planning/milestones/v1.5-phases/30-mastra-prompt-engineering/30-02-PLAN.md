---
phase: 30-mastra-prompt-engineering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mastra/workflow/rules-tools-prompt.ts
  - src/mastra/workflow/02-dispatcher-instructions.ts
  - src/mastra/workflow/02-improver-dispatcher-instructions.ts
  - src/mastra/workflow/02-initial-hypothesizer-instructions.ts
  - src/mastra/workflow/02-synthesizer-instructions.ts
  - src/mastra/workflow/03a-verifier-orchestrator-instructions.ts
  - src/mastra/workflow/03b-rules-improver-instructions.ts
  - src/mastra/workflow/04-question-answerer-instructions.ts
autonomous: true
requirements: [PE-03, PE-05]

must_haves:
  truths:
    - "All 7 Gemini 3 Flash instruction files use XML-delimited sections exclusively (no markdown headers for structure)"
    - "Gemini prompts use data-before-instructions ordering where applicable"
    - "No explicit chain-of-thought scaffolding in any Gemini prompt"
    - "Template injection slots ({{RULES_TOOLS_INSTRUCTIONS}}, {{VOCABULARY_TOOLS_INSTRUCTIONS}}) are preserved exactly in hypothesizer, synthesizer, and rules-improver"
    - "rules-tools-prompt.ts confidence guidelines use the 6-level evidence-based scale with Zod mapping"
    - "All agents dealing with confidence use consistent 6-level scale vocabulary"
    - "Hedged assertion style used across all Gemini prompts (no unqualified assertions)"
  artifacts:
    - path: "src/mastra/workflow/rules-tools-prompt.ts"
      provides: "Updated shared rules tools prompt with 6-level confidence scale"
      exports: ["RULES_TOOLS_INSTRUCTIONS"]
    - path: "src/mastra/workflow/02-dispatcher-instructions.ts"
      provides: "Gemini XML-structured dispatcher prompt"
      exports: ["DISPATCHER_INSTRUCTIONS"]
    - path: "src/mastra/workflow/02-improver-dispatcher-instructions.ts"
      provides: "Gemini XML-structured improver dispatcher prompt"
      exports: ["IMPROVER_DISPATCHER_INSTRUCTIONS"]
    - path: "src/mastra/workflow/02-initial-hypothesizer-instructions.ts"
      provides: "Gemini XML-structured hypothesizer prompt with template slots"
      exports: ["INITIAL_HYPOTHESIZER_INSTRUCTIONS"]
    - path: "src/mastra/workflow/02-synthesizer-instructions.ts"
      provides: "Gemini XML-structured synthesizer prompt with template slots"
      exports: ["SYNTHESIZER_INSTRUCTIONS"]
    - path: "src/mastra/workflow/03a-verifier-orchestrator-instructions.ts"
      provides: "Gemini XML-structured verifier orchestrator prompt"
      exports: ["VERIFIER_ORCHESTRATOR_INSTRUCTIONS"]
    - path: "src/mastra/workflow/03b-rules-improver-instructions.ts"
      provides: "Gemini XML-structured rules improver prompt with template slot"
      exports: ["RULES_IMPROVER_INSTRUCTIONS"]
    - path: "src/mastra/workflow/04-question-answerer-instructions.ts"
      provides: "Gemini XML-structured question answerer prompt with confidence scale"
      exports: ["QUESTION_ANSWERER_INSTRUCTIONS"]
  key_links:
    - from: "src/mastra/workflow/rules-tools-prompt.ts"
      to: "src/mastra/workflow/02-initial-hypothesizer-instructions.ts"
      via: "{{RULES_TOOLS_INSTRUCTIONS}} template injection"
      pattern: "\\{\\{RULES_TOOLS_INSTRUCTIONS\\}\\}"
    - from: "src/mastra/workflow/rules-tools-prompt.ts"
      to: "src/mastra/workflow/02-synthesizer-instructions.ts"
      via: "{{RULES_TOOLS_INSTRUCTIONS}} template injection"
      pattern: "\\{\\{RULES_TOOLS_INSTRUCTIONS\\}\\}"
    - from: "src/mastra/workflow/04-question-answerer-instructions.ts"
      to: "workflow-schemas.ts questionAnswerSchema"
      via: "6-level confidence scale mapped to HIGH/MEDIUM/LOW enum"
      pattern: "well-supported.*HIGH|supported.*HIGH|plausible.*MEDIUM"
---

<objective>
Rewrite all 7 Gemini 3 Flash agent instruction files using Google Gemini 3 prompting guide best practices, and update the shared rules-tools-prompt.ts confidence scale.

Purpose: Gemini 3 Flash agents need XML-delimited sections, concise direct instructions, data-before-instructions ordering, and no chain-of-thought scaffolding per vendor guide. The shared rules-tools-prompt.ts confidence guidelines must be updated to the 6-level evidence-based scale before rewriting the 3 agents that inject it via template. Hedged assertion style standardized across all 7 Gemini prompts.

Output: 8 rewritten files (1 shared prompt fragment + 7 instruction files) with consistent XML structure and vocabulary.
</objective>

<execution_context>
@/home/cervon/.claude/get-shit-done/workflows/execute-plan.md
@/home/cervon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-mastra-prompt-engineering/30-CONTEXT.md
@.planning/phases/30-mastra-prompt-engineering/30-RESEARCH.md

Source files to read and rewrite:
@src/mastra/workflow/rules-tools-prompt.ts
@src/mastra/workflow/02-dispatcher-instructions.ts
@src/mastra/workflow/02-improver-dispatcher-instructions.ts
@src/mastra/workflow/02-initial-hypothesizer-instructions.ts
@src/mastra/workflow/02-synthesizer-instructions.ts
@src/mastra/workflow/03a-verifier-orchestrator-instructions.ts
@src/mastra/workflow/03b-rules-improver-instructions.ts
@src/mastra/workflow/04-question-answerer-instructions.ts

Also read for context (do NOT modify):
@src/mastra/workflow/workflow-schemas.ts
@src/mastra/workflow/vocabulary-tools-prompt.ts

<interfaces>
<!-- Each file exports a single const string. Export names MUST be preserved exactly. -->

From rules-tools-prompt.ts:
```typescript
export const RULES_TOOLS_INSTRUCTIONS = `...`.trim();
```

From 02-dispatcher-instructions.ts:
```typescript
export const DISPATCHER_INSTRUCTIONS = `...`.trim();
```

From 02-improver-dispatcher-instructions.ts:
```typescript
export const IMPROVER_DISPATCHER_INSTRUCTIONS = `...`.trim();
```

From 02-initial-hypothesizer-instructions.ts (HAS TEMPLATE SLOTS):
```typescript
export const INITIAL_HYPOTHESIZER_INSTRUCTIONS = `
...
{{RULES_TOOLS_INSTRUCTIONS}}
...
{{VOCABULARY_TOOLS_INSTRUCTIONS}}
...
`.trim();
```

From 02-synthesizer-instructions.ts (HAS TEMPLATE SLOTS):
```typescript
export const SYNTHESIZER_INSTRUCTIONS = `
...
{{RULES_TOOLS_INSTRUCTIONS}}
...
{{VOCABULARY_TOOLS_INSTRUCTIONS}}
...
`.trim();
```

From 03a-verifier-orchestrator-instructions.ts:
```typescript
export const VERIFIER_ORCHESTRATOR_INSTRUCTIONS = `...`.trim();
```

From 03b-rules-improver-instructions.ts (HAS TEMPLATE SLOT):
```typescript
export const RULES_IMPROVER_INSTRUCTIONS = `
...
{{VOCABULARY_TOOLS_INSTRUCTIONS}}
...
`.trim();
```

From 04-question-answerer-instructions.ts:
```typescript
export const QUESTION_ANSWERER_INSTRUCTIONS = `...`.trim();
```

<!-- Template injection slots that MUST survive rewrite: -->
<!-- {{RULES_TOOLS_INSTRUCTIONS}} - in hypothesizer and synthesizer -->
<!-- {{VOCABULARY_TOOLS_INSTRUCTIONS}} - in hypothesizer, synthesizer, and rules-improver -->

<!-- Zod confidence enum (immutable): z.enum(['HIGH', 'MEDIUM', 'LOW']) -->
<!-- Zod priority enum (immutable, different concept): z.enum(['HIGH', 'MEDIUM', 'LOW']) in perspectiveSchema -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared confidence scale and rewrite 4 non-template Gemini prompts</name>
  <files>
    src/mastra/workflow/rules-tools-prompt.ts
    src/mastra/workflow/02-dispatcher-instructions.ts
    src/mastra/workflow/02-improver-dispatcher-instructions.ts
    src/mastra/workflow/03a-verifier-orchestrator-instructions.ts
    src/mastra/workflow/04-question-answerer-instructions.ts
  </files>
  <action>
Read all 5 files fully before making any changes.

**Step 1: Update rules-tools-prompt.ts confidence guidelines**

In the RULES_TOOLS_INSTRUCTIONS string, find the "Set confidence accurately" section (currently 3-level HIGH/MEDIUM/LOW) and replace it with the 6-level evidence-based scale with Zod mapping:

```
4. **Set confidence using this evidence-based scale**:
   - well-supported: ALL examples work, no ambiguity, simple rule with no exceptions. Output as HIGH.
   - supported: 2+ examples align, minor gaps acceptable. Output as HIGH.
   - plausible: 1 clear example, or pattern works but complex with exceptions. Output as MEDIUM.
   - tentative: Partial pattern, gaps remain. Output as MEDIUM.
   - speculative: Inferred, no direct example. Output as LOW.
   - unsupported: Contradicted by data or no evidence. Output as LOW.

   Key: If even one example is ambiguous, the rule cannot be "well-supported".
   Complexity and exceptions reduce confidence -- a rule with 5 exceptions is "plausible" at best.
```

Keep the rest of the rules-tools-prompt content and structure. This file is injected into 3 Gemini agents via template, so it must work within those XML-structured prompts. Leave markdown formatting (the file uses markdown already and will be embedded inside an XML `<tools>` section in host prompts).

**Step 2: Rewrite 4 non-template Gemini instruction files**

For ALL 4 Gemini instruction files (dispatcher, improver-dispatcher, verifier-orchestrator, question-answerer), apply these Gemini 3 guide strategies:

1. Replace ALL markdown headers with XML section tags. Use consistently: `<role>`, `<task>`, `<rules>`, `<output>`, `<constraints>`. Add `<evidence_assessment>` for agents that assess confidence.
2. Do NOT mix XML tags and markdown headers within a prompt (all section boundaries must be XML)
3. Keep instructions concise and direct -- Gemini 3 "may over-analyze" verbose prompt engineering
4. Remove any explicit chain-of-thought scaffolding (step-by-step instructions). Gemini 3 has built-in thinking. Use a brief `<approach>` section if structured decomposition is needed, but keep it minimal.
5. Place critical constraints in `<constraints>` section at the END of the prompt (last section)
6. Use hedged assertion style in any example language or directives about conclusions
7. Keep the `.trim()` call

Agent-specific guidance:

**02-dispatcher-instructions.ts** (generates perspectives for exploration):
- Uses `priority: HIGH/MEDIUM/LOW` -- this is a DIFFERENT concept from rule confidence. Leave priority vocabulary as-is. Do NOT apply 6-level confidence scale to priority.
- Focus on concise task description and clear output format.

**02-improver-dispatcher-instructions.ts** (gap analysis for improvement rounds):
- Uses `priority: HIGH/MEDIUM/LOW` -- same as dispatcher, leave priority vocabulary as-is.
- Focus on gap identification and prioritization criteria.

**03a-verifier-orchestrator-instructions.ts** (systematic rule/sentence testing):
- Does not output confidence directly but orchestrates testers. Add hedged assertion style to how it describes findings.
- Emphasize exhaustiveness: test EVERY rule, test EVERY sentence.
- Keep tool descriptions concise.

**04-question-answerer-instructions.ts** (applies rules to answer questions):
- Has confidence output field (HIGH/MEDIUM/LOW in Zod). Add 6-level confidence scale with Zod mapping.
- Add hedged assertion style: "Based on rules X and Y, the translation appears to be Z"
- This agent produces final answers -- make sure output format is clear and direct.

CRITICAL: Preserve exact export const names. No imports. Single exported const string with `.trim()`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | grep -c "error" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
rules-tools-prompt.ts has 6-level confidence scale with Zod mapping. All 4 non-template Gemini instruction files use XML section tags exclusively, have concise direct instructions, no chain-of-thought scaffolding, and constraints placed last. Priority vocabulary unchanged for dispatchers. Question answerer has 6-level confidence scale. Export names preserved. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite 3 template-injected Gemini prompts</name>
  <files>
    src/mastra/workflow/02-initial-hypothesizer-instructions.ts
    src/mastra/workflow/02-synthesizer-instructions.ts
    src/mastra/workflow/03b-rules-improver-instructions.ts
  </files>
  <action>
Read all 3 files fully before making any changes.

These 3 agents have `{{PLACEHOLDER}}` template slots that are replaced at runtime with the content of rules-tools-prompt.ts and/or vocabulary-tools-prompt.ts. The template slots MUST survive the rewrite in exactly the same form.

For ALL 3 template-injected Gemini instruction files, apply these Gemini 3 guide strategies:

1. Replace ALL markdown headers with XML section tags
2. Place template injection slots inside a dedicated `<tools>` XML section:
   ```
   <tools>
   {{RULES_TOOLS_INSTRUCTIONS}}

   {{VOCABULARY_TOOLS_INSTRUCTIONS}}
   </tools>
   ```
3. Keep instructions concise and direct
4. Remove explicit chain-of-thought scaffolding
5. Place critical constraints in `<constraints>` section LAST
6. Use hedged assertion style for conclusion language
7. Add 6-level confidence scale section where the agent assesses rule confidence (hypothesizer and rules-improver). Note: the rules-tools-prompt.ts (injected via template) already contains the updated 6-level scale from Task 1, so the host prompt's confidence section should REINFORCE the scale, not duplicate it verbatim. A brief reference like "Use the evidence-based confidence scale described in the rules tools section above" works.
8. Keep the `.trim()` call

Agent-specific guidance:

**02-initial-hypothesizer-instructions.ts** (core hypothesis generation):
- Template slots: `{{RULES_TOOLS_INSTRUCTIONS}}` and `{{VOCABULARY_TOOLS_INSTRUCTIONS}}`
- This is the primary reasoning agent. Keep the task description focused on pattern discovery.
- Has confidence output -- reinforce 6-level scale.
- Heavy tool use -- keep tool section clean with templates inside.

**02-synthesizer-instructions.ts** (merges competing rulesets):
- Template slots: `{{RULES_TOOLS_INSTRUCTIONS}}` and `{{VOCABULARY_TOOLS_INSTRUCTIONS}}`
- Focus on rule conflict resolution and merge strategy.
- Has confidence output -- reinforce 6-level scale.

**03b-rules-improver-instructions.ts** (revises rules from feedback):
- Template slot: `{{VOCABULARY_TOOLS_INSTRUCTIONS}}` only (NO `{{RULES_TOOLS_INSTRUCTIONS}}`)
- This agent receives rules via function parameters, not via the rules tools prompt. It still needs the 6-level confidence scale, but must include it directly (not via rules-tools-prompt template).
- Include the full 6-level confidence scale with Zod mapping in this prompt.
- Focus on evidence-based rule improvement from verifier feedback.

CRITICAL VERIFICATION after each file rewrite:
- Run `grep "{{RULES_TOOLS_INSTRUCTIONS}}" <file>` to confirm template slot survives (for hypothesizer and synthesizer)
- Run `grep "{{VOCABULARY_TOOLS_INSTRUCTIONS}}" <file>` to confirm template slot survives (for all 3)
- Ensure the rules-improver does NOT have `{{RULES_TOOLS_INSTRUCTIONS}}` (it never had it)

CRITICAL: Preserve exact export const names. No imports. Single exported const string with `.trim()`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | grep -c "error" | xargs -I{} test {} -eq 0 && grep -c "{{RULES_TOOLS_INSTRUCTIONS}}" src/mastra/workflow/02-initial-hypothesizer-instructions.ts src/mastra/workflow/02-synthesizer-instructions.ts | grep -v ":0$" | wc -l | xargs -I{} test {} -eq 2 && grep -c "{{VOCABULARY_TOOLS_INSTRUCTIONS}}" src/mastra/workflow/02-initial-hypothesizer-instructions.ts src/mastra/workflow/02-synthesizer-instructions.ts src/mastra/workflow/03b-rules-improver-instructions.ts | grep -v ":0$" | wc -l | xargs -I{} test {} -eq 3 && echo "ALL PASS" || echo "FAIL"</automated>
  </verify>
  <done>
All 3 template-injected Gemini instruction files use XML section tags exclusively, have template slots preserved in a `<tools>` section, concise direct instructions, no chain-of-thought scaffolding, constraints placed last, and hedged assertion style. Hypothesizer and synthesizer contain both `{{RULES_TOOLS_INSTRUCTIONS}}` and `{{VOCABULARY_TOOLS_INSTRUCTIONS}}`. Rules-improver contains only `{{VOCABULARY_TOOLS_INSTRUCTIONS}}`. Export names preserved. TypeScript compiles.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. All 8 files compile: `npx tsc --noEmit` (only pre-existing globals.css error)
2. All 8 files export original const names: `grep "export const" src/mastra/workflow/rules-tools-prompt.ts src/mastra/workflow/02-dispatcher-instructions.ts src/mastra/workflow/02-improver-dispatcher-instructions.ts src/mastra/workflow/02-initial-hypothesizer-instructions.ts src/mastra/workflow/02-synthesizer-instructions.ts src/mastra/workflow/03a-verifier-orchestrator-instructions.ts src/mastra/workflow/03b-rules-improver-instructions.ts src/mastra/workflow/04-question-answerer-instructions.ts`
3. No markdown headers remain in Gemini files: `grep "^#" src/mastra/workflow/02-*.ts src/mastra/workflow/03a-verifier-orchestrator-instructions.ts src/mastra/workflow/03b-rules-improver-instructions.ts src/mastra/workflow/04-question-answerer-instructions.ts` should return empty (note: rules-tools-prompt.ts may retain markdown for its embedded content — that is OK)
4. XML tags present in all 7 instruction files: `grep "<role>" src/mastra/workflow/02-*.ts src/mastra/workflow/03a-verifier-orchestrator-instructions.ts src/mastra/workflow/03b-rules-improver-instructions.ts src/mastra/workflow/04-question-answerer-instructions.ts`
5. Template slots preserved: `grep "{{" src/mastra/workflow/02-initial-hypothesizer-instructions.ts src/mastra/workflow/02-synthesizer-instructions.ts src/mastra/workflow/03b-rules-improver-instructions.ts`
6. 6-level confidence scale in rules-tools-prompt.ts: `grep "well-supported" src/mastra/workflow/rules-tools-prompt.ts`
</verification>

<success_criteria>
- rules-tools-prompt.ts has 6-level evidence-based confidence scale with Zod mapping
- All 7 Gemini instruction files use XML section tags exclusively (no markdown headers for structure)
- Template injection slots preserved in hypothesizer (2 slots), synthesizer (2 slots), rules-improver (1 slot)
- No explicit chain-of-thought scaffolding in any Gemini prompt
- Dispatcher and improver-dispatcher keep priority vocabulary unchanged
- Question answerer and rules-improver have 6-level confidence scale
- Hedged assertion style throughout
- All export names preserved, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/30-mastra-prompt-engineering/30-02-SUMMARY.md`
</output>
