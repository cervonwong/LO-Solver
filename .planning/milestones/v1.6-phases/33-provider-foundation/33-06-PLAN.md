---
phase: 33-provider-foundation
plan: 06
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/hooks/use-model-mode.ts
  - src/hooks/use-provider-mode.ts
  - src/hooks/use-solver-workflow.ts
  - src/components/model-mode-toggle.tsx
  - src/components/provider-mode-toggle.tsx
  - src/components/layout-shell.tsx
autonomous: true
requirements:
  - PROV-01

must_haves:
  truths:
    - "useProviderMode hook reads/writes 'lo-solver-provider-mode' localStorage key"
    - "Provider toggle component shows three options: Testing, Production, Claude Code"
    - "Default provider mode is openrouter-testing"
    - "useSolverWorkflow sends providerMode in request body"
    - "layout-shell imports the renamed component"
  artifacts:
    - path: "src/hooks/use-provider-mode.ts"
      provides: "useProviderMode hook replacing useModelMode"
      exports: ["useProviderMode"]
    - path: "src/components/provider-mode-toggle.tsx"
      provides: "Three-way provider selector replacing binary toggle"
      exports: ["ProviderModeToggle"]
  key_links:
    - from: "src/hooks/use-solver-workflow.ts"
      to: "src/hooks/use-provider-mode.ts"
      via: "imports useProviderMode, sends providerMode in request body"
      pattern: "providerMode"
    - from: "src/components/layout-shell.tsx"
      to: "src/components/provider-mode-toggle.tsx"
      via: "renders ProviderModeToggle"
      pattern: "ProviderModeToggle"
---

<objective>
Rename frontend hooks and components from modelMode to providerMode with three-way selection.

Purpose: Enable users to select Claude Code as their provider from the UI. This plan is frontend-only and runs in parallel with Plans 04/05 (all depend on Plan 01's type rename).

Output: Renamed hook, three-way toggle component, updated request body.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-provider-foundation/33-CONTEXT.md
@.planning/phases/33-provider-foundation/33-01-SUMMARY.md
@DESIGN.md

@src/hooks/use-model-mode.ts
@src/hooks/use-solver-workflow.ts
@src/components/model-mode-toggle.tsx
@src/components/layout-shell.tsx

<interfaces>
<!-- Post Plan 01 types. -->

From src/mastra/openrouter.ts (after Plan 01):
```typescript
export type ProviderMode = 'openrouter-testing' | 'openrouter-production' | 'claude-code';
```

From src/mastra/workflow/workflow-schemas.ts (after Plan 01):
```typescript
// rawProblemInputSchema expects providerMode field
providerMode: z.enum(['openrouter-testing', 'openrouter-production', 'claude-code']).default('openrouter-testing')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useProviderMode hook and update useSolverWorkflow</name>
  <files>src/hooks/use-provider-mode.ts, src/hooks/use-solver-workflow.ts, src/hooks/use-model-mode.ts</files>
  <action>
1. Create `src/hooks/use-provider-mode.ts` based on `use-model-mode.ts` with these changes:
   - Type: `type ProviderMode = 'openrouter-testing' | 'openrouter-production' | 'claude-code'` (duplicated from openrouter.ts to avoid importing server module in client code)
   - Storage key: `'lo-solver-provider-mode'`
   - Default: `'openrouter-testing'`
   - `getSnapshot()`: Read from localStorage, validate against the three values, default to `'openrouter-testing'` if invalid
   - Export `useProviderMode(): [ProviderMode, (mode: ProviderMode) => void]`
   - Migration: In `getSnapshot()`, also check for old `'lo-solver-model-mode'` key. If found and `'lo-solver-provider-mode'` doesn't exist, migrate: 'testing' -> 'openrouter-testing', 'production' -> 'openrouter-production'. Write the new key and delete the old one.

2. Update `src/hooks/use-solver-workflow.ts`:
   - Replace `import { useModelMode }` with `import { useProviderMode }` from `'@/hooks/use-provider-mode'`
   - Replace `const [modelMode] = useModelMode()` with `const [providerMode] = useProviderMode()`
   - In `prepareSendMessagesRequest`, replace `modelMode` key with `providerMode` in the `inputData` object
   - Update `chatId` template string: replace `${modelMode}` with `${providerMode}`

3. Delete `src/hooks/use-model-mode.ts` (no longer needed after migration logic is in new hook)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | tail -5</automated>
  </verify>
  <done>useProviderMode hook exists with 3-value type, localStorage migration from old key, and useSolverWorkflow sends providerMode in request body. Old use-model-mode.ts is deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProviderModeToggle component and update layout</name>
  <files>src/components/provider-mode-toggle.tsx, src/components/layout-shell.tsx, src/components/model-mode-toggle.tsx</files>
  <action>
1. Read DESIGN.md first to follow UI conventions (colors, typography, component patterns).

2. Create `src/components/provider-mode-toggle.tsx`:
   - Import `useProviderMode` from `'@/hooks/use-provider-mode'`
   - Replace the binary Switch with a three-segment selector. Use a simple button group pattern (not a full shadcn Select, to keep it lightweight and visible):
     ```
     [Testing ($)] [Production ($$$)] [Claude Code]
     ```
   - Each segment is a button. The active segment gets `text-accent` styling; inactive get `text-muted-foreground`. Follow existing DESIGN.md conventions for hover states and font.
   - Use `font-heading text-sm` class (same as current ModelModeToggle)
   - Active state: use the accent color per DESIGN.md
   - Inactive state: `text-muted-foreground` (same as current toggle)
   - Accept `disabled?: boolean` prop (same interface as current component)
   - When disabled, apply `opacity-50 pointer-events-none` (same as current)
   - Export as `ProviderModeToggle`

3. Update `src/components/layout-shell.tsx`:
   - Replace `import { ModelModeToggle }` with `import { ProviderModeToggle }` from `'@/components/provider-mode-toggle'`
   - Replace `<ModelModeToggle` with `<ProviderModeToggle` in the JSX

4. Delete `src/components/model-mode-toggle.tsx` (replaced by provider-mode-toggle)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | tail -5</automated>
  </verify>
  <done>ProviderModeToggle renders three options with correct styling. layout-shell uses the new component. Old model-mode-toggle.tsx is deleted. No references to ModelModeToggle or useModelMode remain in src/.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (only pre-existing globals.css error)
2. `grep -r 'modelMode\|model-mode\|ModelMode\|ModelModeToggle\|useModelMode\|lo-solver-model-mode' src/ --include='*.ts' --include='*.tsx' | grep -v node_modules` returns 0 results
3. `grep 'providerMode' src/hooks/use-solver-workflow.ts` shows providerMode in request body
4. `grep 'ProviderModeToggle' src/components/layout-shell.tsx` shows new component
</verification>

<success_criteria>
- Three-way provider selector visible in the UI
- providerMode sent in request body with correct values
- localStorage key migrated from old format
- All old modelMode/ModelModeToggle references removed from frontend
- All files compile
</success_criteria>

<output>
After completion, create `.planning/phases/33-provider-foundation/33-06-SUMMARY.md`
</output>
