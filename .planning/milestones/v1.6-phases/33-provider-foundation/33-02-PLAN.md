---
phase: 33-provider-foundation
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/mastra/workflow/steps/01-extract.ts
  - src/mastra/workflow/steps/02-hypothesize.ts
  - src/mastra/workflow/steps/02a-dispatch.ts
  - src/mastra/workflow/steps/02b-hypothesize.ts
  - src/mastra/workflow/steps/02c-verify.ts
  - src/mastra/workflow/steps/02d-synthesize.ts
  - src/mastra/workflow/steps/03-answer.ts
  - src/mastra/workflow/03a-rule-tester-agent.ts
  - src/mastra/workflow/03a-sentence-tester-agent.ts
autonomous: true
requirements:
  - PROV-01

must_haves:
  truths:
    - "No references to modelMode or model-mode remain in src/mastra/workflow/"
    - "All 9 step/agent files compile with the new ProviderMode type"
  artifacts:
    - path: "src/mastra/workflow/steps/01-extract.ts"
      provides: "First step using providerMode"
      contains: "providerMode"
    - path: "src/mastra/workflow/steps/02-hypothesize.ts"
      provides: "Hypothesize step with ProviderMode in HypothesizeContext"
      contains: "providerMode"
  key_links:
    - from: "src/mastra/workflow/steps/01-extract.ts"
      to: "src/mastra/openrouter.ts"
      via: "imports ProviderMode type"
      pattern: "import.*ProviderMode"
    - from: "src/mastra/workflow/steps/02-hypothesize.ts"
      to: "src/mastra/workflow/workflow-schemas.ts"
      via: "reads providerMode from state"
      pattern: "state\\.providerMode"
---

<objective>
Mechanical rename of modelMode to providerMode across all 9 workflow step and agent files.

Purpose: Complete the rename started in Plan 01's core files. This is a TypeScript-compiler-guided mechanical change -- every file follows the same pattern of replacing imports, variable names, and string keys.

Output: All workflow step and agent files compiling with the new ProviderMode type.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-provider-foundation/33-CONTEXT.md
@.planning/phases/33-provider-foundation/33-01-SUMMARY.md

@src/mastra/openrouter.ts
@src/mastra/workflow/workflow-schemas.ts
@src/mastra/workflow/request-context-types.ts
@src/mastra/workflow/steps/01-extract.ts
@src/mastra/workflow/steps/02-hypothesize.ts
@src/mastra/workflow/steps/02a-dispatch.ts
@src/mastra/workflow/steps/02b-hypothesize.ts
@src/mastra/workflow/steps/02c-verify.ts
@src/mastra/workflow/steps/02d-synthesize.ts
@src/mastra/workflow/steps/03-answer.ts
@src/mastra/workflow/03a-rule-tester-agent.ts
@src/mastra/workflow/03a-sentence-tester-agent.ts

<interfaces>
<!-- Post Plan 01 types. -->

From src/mastra/openrouter.ts (after Plan 01):
```typescript
export type ProviderMode = 'openrouter-testing' | 'openrouter-production' | 'claude-code';
export function activeModelId(providerMode: ProviderMode, productionModel: string, claudeCodeModel?: string): string;
```

From src/mastra/workflow/request-context-types.ts (after Plan 01):
```typescript
export interface WorkflowRequestContext {
  'provider-mode': ProviderMode;
  // ...
}
```

From src/mastra/workflow/steps/02-hypothesize.ts (before rename):
```typescript
export interface HypothesizeContext {
  modelMode: ModelMode;
  // ...other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename modelMode to providerMode in all 7 step files</name>
  <files>src/mastra/workflow/steps/01-extract.ts, src/mastra/workflow/steps/02-hypothesize.ts, src/mastra/workflow/steps/02a-dispatch.ts, src/mastra/workflow/steps/02b-hypothesize.ts, src/mastra/workflow/steps/02c-verify.ts, src/mastra/workflow/steps/02d-synthesize.ts, src/mastra/workflow/steps/03-answer.ts</files>
  <action>
Apply the same mechanical rename pattern across all 7 step files:

- Replace all `import type { ModelMode }` with `import type { ProviderMode }`
- Replace `import { activeModelId }` (already imported, just signature changed)
- Replace all `ctx.modelMode` / `state.modelMode` / `inputData.modelMode` references with `ctx.providerMode` / `state.providerMode` / `inputData.providerMode`
- Replace all `requestContext.set('model-mode', ...)` with `requestContext.set('provider-mode', ...)`
- Replace all type casts `as ModelMode` with `as ProviderMode`
- Update `activeModelId()` calls: old signature was `activeModelId(mode, productionModel)`, new is `activeModelId(providerMode, productionModel)` (the third arg claudeCodeModel is optional, not needed yet)
- In `02-hypothesize.ts`: update `HypothesizeContext.modelMode: ModelMode` to `providerMode: ProviderMode`, and the testing-mode guards `state.modelMode === 'testing'` become `state.providerMode === 'openrouter-testing'`

After all changes, run `npx tsc --noEmit` and fix any remaining type errors in these files.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | grep -c "workflow/steps/" ; echo "---" ; grep -r 'modelMode\|model-mode\|ModelMode' src/mastra/workflow/steps/ --include='*.ts' | wc -l</automated>
  </verify>
  <done>Zero type errors from step files. Zero references to modelMode/model-mode/ModelMode in src/mastra/workflow/steps/.</done>
</task>

<task type="auto">
  <name>Task 2: Rename modelMode to providerMode in 2 tester agent files</name>
  <files>src/mastra/workflow/03a-rule-tester-agent.ts, src/mastra/workflow/03a-sentence-tester-agent.ts</files>
  <action>
Update requestContextSchema in both tester agent files:

**src/mastra/workflow/03a-rule-tester-agent.ts and 03a-sentence-tester-agent.ts:**
- Update requestContextSchema: `'model-mode': z.enum(['testing', 'production'])` becomes `'provider-mode': z.enum(['openrouter-testing', 'openrouter-production', 'claude-code'])`
- Update any other `modelMode` / `model-mode` / `ModelMode` references to `providerMode` / `provider-mode` / `ProviderMode`

After changes, run `npx tsc --noEmit` and confirm no errors remain in src/mastra/workflow/.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "globals.css" | grep -c "mastra/workflow" ; echo "---" ; grep -r 'modelMode\|model-mode\|ModelMode' src/mastra/workflow/ --include='*.ts' | wc -l</automated>
  </verify>
  <done>Zero type errors from workflow files. Zero references to modelMode/model-mode/ModelMode anywhere in src/mastra/workflow/. The rename is complete for all workflow code.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit 2>&1 | grep -v "globals.css"` — no errors in workflow files (eval file errors expected until Plan 03)
2. `grep -r 'modelMode\|model-mode\|ModelMode' src/mastra/workflow/ --include='*.ts' | wc -l` returns 0
3. `grep -r 'providerMode' src/mastra/workflow/steps/01-extract.ts` shows the new field name
</verification>

<success_criteria>
- All 9 step/agent files compile with ProviderMode
- No references to old modelMode/model-mode remain in src/mastra/workflow/
- Existing OpenRouter behavior preserved (openrouter-testing/openrouter-production map to old testing/production)
</success_criteria>

<output>
After completion, create `.planning/phases/33-provider-foundation/33-02-SUMMARY.md`
</output>
