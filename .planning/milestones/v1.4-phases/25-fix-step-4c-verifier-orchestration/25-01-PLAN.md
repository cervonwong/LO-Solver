---
phase: 25-fix-step-4c-verifier-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/.claude/skills/solve/SKILL.md
autonomous: true
requirements:
  - ORCH-03
  - ORCH-04
  - VERI-01

must_haves:
  truths:
    - "Step 4c dispatches verifier per-rule and per-sentence (not monolithic), matching Step 5c's per-call pattern"
    - "Step 4c assembles verification-{P}.md with ## Summary section and Pass rate from individual test results"
    - "Step 4f dispatches verifier per-rule and per-sentence for convergence check on solution.md"
    - "Step 4f writes verification.md in the same ## Summary format as Step 5c"
    - "Step 4d can extract per-perspective pass rates from Step 4c's output without needing any changes"
  artifacts:
    - path: "claude-code/.claude/skills/solve/SKILL.md"
      provides: "Per-call verifier orchestration for Steps 4c and 4f"
      contains: "Step 4c.1"
  key_links:
    - from: "SKILL.md Step 4c.4"
      to: "SKILL.md Step 4d"
      via: "verification-{P}.md with ## Summary section containing Pass rate: {N}%"
      pattern: "Pass rate:"
    - from: "SKILL.md Step 4f"
      to: "SKILL.md Step 5a"
      via: "verification.md with ## Summary section containing Pass rate: {N}%"
      pattern: "Pass rate:"
    - from: "SKILL.md Step 4c"
      to: "claude-code/.claude/agents/verifier.md"
      via: "Per-call dispatch: rule test mode + sentence test mode"
      pattern: "rule test mode"
---

<objective>
Rewrite SKILL.md Steps 4c and 4f to use the per-call verifier orchestration pattern already proven in Step 5c, fixing the INT-01 design mismatch identified in the v1.4 milestone audit.

Purpose: The verifier agent (Phase 23) only supports Rule Test Mode (one rule per call) and Sentence Test Mode (one sentence per call). Step 4c currently dispatches it monolithically, which produces no usable output. Step 5c correctly implements per-call dispatch with skill-side aggregation. Step 4c and 4f must match.

Output: Updated SKILL.md with Step 4c using sub-steps 4c.1-4c.4 (matching 5c.1-5c.4) and Step 4f using the same per-call pattern for convergence checking.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-fix-step-4c-verifier-orchestration/25-RESEARCH.md

@claude-code/.claude/skills/solve/SKILL.md
@claude-code/.claude/agents/verifier.md
@claude-code/references/workspace-format.md

<interfaces>
<!-- The verifier agent API (from claude-code/.claude/agents/verifier.md) -->

Input:
1. Test type: "rule" or "sentence"
2. Path to current solution file (solution.md, improved-{N}.md, or perspective-{P}.md)
3. Path to problem.md
4. Test-specific input:
   - Rule tests: exact rule title (matching ### {title} heading)
   - Sentence tests: specific sentence + translation direction
5. Output file path

Rule Test Output:
```
## Rule: {rule title}
**Status:** {PASS|FAIL|NEEDS_UPDATE}
**Reasoning:** {2-3 sentences}
**Recommendation:** {fix suggestion or "None."}
```

Sentence Test Output:
```
## Sentence: {the input sentence}
**Translation:** {blind translation}
**Working:** {morpheme breakdown, rule application, synthesis}
```

<!-- Step 5c sub-step structure to mirror (from SKILL.md lines 196-281) -->

5c.1: Extract rules and sentences from current solution and problem
5c.2: Test rules (one per call) - dispatch verifier per rule title, read Status
5c.3: Test sentences (one per call, blind translation) - dispatch per sentence, normalize + compare
5c.4: Aggregate and write verification file with ## Summary, ## Rule Results, ## Sentence Results, ## Question Coverage

Pass rate formula: round(100 * (rules_passed + sentences_passed) / (rules_total + sentences_total))
Questions: NOT in pass rate denominator

<!-- Step 4d extraction pattern (from SKILL.md lines 103-111) -->

For each perspective with a verification file:
- Read ## Summary section of verification-{P}.md
- Extract Pass rate: {N}% value
- Print summary line

<!-- Verification file format (from workspace-format.md) -->

Header for perspectives: # Verification: Perspective {P}
Header for final/convergence: # Final Verification
## Summary section with: Rules tested/passed/failed, Sentences tested/passed/failed, Pass rate: {N}%
## Rule Results with per-rule ### {title}, **Status**, **Notes**
## Sentence Results table
## Question Coverage table (for logging only)

<!-- Step 4c adaptation points (from 25-RESEARCH.md) -->

1. Solution file: {WORKSPACE}/hypotheses/round-{N}/perspective-{P}.md (not CURRENT_SOLUTION)
2. Output file: {WORKSPACE}/hypotheses/round-{N}/verification-{P}.md (not workspace root)
3. Header: # Verification: Perspective {P} (not Iteration {I})
4. Step 4f solution file: {WORKSPACE}/solution.md
5. Step 4f output file: {WORKSPACE}/verification.md
6. Step 4f header: # Final Verification (per workspace-format.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Step 4c with per-call verifier orchestration</name>
  <files>claude-code/.claude/skills/solve/SKILL.md</files>
  <action>
Replace SKILL.md's current Step 4c section (lines 88-101, from "### Step 4c: Dispatch Verifiers" through the error handling block ending before "### Step 4d") with a per-call verifier orchestration matching Step 5c's sub-step structure. The new Step 4c must have four sub-steps:

**Step 4c.1: Extract rules and sentences from perspective and problem**
- Read `{WORKSPACE}/hypotheses/round-{N}/perspective-{P}.md` to get rule titles (each `### {title}` under `## Rules`)
- Read `{WORKSPACE}/problem.md` to get dataset sentences and questions

**Step 4c.2: Test rules (one per call)**
- For each rule title: dispatch verifier in rule test mode with:
  - Test type: "rule"
  - Rule title: {rule_title}
  - Solution file: `{WORKSPACE}/hypotheses/round-{N}/perspective-{P}.md`
  - Problem file: `{WORKSPACE}/problem.md`
  - Output file: a temporary result (read back immediately)
- Read verifier output to get Status (PASS/FAIL/NEEDS_UPDATE) and reasoning
- Record: rule title, status, reasoning

**Step 4c.3: Test sentences (one per call, blind translation)**
- For each dataset sentence: dispatch verifier in sentence test mode with:
  - Test type: "sentence"
  - Sentence text and direction
  - Solution file: `{WORKSPACE}/hypotheses/round-{N}/perspective-{P}.md`
  - Problem file: `{WORKSPACE}/problem.md`
  - Output file: a temporary result
- Read verifier output to get blind Translation
- Normalize both translations (trim, lowercase, strip punctuation)
- Compare: PASS if match, FAIL otherwise
- Record: sentence number, expected, got, PASS/FAIL
- For questions: dispatch verifier similarly but record for coverage logging only (NOT in pass rate denominator)

**Step 4c.4: Aggregate and write verification file**
- Compute pass rate: `round(100 * (rules_passed + sentences_passed) / (rules_total + sentences_total))`
- Write `{WORKSPACE}/hypotheses/round-{N}/verification-{P}.md` with:
  - Header: `# Verification: Perspective {P}`
  - `## Summary` with Rules tested/passed/failed, Sentences tested/passed/failed, Pass rate: {N}%
  - `## Rule Results` with per-rule sections (### {title}, **Status**, **Notes**)
  - `## Sentence Results` table (matching Step 5c.4 format)
  - `## Question Coverage` table (matching Step 5c.4 format)

Mirror Step 5c's writing style and level of instruction detail. Use the same key phrases ("read back immediately", "normalize both", "for logging, not pass rate") so the two sections are clearly parallel.

Keep the existing error handling pattern: "If verification file does not exist, print error and continue."

Do NOT modify Step 5c or any other step.
  </action>
  <verify>
    <automated>grep -c "Step 4c\." claude-code/.claude/skills/solve/SKILL.md | grep -q "[4-9]" && grep -q "rule test mode" claude-code/.claude/skills/solve/SKILL.md && grep -q "sentence test mode" claude-code/.claude/skills/solve/SKILL.md && grep -q "Step 4c.4" claude-code/.claude/skills/solve/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Step 4c has four sub-steps (4c.1-4c.4) that dispatch the verifier per-rule and per-sentence, aggregate results, and write verification-{P}.md with ## Summary including Pass rate: {N}%</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Step 4f convergence check and verify Step 4d compatibility</name>
  <files>claude-code/.claude/skills/solve/SKILL.md</files>
  <action>
**Part A: Rewrite Step 4f**

Replace SKILL.md's current Step 4f section (from "### Step 4f: Convergence Check" through the convergence decision block ending before "## Step 5") with a per-call verifier orchestration. The structure mirrors Step 4c but adapted for the merged solution:

- Solution file: `{WORKSPACE}/solution.md` (not a perspective file)
- Output file: `{WORKSPACE}/verification.md` (workspace root, not hypotheses/round-{N}/)
- Header: `# Final Verification` (per workspace-format.md template)
- Same four sub-steps as Step 4c (4f.1, 4f.2, 4f.3, 4f.4) but adapted for the convergence context

After writing verification.md, keep the existing convergence decision logic:
- If 100%: print "Converged!" and break
- If not converged and round < 3: print pass rate and continue
- If not converged and round = 3: print "Maximum rounds reached" and break

The convergence decision reads from the `## Summary` section of `{WORKSPACE}/verification.md`, which the new Step 4f.4 just wrote. This is self-consistent.

**Part B: Verify Step 4d compatibility**

Read the current Step 4d instructions (lines ~103-111). Verify that Step 4d's pass rate extraction still works with the new Step 4c output:

1. Step 4d reads `## Summary` section of `{WORKSPACE}/hypotheses/round-{N}/verification-{P}.md` -- this path is unchanged
2. Step 4d extracts `Pass rate: {N}%` -- the new Step 4c.4 writes this exact format
3. Step 4d prints a summary line -- format is unchanged

If Step 4d's instructions already match (they should, since we're writing the format they expect), do NOT modify Step 4d. If any wording is ambiguous or references the old monolithic verifier dispatch, make minimal clarifying edits only.

Do NOT modify Steps 5a-5d, Step 6, or any other section outside Step 4c, 4d, and 4f.
  </action>
  <verify>
    <automated>grep -q "Step 4f\." claude-code/.claude/skills/solve/SKILL.md && grep -q "Final Verification" claude-code/.claude/skills/solve/SKILL.md && grep -q "Pass rate:" claude-code/.claude/skills/solve/SKILL.md && grep -c "monolithic\|full perspective\|Use the \*\*verifier\*\* agent:" claude-code/.claude/skills/solve/SKILL.md | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Step 4f uses per-call verifier pattern (4f.1-4f.4) writing verification.md with # Final Verification header. Step 4d's pass rate extraction works unchanged with the corrected Step 4c output format. No monolithic verifier dispatch remains in Steps 4c or 4f.</done>
</task>

</tasks>

<verification>
1. Step 4c has sub-steps 4c.1, 4c.2, 4c.3, 4c.4 (grep confirms)
2. Step 4f has sub-steps 4f.1, 4f.2, 4f.3, 4f.4 (grep confirms)
3. No monolithic verifier dispatch remains in Steps 4c or 4f
4. Step 4c.4 writes ## Summary with Pass rate: {N}% -- same format Step 5c.4 uses
5. Step 4f.4 writes # Final Verification header (matching workspace-format.md)
6. Step 4d reads the same format without changes needed
7. Step 5c is unchanged
8. Questions are excluded from pass rate denominator in both 4c and 4f
9. Normalization (trim, lowercase, strip punctuation) matches Step 5c.3
</verification>

<success_criteria>
- SKILL.md Steps 4c and 4f use per-call verifier orchestration (no monolithic dispatch)
- Verification file format is consistent across Steps 4c, 4f, and 5c (same ## Summary section structure)
- Step 4d extracts pass rates from Step 4c output without modification
- Step 5 and Step 6 are completely unchanged
- `npx tsc --noEmit` still passes (no TypeScript files modified, but verifies no accidental breakage)
</success_criteria>

<output>
After completion, create `.planning/phases/25-fix-step-4c-verifier-orchestration/25-01-SUMMARY.md`
</output>
