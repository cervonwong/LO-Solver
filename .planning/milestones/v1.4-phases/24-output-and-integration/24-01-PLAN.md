---
phase: 24-output-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/.claude/skills/solve/SKILL.md
  - claude-code/references/workspace-format.md
autonomous: true
requirements: [OUTP-01, OUTP-02, OUTP-03]

must_haves:
  truths:
    - "After the pipeline completes, the terminal displays discovered rules as numbered one-liners with titles and descriptions"
    - "Failing rules are shown inline with [FAIL] and the verifier's reasoning indented beneath"
    - "Terminal displays translated answers as a numbered list (Q1: input -> translation)"
    - "Terminal always displays the final pass rate (even at 100%)"
    - "Agent failures appear in terminal only if they affected the result"
    - "Solution file path and workspace path are NOT printed at the end"
    - "A solution-complete.md file is written to the workspace with sections: Rules, Vocabulary, Verification Summary, Answers, and conditionally Pipeline Notes"
    - "Rules in solution-complete.md include full detail: title, description, evidence, confidence, verification status, and failure reasoning for failed rules"
    - "Vocabulary in solution-complete.md uses a table with Form, Meaning, Type, Notes columns"
    - "Verification summary shows one line per iteration with pass rate"
    - "Pipeline Notes section is only included if errors occurred"
    - "Problem structure is referenced (See problem.md), not inlined"
    - "Hypothesis round details are not included in solution-complete.md"
    - "All intermediate workspace files are preserved (no cleanup step)"
  artifacts:
    - path: "claude-code/.claude/skills/solve/SKILL.md"
      provides: "Step 6 output logic (terminal display + solution file)"
      contains: "Step 6"
    - path: "claude-code/references/workspace-format.md"
      provides: "solution-complete.md format template"
      contains: "solution-complete.md"
  key_links:
    - from: "claude-code/.claude/skills/solve/SKILL.md"
      to: "claude-code/references/workspace-format.md"
      via: "Step 6b references the solution-complete.md template"
      pattern: "solution-complete"
    - from: "claude-code/.claude/skills/solve/SKILL.md Step 6"
      to: "claude-code/.claude/skills/solve/SKILL.md Step 5"
      via: "Step 6 reads CURRENT_SOLUTION and CURRENT_VERIFICATION variables set in Step 5"
      pattern: "CURRENT_SOLUTION|CURRENT_VERIFICATION"
---

<objective>
Add terminal results display and consolidated solution file output to the /solve skill, completing the pipeline.

Purpose: Without this, the pipeline ends with "Pipeline complete" and the user must manually read workspace files to see results. Step 6 presents a formatted summary in the terminal and writes a comprehensive solution-complete.md that consolidates all intermediate outputs into one document.

Output: Updated SKILL.md with Step 6 (two substeps: 6a terminal display, 6b solution file). Updated workspace-format.md with solution-complete.md template.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-output-and-integration/24-CONTEXT.md
@.planning/phases/24-output-and-integration/24-RESEARCH.md

# The files being modified
@claude-code/.claude/skills/solve/SKILL.md
@claude-code/references/workspace-format.md

# Pipeline reference (for understanding workspace file structure)
@claude-code/CLAUDE.md
</context>

<interfaces>
<!-- From SKILL.md: Variables set during Step 5 that Step 6 must use -->

CURRENT_SOLUTION: path to the latest solution file
  - Initially set to {WORKSPACE}/solution.md (Step 5a)
  - Updated to {WORKSPACE}/improved-{I}.md after each improvement iteration (Step 5b)
  - Contains ## Vocabulary and ## Rules sections

CURRENT_VERIFICATION: path to the latest verification file
  - Initially set to {WORKSPACE}/verification.md (Step 5a)
  - Updated to {WORKSPACE}/verification-{I}.md after each verify iteration (Step 5c)
  - Contains ## Summary with pass rate, ## Rule Results with per-rule status

Other workspace files read by Step 6:
  - {WORKSPACE}/answers.md: per-question translations (## Q{N} sections)
  - {WORKSPACE}/errors.md: error log (if it exists, contains ## Entry sections)
  - {WORKSPACE}/verification.md: iteration 0 pass rate (from Step 4f)
  - {WORKSPACE}/verification-{I}.md: iteration I pass rate (from Step 5c, I=1..4, may not all exist)

<!-- From SKILL.md: Current ending that must be replaced -->

Current final line of SKILL.md:
  Print: `"Pipeline complete. Workspace: {WORKSPACE}/"`

This line must be removed (user decision: do not print workspace path). Step 6 output replaces it as the completion signal.

<!-- From workspace-format.md: File structures that Step 6 reads from -->

solution.md / improved-N.md structure:
  ## Vocabulary: table with Foreign Form, Meaning, Type, Notes, Source columns
  ## Rules: each rule as ### {N}. {Title} with description, Evidence, Source, Confidence

verification.md / verification-N.md structure:
  ## Summary: pass rate and counts
  ## Rule Results: each rule as ### Rule {N}: {Title} with Status and Notes
  ## Sentence Results: table with sentence translations and Status

answers.md structure:
  ## Q{N}: {Direction}: with Input, Translation, Confidence, Working, Rules applied

errors.md structure (if exists):
  ## Entry {N}: with Time, Agent, Error type, Message, Recovered fields
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add solution-complete.md template to workspace-format.md</name>
  <files>claude-code/references/workspace-format.md</files>
  <action>
Append a new section to the end of workspace-format.md (after the existing `errors.md` section) with the template for `solution-complete.md`. Follow the same documentation style as existing sections in the file (section header, description, then fenced markdown template).

Add this section:

```
---

## solution-complete.md

Produced by the /solve skill in Step 6. Consolidates rules, vocabulary, verification history, and answers into a single results document. References the problem file instead of including it inline.

(fenced markdown template -- see below)
```

The template content inside the fenced block should be:

```markdown
# Complete Solution

> Problem: See problem.md

## Rules

### 1. {Rule Title}

{Full description of the rule}

**Evidence:** {evidence sentences from the dataset}
**Confidence:** {HIGH/MEDIUM/LOW}
**Verification:** {PASS/FAIL}
{If FAIL: **Failure reason:** {verifier's reasoning about why the rule failed}}

(repeat for each rule)

## Vocabulary

| Form | Meaning | Type | Notes |
|------|---------|------|-------|
| {form} | {meaning} | {type} | {notes} |

## Verification Summary

- Iteration 0 (initial): {rate}%
- Iteration 1: {rate}%
- ...
- Final: {rate}%

## Answers

### Q1: {Direction}

**Input:** {sentence}
**Translation:** {answer}
**Confidence:** {level}

(repeat for each question)

## Pipeline Notes

> This section is only included if errors occurred during the pipeline.

- {agent}: {error message} (Recovered: {yes/no})
```

Use the Taloki fictional language for the template example, consistent with all other templates in workspace-format.md. Fill in the template with concrete Taloki examples just like the other sections do (e.g., the solution.md and answers.md examples use Taloki). Include 2-3 rules, a vocabulary table, 2-3 verification iterations, and 2-3 answer questions.
  </action>
  <verify>
- `test -f claude-code/references/workspace-format.md`
- Confirm the file contains a `## solution-complete.md` section
- Confirm the template has sections in the required order: Rules, Vocabulary, Verification Summary, Answers, Pipeline Notes
- Confirm the Rules template includes Evidence, Confidence, Verification, and Failure reason fields
- Confirm the Vocabulary table has Form, Meaning, Type, Notes columns
- Confirm the Verification Summary uses one-line-per-iteration format
- Confirm the Pipeline Notes section has a conditional note about only being included on errors
- Confirm the problem is referenced as "See problem.md" (not inlined)
  </verify>
  <done>workspace-format.md contains a solution-complete.md template section with concrete Taloki examples, following the same documentation style as other sections, with all required fields and section ordering matching user decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 6 output logic to SKILL.md</name>
  <files>claude-code/.claude/skills/solve/SKILL.md</files>
  <action>
Replace the final line of SKILL.md (`Print: "Pipeline complete. Workspace: {WORKSPACE}/"`) with a complete Step 6. Keep all existing content (Steps 1-5d) exactly as-is. The new Step 6 follows the same prose instruction style as Steps 3-5.

### Step 6: Output Results

#### Step 6a: Display Results in Terminal

Read {CURRENT_SOLUTION} to extract the rules (from ## Rules section) and vocabulary (from ## Vocabulary section).
Read {WORKSPACE}/answers.md to extract translated answers (from each ## Q{N} section).
Read {CURRENT_VERIFICATION} to get the overall pass rate (from ## Summary) and per-rule results (from ## Rule Results).

Print: `""` (blank line for spacing)
Print: `"--- Results ---"`
Print: `""`

Print: `"Rules:"`

For each rule (### {title} under ## Rules in CURRENT_SOLUTION):
  - Extract the rule title and the first sentence of its description as a one-liner
  - Look up this rule's status in CURRENT_VERIFICATION's ## Rule Results section
  - Print: `"{N}. {title} -- {one-line description}"`
  - If the rule's status is FAIL or NEEDS_UPDATE:
    - Extract the verifier's reasoning from the Rule Results Notes field
    - Print: `"   [FAIL] {reasoning}"`

Print: `""`
Print: `"Answers:"`

For each question section (## Q{N} in answers.md):
  - Extract the input sentence (from **Input:** line) and translation (from **Translation:** or **Revised translation:** line, preferring revised if present)
  - Print: `"{Q_ID}: {input} -> {translation}"`

Print: `""`

Extract the pass rate from CURRENT_VERIFICATION's ## Summary section.
Print: `"Pass rate: {rate}%"`

If {WORKSPACE}/errors.md exists (check with `test -f`):
  Read it and check for errors that affected the outcome -- look for entries where Recovered is "No" or entries that mention fallback/degraded behavior.
  If any such errors exist:
    Print: `""`
    Print: `"Warnings:"`
    For each relevant error:
      Print: `"- {agent}: {message}"`

#### Step 6b: Write Solution File

Read {CURRENT_SOLUTION} for the full rules and vocabulary sections.
Read {CURRENT_VERIFICATION} for per-rule verification status and reasoning.
Read {WORKSPACE}/answers.md for translated answers.
If {WORKSPACE}/errors.md exists (check with `test -f`), read it for pipeline notes.

Collect verification history:
  Read {WORKSPACE}/verification.md and extract the pass rate from ## Summary (this is iteration 0).
  For I = 1 to 4:
    Check if {WORKSPACE}/verification-{I}.md exists (using `test -f` via Bash).
    If it exists, read it and extract the pass rate from ## Summary.

Write {WORKSPACE}/solution-complete.md using the Write tool with the structure defined in references/workspace-format.md (## solution-complete.md template).

Key formatting rules for the solution file:
- Problem reference: `> Problem: See problem.md` (do not include problem content inline)
- Rules section: include full description, evidence, confidence, verification status (PASS/FAIL), and for failed rules include the verifier's failure reasoning
- Vocabulary section: table with Form, Meaning, Type, Notes columns (Source column from CURRENT_SOLUTION can be omitted)
- Verification Summary: one line per iteration that actually occurred (check file existence before including), format: `- Iteration {N}: {rate}%` with iteration 0 labeled `- Iteration 0 (initial): {rate}%`
- Answers section: one subsection per question with Input, Translation, and Confidence
- Pipeline Notes section: include ONLY if errors.md exists and contains entries. List each error with agent name, message, and recovery status. Omit this section entirely on clean runs.

Do NOT include:
- Hypothesis round details (per-perspective results, individual round data)
- Full verification tables (the summary lines per iteration cover the history)
- The problem dataset or context (referenced via "See problem.md")

**Important variable tracking:** Step 6 must use the same CURRENT_SOLUTION and CURRENT_VERIFICATION variables that Step 5 sets. If the verify-improve loop ran, CURRENT_SOLUTION points to the last improved-{I}.md and CURRENT_VERIFICATION points to the last verification-{I}.md. If the loop was skipped (100% at Step 4f), CURRENT_SOLUTION is solution.md and CURRENT_VERIFICATION may not be set (in that case, use verification.md). Handle both cases.

**Edge cases:**
- If answers.md does not exist (answerer failed), print `"No answers generated -- see errors.md for details."` instead of the Answers section in terminal. In the solution file, include an Answers section noting the failure.
- If the verify-improve loop was skipped (100% at Step 4f), the verification summary has only one line: `- Iteration 0 (initial): 100%`
- If fewer than 4 improvement iterations ran (early convergence), only include the iterations that actually have verification files
  </action>
  <verify>
- `test -f claude-code/.claude/skills/solve/SKILL.md`
- Confirm Steps 1-5d are preserved unchanged
- Confirm the old `"Pipeline complete. Workspace: {WORKSPACE}/"` line is removed
- Confirm Step 6 exists with two substeps: 6a (terminal display) and 6b (solution file)
- Confirm Step 6a prints rules as numbered one-liners with title and description
- Confirm Step 6a prints failing rules inline with `[FAIL]` marker and reasoning
- Confirm Step 6a prints answers as `{Q_ID}: {input} -> {translation}`
- Confirm Step 6a always prints pass rate
- Confirm Step 6a checks errors.md conditionally (only if it exists and contains outcome-affecting errors)
- Confirm Step 6a does NOT print workspace path or solution file path
- Confirm Step 6b writes {WORKSPACE}/solution-complete.md
- Confirm Step 6b collects verification history across iteration files
- Confirm Step 6b references workspace-format.md for the template
- Confirm Step 6b includes conditional Pipeline Notes (only on errors)
- Confirm Step 6b references problem as "See problem.md" (not inline)
- Confirm Step 6b does NOT include hypothesis round details
- Confirm no cleanup/deletion of workspace files exists anywhere in the skill
- `grep -c 'Pipeline complete. Workspace' claude-code/.claude/skills/solve/SKILL.md` returns 0
  </verify>
  <done>SKILL.md Step 6 contains complete output logic: Step 6a displays rules (with inline [FAIL] markers), answers (numbered list), and pass rate in terminal without printing paths. Step 6b writes solution-complete.md with Rules (full detail + verification status), Vocabulary (table), Verification Summary (per-iteration lines), Answers, and conditional Pipeline Notes. The old "Pipeline complete" line is removed. CURRENT_SOLUTION and CURRENT_VERIFICATION variables from Step 5 are used consistently. All workspace files are preserved (no cleanup).</done>
</task>

</tasks>

<verification>
The complete /solve pipeline includes output formatting:
- Steps 1-5d preserved from Phase 23
- Step 6a displays formatted results in terminal (rules, answers, pass rate, conditional warnings)
- Step 6b writes solution-complete.md consolidating all intermediate outputs
- No workspace cleanup step exists
- The old "Pipeline complete. Workspace:" line is gone
- `grep -c 'Pipeline complete. Workspace' claude-code/.claude/skills/solve/SKILL.md` returns 0
- `grep -c 'Step 6' claude-code/.claude/skills/solve/SKILL.md` returns at least 1
- `grep -c 'solution-complete' claude-code/references/workspace-format.md` returns at least 1
- OUTP-03 verified: `grep -c 'delete\|cleanup\|clean up\|remove.*workspace' claude-code/.claude/skills/solve/SKILL.md` returns 0
</verification>

<success_criteria>
SKILL.md and workspace-format.md together implement all three OUTP requirements:
1. OUTP-01: Terminal shows rules (numbered one-liners, inline [FAIL]), answers (Q_ID: input -> translation), pass rate, and conditional warnings
2. OUTP-02: solution-complete.md written with Rules (full detail), Vocabulary (table), Verification Summary (per-iteration), Answers, and conditional Pipeline Notes
3. OUTP-03: No cleanup step exists -- all intermediate files preserved
4. All user decisions from CONTEXT.md are honored (no path printing, numbered list format, section order, conditional Pipeline Notes, problem referenced not inlined)
5. The workspace-format.md template matches the solution file structure in SKILL.md
</success_criteria>

<output>
After completion, create `.planning/phases/24-output-and-integration/24-01-SUMMARY.md`
</output>
