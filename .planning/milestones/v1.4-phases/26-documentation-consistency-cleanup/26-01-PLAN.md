---
phase: 26-documentation-consistency-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - INFR-02

must_haves:
  truths:
    - "CLAUDE.md model line notes the verifier Sonnet exception inline"
    - "Every stale requirement in REQUIREMENTS.md has updated wording with a parenthetical evolution note"
    - "INFR-02 is checked [x] with updated wording and traceability table shows Complete"
    - "All ROADMAP success criteria across phases 20-24 reference markdown files instead of JSON where applicable"
  artifacts:
    - path: "claude-code/CLAUDE.md"
      provides: "Verifier Sonnet exception noted on model convention line"
      contains: "verifier uses Sonnet for cost efficiency"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement wording with evolution notes"
      contains: "changed from"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected success criteria across phases 20-24"
      contains: "workspace/problem.md"
  key_links:
    - from: "claude-code/CLAUDE.md"
      to: "claude-code/.claude/agents/verifier.md"
      via: "model exception documented"
      pattern: "Sonnet"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "consistent terminology"
      pattern: "markdown"
---

<objective>
Fix stale documentation across CLAUDE.md, REQUIREMENTS.md, and ROADMAP.md so all three files accurately reflect the implemented design decisions.

Purpose: Documentation accuracy is critical — CLAUDE.md is loaded into every Claude Code conversation, REQUIREMENTS.md is referenced by phase plans, and ROADMAP.md success criteria are used by verifier agents. Stale references to JSON files, parallel dispatch, and blanket Opus model statements contradict the actual implementation.

Output: Three updated documentation files with consistent, accurate wording.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.4-MILESTONE-AUDIT.md
@.planning/phases/26-documentation-consistency-cleanup/26-CONTEXT.md
@.planning/phases/26-documentation-consistency-cleanup/26-RESEARCH.md
@claude-code/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md and REQUIREMENTS.md</name>
  <files>claude-code/CLAUDE.md, .planning/REQUIREMENTS.md</files>
  <action>
**CLAUDE.md** (1 edit):

In `claude-code/CLAUDE.md`, find the model convention line (line 7):
```
All solver agents use Opus 4.6. Every agent definition file MUST include `model: opus` in its YAML frontmatter.
```
Replace with:
```
All solver agents use Opus 4.6 (exception: verifier uses Sonnet for cost efficiency). Every agent definition file MUST include `model: opus` in its YAML frontmatter.
```
Note: inline parenthetical on the same line, NOT a separate bullet. Per user decision.

**REQUIREMENTS.md** (8 requirement edits + INFR-02 update + traceability):

For each stale requirement below, update the wording in place and add a parenthetical evolution note. Keep same requirement IDs. Do NOT include phase references in evolution notes — use reason-based notes only.

1. **EXTR-02**: Change `Structured output written to \`workspace/extracted.json\`` to:
   `Structured output written to \`workspace/problem.md\` (changed from extracted.json: markdown format adopted for all workspace files)`

2. **HYPO-01**: Change `Multiple hypothesizer agents dispatched in parallel, each with a different linguistic perspective` to:
   `Multiple hypothesizer agents dispatched sequentially, each with a different linguistic perspective (changed from parallel: deterministic perspective ordering)`

3. **HYPO-02**: Change `Each hypothesizer writes rules + vocabulary to its own draft file (\`workspace/hypothesis-{n}.json\`)` to:
   `Each hypothesizer writes rules + vocabulary to its own draft file (\`workspace/hypotheses/round-{R}/perspective-{N}.md\`) (changed from hypothesis-{n}.json: markdown format, round-based directory structure)`

4. **VERI-02**: Change `Verifier writes test results to \`workspace/verification-{iteration}.json\`` to:
   `Verifier writes test results to \`workspace/verification-{iteration}.md\` (changed from .json: markdown format adopted for all workspace files)`

5. **IMPR-02**: Change `Improved rules written to \`workspace/improved-{iteration}.json\`` to:
   `Improved rules written to \`workspace/improved-{iteration}.md\` (changed from .json: markdown format adopted for all workspace files)`

6. **ANSR-02**: Change `Final answers written to \`workspace/answers.json\`` to:
   `Final answers written to \`workspace/answers.md\` (changed from answers.json: markdown format adopted for all workspace files)`

7. **OUTP-03**: Change `All intermediate JSON files preserved in workspace for debugging` to:
   `All intermediate markdown files preserved in workspace for debugging (changed from JSON: markdown format adopted for all workspace files)`

8. **INFR-02**: Change `- [ ] **INFR-02**: All agents use Opus 4.6` to:
   `- [x] **INFR-02**: All agents use Opus 4.6 (exception: verifier uses Sonnet for cost efficiency)`

Also update the traceability table:
- Change the INFR-02 row status from `Pending` to `Complete`
- Update the pending count line: change from `Pending (gap closure): 4 (ORCH-03, ORCH-04, VERI-01, INFR-02)` to `Pending (gap closure): 3 (ORCH-03, ORCH-04, VERI-01)` — wait, check if ORCH-03, ORCH-04, VERI-01 are already marked Complete in the traceability table. If they are already Complete from Phase 25, update the pending count accordingly (could be 0 if all are already complete, leaving just INFR-02 which is now being completed).

Check the current state of the traceability section carefully. The requirement checkboxes in the main section may already show `[x]` for ORCH-03, ORCH-04, VERI-01 — if so, the pending count should drop to 0 after INFR-02 is checked.

Update the Complete count accordingly.
  </action>
  <verify>
    <automated>grep -n "Sonnet" claude-code/CLAUDE.md && grep -c "changed from" .planning/REQUIREMENTS.md && grep "INFR-02" .planning/REQUIREMENTS.md | head -5</automated>
  </verify>
  <done>
- CLAUDE.md line 7 contains "(exception: verifier uses Sonnet for cost efficiency)" inline
- All 7 file-format requirements (EXTR-02, HYPO-02, VERI-02, IMPR-02, ANSR-02, OUTP-03) reference markdown instead of JSON with evolution notes
- HYPO-01 says "sequentially" with evolution note
- INFR-02 is checked [x] with Sonnet exception noted
- Traceability table shows INFR-02 as Complete with correct pending count
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep and fix all stale ROADMAP success criteria</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Systematically review every success criterion in ROADMAP phases 20-24 and fix stale references. Update completed phases too — the roadmap should be accurate as a historical record per user decision.

**Phase 20 SC2** (currently: "All agent definition files specify Opus 4.6 as the model"):
Change to: `All agent definition files specify Opus 4.6 as the model (verifier uses Sonnet for cost efficiency)`

**Phase 21 SC1** (currently: "An extractor agent can parse a raw Linguistics Olympiad problem into structured JSON and write it to `workspace/extracted.json`"):
Change to: `An extractor agent can parse a raw Linguistics Olympiad problem into structured markdown and write it to \`workspace/problem.md\``

**Phase 21 SC2** (currently: "A hypothesizer agent can generate linguistic rules and vocabulary from a specific perspective and write to `workspace/hypothesis-{n}.json`"):
Change to: `A hypothesizer agent can generate linguistic rules and vocabulary from a specific perspective and write to \`workspace/hypotheses/round-{R}/perspective-{N}.md\``

**Phase 22 SC5** (currently: "The orchestrator validates subagent completion via spot-check (output file exists and contains valid JSON) rather than relying on return status"):
Change to: `The orchestrator validates subagent completion via spot-check (output file exists and contains expected content) rather than relying on return status`

**Phase 23 SC1** (currently: "A verifier agent tests each rule and sentence against the dataset and writes structured results to `workspace/verification-{iteration}.json`"):
Change to: `A verifier agent tests each rule and sentence against the dataset and writes structured results to \`workspace/verification-{iteration}.md\``

**Phase 23 SC2** (currently: "An improver agent reads verification failures and writes revised rules to `workspace/improved-{iteration}.json`"):
Change to: `An improver agent reads verification failures and writes revised rules to \`workspace/improved-{iteration}.md\``

**Phase 23 SC4** (currently: "An answerer agent applies the validated rules to translate questions and writes results to `workspace/answers.json`"):
Change to: `An answerer agent applies the validated rules to translate questions and writes results to \`workspace/answers.md\``

**Phase 23 SC5** (currently: "All intermediate JSON files follow the workspace naming convention and are valid JSON"):
Change to: `All intermediate markdown files follow the workspace naming convention`

**Phase 24 SC3** (currently: "All intermediate JSON files remain in the workspace directory for debugging and inspection"):
Change to: `All intermediate markdown files remain in the workspace directory for debugging and inspection`

Also update the Phase 26 plans section:
- Change `**Plans**: TBD` to `**Plans**: 1 plan`
- Add plan list: `- [ ] 26-01-PLAN.md — Fix stale documentation across CLAUDE.md, REQUIREMENTS.md, and ROADMAP.md`

Leave Phase 21 SC3 as-is — it already correctly says "sequentially".
Leave Phase 26 SC3 as-is — it describes the fix criterion, which remains accurate.
Do NOT touch any other files beyond the three in scope (CLAUDE.md, REQUIREMENTS.md, ROADMAP.md).
  </action>
  <verify>
    <automated>grep -c "\.json" .planning/ROADMAP.md | head -1 && grep "problem.md" .planning/ROADMAP.md && grep "Sonnet" .planning/ROADMAP.md && grep "Plans.*1 plan" .planning/ROADMAP.md | grep "Phase 26" -A1</automated>
  </verify>
  <done>
- Phase 20 SC2 notes the Sonnet exception
- Phase 21 SC1 references "structured markdown" and "workspace/problem.md"
- Phase 21 SC2 references "workspace/hypotheses/round-{R}/perspective-{N}.md"
- Phase 22 SC5 says "expected content" instead of "valid JSON"
- Phase 23 SC1, SC2, SC4 reference .md instead of .json
- Phase 23 SC5 says "markdown files" instead of "JSON files"
- Phase 24 SC3 says "markdown files" instead of "JSON files"
- Phase 26 shows "1 plan" with plan list entry
- No remaining ".json" references in phases 20-24 success criteria (except where legitimately referencing JSON)
  </done>
</task>

</tasks>

<verification>
1. `grep "Sonnet" claude-code/CLAUDE.md` — returns the model convention line with Sonnet exception
2. `grep -c "changed from" .planning/REQUIREMENTS.md` — returns 8 (one evolution note per updated requirement)
3. `grep "\[x\].*INFR-02" .planning/REQUIREMENTS.md` — INFR-02 is checked
4. `grep "Complete.*INFR-02\|INFR-02.*Complete" .planning/REQUIREMENTS.md` — traceability shows Complete
5. `grep "\.json" .planning/ROADMAP.md` — no stale .json references in phases 20-24 success criteria
6. `grep "problem.md" .planning/ROADMAP.md` — Phase 21 SC1 updated
7. `grep "Sonnet" .planning/ROADMAP.md` — Phase 20 SC2 updated
</verification>

<success_criteria>
1. CLAUDE.md model line includes "(exception: verifier uses Sonnet for cost efficiency)" as an inline parenthetical
2. All 8 stale REQUIREMENTS.md items have updated wording with parenthetical evolution notes (no phase references)
3. INFR-02 is checked [x] with traceability table showing Complete and correct pending count
4. All 9 stale ROADMAP success criteria across phases 20-24 reference correct file formats and patterns
5. No remaining ".json" workspace file references in ROADMAP phases 20-24 success criteria
6. Phase 26 plan list is populated in ROADMAP
</success_criteria>

<output>
After completion, create `.planning/phases/26-documentation-consistency-cleanup/26-01-SUMMARY.md`
</output>
