---
phase: 20-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/.claude/agents/extractor.md
  - claude-code/.claude/agents/hypothesizer.md
  - claude-code/.claude/agents/verifier.md
  - claude-code/.claude/agents/improver.md
  - claude-code/.claude/agents/synthesizer.md
  - claude-code/.claude/agents/answerer.md
  - claude-code/.claude/skills/solve/SKILL.md
  - claude-code/.gitignore
  - claude-code/workspace/.gitkeep
  - claude-code/CLAUDE.md
  - claude-code/references/workspace-format.md
autonomous: true
requirements: [INFR-01, INFR-02, INFR-03]

must_haves:
  truths:
    - "claude-code/.claude/agents/ contains 6 agent definition files with valid YAML frontmatter"
    - "Every agent definition file specifies model: opus in its frontmatter"
    - "claude-code/.claude/skills/solve/SKILL.md exists with valid skill frontmatter"
    - "claude-code/CLAUDE.md describes project context, model requirement, workspace structure, and agent conventions"
    - "claude-code/references/workspace-format.md contains markdown templates for all workspace file types"
    - "claude-code/workspace/ is gitignored but the directory itself is tracked via .gitkeep"
  artifacts:
    - path: "claude-code/.claude/agents/extractor.md"
      provides: "Skeleton agent definition for problem extractor"
      contains: "model: opus"
    - path: "claude-code/.claude/agents/hypothesizer.md"
      provides: "Skeleton agent definition for hypothesis generator"
      contains: "model: opus"
    - path: "claude-code/.claude/agents/verifier.md"
      provides: "Skeleton agent definition for rule verifier"
      contains: "model: opus"
    - path: "claude-code/.claude/agents/improver.md"
      provides: "Skeleton agent definition for rule improver"
      contains: "model: opus"
    - path: "claude-code/.claude/agents/synthesizer.md"
      provides: "Skeleton agent definition for ruleset synthesizer"
      contains: "model: opus"
    - path: "claude-code/.claude/agents/answerer.md"
      provides: "Skeleton agent definition for question answerer"
      contains: "model: opus"
    - path: "claude-code/.claude/skills/solve/SKILL.md"
      provides: "Slash command shell for /solve"
      contains: "disable-model-invocation: true"
    - path: "claude-code/CLAUDE.md"
      provides: "Project context and conventions"
    - path: "claude-code/references/workspace-format.md"
      provides: "Workspace file format templates"
    - path: "claude-code/.gitignore"
      provides: "Gitignore for workspace directory"
      contains: "workspace/"
    - path: "claude-code/workspace/.gitkeep"
      provides: "Empty placeholder to track workspace directory in git"
  key_links:
    - from: "claude-code/CLAUDE.md"
      to: "claude-code/PIPELINE.md"
      via: "reference"
      pattern: "PIPELINE.md"
    - from: "claude-code/CLAUDE.md"
      to: "claude-code/references/workspace-format.md"
      via: "reference"
      pattern: "references/workspace-format.md"
---

<objective>
Set up the `claude-code/` directory with all agent skeleton files, the `/solve` skill shell, CLAUDE.md project context, shared workspace format references, and gitignore configuration.

Purpose: Establish the directory structure and conventions so Phase 21+ can fill in agent prompts against a working project scaffold.
Output: 11 markdown/config files forming a complete Claude Code project skeleton.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-infrastructure-setup/20-CONTEXT.md
@.planning/phases/20-infrastructure-setup/20-RESEARCH.md
@.planning/phases/19-workflow-documentation/19-01-SUMMARY.md
@claude-code/PIPELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure, agent skeletons, skill shell, and gitignore</name>
  <files>
    claude-code/.claude/agents/extractor.md
    claude-code/.claude/agents/hypothesizer.md
    claude-code/.claude/agents/verifier.md
    claude-code/.claude/agents/improver.md
    claude-code/.claude/agents/synthesizer.md
    claude-code/.claude/agents/answerer.md
    claude-code/.claude/skills/solve/SKILL.md
    claude-code/.gitignore
    claude-code/workspace/.gitkeep
  </files>
  <action>
Create the Claude Code project directory structure under `claude-code/`.

**Agent skeleton files** — Create 6 files in `claude-code/.claude/agents/`, each with YAML frontmatter and a placeholder body. Every agent MUST have `model: opus` in its frontmatter. Use plain descriptive names (no numbered prefixes). Each agent gets `Read, Write, Bash, Glob, Grep` as tools (agents need filesystem access for workspace file I/O).

Frontmatter fields per agent:
- `name`: descriptive kebab-case identifier
- `description`: 1-2 sentence description of when to delegate to this agent, including keywords the orchestrator will use
- `tools`: `Read, Write, Bash, Glob, Grep`
- `model`: `opus`

Body: A brief placeholder comment like `[System prompt — Phase 21]` for Phase 21/23 agents, or `[System prompt — Phase 23]` for verify/improve/answer agents. Include a 1-line summary of the agent's role so it's clear what will go there.

The 6 agents and their descriptions:

1. **extractor.md** — `name: extractor`, description: "Parses raw Linguistics Olympiad problem text into structured markdown with context, dataset, and questions. Use when extracting structure from raw problem input."
2. **hypothesizer.md** — `name: hypothesizer`, description: "Generates linguistic rules and vocabulary from a specific analytical perspective. Use when creating hypothesis drafts for a given perspective (morphological, syntactic, phonological, etc.)."
3. **verifier.md** — `name: verifier`, description: "Tests a set of linguistic rules and vocabulary against the dataset by attempting to translate each sentence. Use when checking whether a hypothesis correctly explains the data."
4. **improver.md** — `name: improver`, description: "Revises failing linguistic rules based on verification results. Use when rules have been tested and some sentences fail."
5. **synthesizer.md** — `name: synthesizer`, description: "Merges the best-performing rules and vocabulary from multiple hypothesis perspectives into a single consolidated ruleset. Use after verification results are available for all perspectives in a round."
6. **answerer.md** — `name: answerer`, description: "Applies validated linguistic rules and vocabulary to translate question sentences. Use when rules have passed verification and questions need to be answered."

**Skill shell** — Create `claude-code/.claude/skills/solve/SKILL.md` with this exact content:

```yaml
---
name: solve
description: Solve a Linguistics Olympiad Rosetta Stone problem
disable-model-invocation: true
argument-hint: "[file-path]"
---
```

Followed by the placeholder body:

```
Solve a Linguistics Olympiad Rosetta Stone problem.

If $ARGUMENTS contains a file path, read the problem from that file.
If $ARGUMENTS is empty, ask the user to paste the problem text or provide a file path.

[Orchestrator logic — Phase 22]
```

IMPORTANT: Do NOT set `context: fork` on this skill. The orchestrator must run in the main conversation context so it can dispatch subagents (subagents cannot spawn other subagents).

**Gitignore** — Create `claude-code/.gitignore`:
```
workspace/
!workspace/.gitkeep
```

**Workspace placeholder** — Create `claude-code/workspace/.gitkeep` as an empty file.
  </action>
  <verify>
    <automated>ls claude-code/.claude/agents/extractor.md claude-code/.claude/agents/hypothesizer.md claude-code/.claude/agents/verifier.md claude-code/.claude/agents/improver.md claude-code/.claude/agents/synthesizer.md claude-code/.claude/agents/answerer.md claude-code/.claude/skills/solve/SKILL.md claude-code/.gitignore claude-code/workspace/.gitkeep && grep -l "model: opus" claude-code/.claude/agents/*.md | wc -l && grep -c "disable-model-invocation: true" claude-code/.claude/skills/solve/SKILL.md && ! grep -q "context: fork" claude-code/.claude/skills/solve/SKILL.md && echo "PASS"</automated>
  </verify>
  <done>
    - 6 agent files exist in claude-code/.claude/agents/ with plain descriptive names
    - All 6 agent files contain `model: opus` in frontmatter (grep count = 6)
    - Skill file exists at claude-code/.claude/skills/solve/SKILL.md with `disable-model-invocation: true`
    - Skill file does NOT contain `context: fork`
    - .gitignore and workspace/.gitkeep exist
  </done>
</task>

<task type="auto">
  <name>Task 2: Write CLAUDE.md and workspace format reference</name>
  <files>
    claude-code/CLAUDE.md
    claude-code/references/workspace-format.md
  </files>
  <action>
**CLAUDE.md** — Create `claude-code/CLAUDE.md` (target: under 150 lines, hard cap: 200 lines). This is the project context file loaded at every Claude Code session in `claude-code/`.

Structure and content:

```markdown
# LO-Solver Claude Code Pipeline

This project reimplements the LO-Solver's Mastra-based pipeline as a Claude Code native experience using subagents, skills, and file-based state. It runs alongside the existing Mastra implementation in the parent repository.

## Model

All solver agents use Opus 4.6. Every agent definition file MUST include `model: opus` in its YAML frontmatter.

## Pipeline

See PIPELINE.md for the full pipeline reference document describing all steps, data flow, agent roles, and design rationale.

The pipeline has 4 logical stages:
1. **Extract** — Parse raw problem into structured markdown (context, dataset, questions)
2. **Hypothesize** — Generate linguistic rules and vocabulary from multiple perspectives
3. **Verify & Improve** — Test rules against the dataset, revise failures (up to 4 iterations)
4. **Answer** — Apply validated rules to translate question sentences

## Agents

Agent definitions live in `.claude/agents/`. Each is a single self-contained markdown file:
- `extractor.md` — Parse raw problem into structured markdown
- `hypothesizer.md` — Generate linguistic rules and vocabulary from a given perspective
- `verifier.md` — Test rules against the dataset
- `improver.md` — Revise failing rules based on verification results
- `synthesizer.md` — Merge best rules from multiple perspectives
- `answerer.md` — Apply rules to translate questions

Agents communicate via file-based handoff: each reads predecessor files from the workspace and writes its own output files.

## Workspace

Per-run output stored in `workspace/{datetime}/` (e.g., `workspace/2026-03-07_14-30-00/`). Runs are always preserved (no auto-cleanup).

### Structure
```
workspace/{datetime}/
  problem.md                 # Extracted: context, dataset, questions
  hypotheses/
    round-1/
      perspective-1.md       # Vocab + rules (e.g., morphological focus)
      perspective-2.md       # Vocab + rules (e.g., syntactic focus)
      verification-1.md      # Test results for perspective-1
      verification-2.md      # Test results for perspective-2
    round-2/
      perspective-1.md
      verification-1.md
  solution.md                # Merged/synthesized vocab + rules
  verification.md            # Final verification results
  answers.md                 # Translated answers
  errors.md                  # Error log (if any failures occurred)
```

### File Format
All workspace files use markdown (no JSON). See `references/workspace-format.md` for templates.

## Conventions

- Agent file names: plain descriptive (e.g., `extractor.md`, not `01-extractor.md`)
- Default perspective count per round: 3
- Non-critical failures (hypothesizer, verifier): retry once, then skip and continue
- Critical failures (extractor): abort with clear error message
- Error logging: separate `errors.md` file in the run folder

## Domain Context

A Rosetta Stone problem provides sentences in an unfamiliar language paired with their English translations. The task is to discover the language's linguistic rules (grammar, morphology, phonology) and use them to translate new sentences that have no provided translation. These problems come from Linguistics Olympiad competitions.
```

Adjust wording for clarity and flow, but keep the same structure and content scope. Do NOT include testing guidance. Do NOT duplicate pipeline details from PIPELINE.md.

**Workspace format reference** — Create `claude-code/references/workspace-format.md` with markdown templates for each workspace file type. This is the shared reference that agents read on demand to understand the expected file formats.

Include a template section for each file in the workspace structure:

1. **problem.md** — Template showing context section (language name, language family if known, special notes), dataset section (numbered sentence pairs: foreign | English), and questions section (numbered translation tasks with direction indicated)

2. **perspective-N.md** (hypothesis file) — Template showing vocabulary section (a table or list of foreign-word → English-meaning mappings) and rules section (numbered linguistic rules with examples, each rule having a title, description, and example)

3. **verification-N.md** — Template showing a results summary (pass/fail count), then per-sentence test results (sentence number, foreign sentence, expected English, generated English, status pass/fail, notes on any mismatches)

4. **solution.md** — Template showing merged vocabulary (combined from best perspectives) and consolidated rules (synthesized from best-performing perspectives, with attribution to which perspective each rule originated from)

5. **verification.md** (final) — Same structure as verification-N.md but for the final consolidated ruleset

6. **answers.md** — Template showing each question with the foreign sentence, the generated English translation, the key rules applied, and confidence notes

7. **errors.md** — Template showing timestamped error entries with agent name, error type, error message, and whether the error was recovered from

Keep templates readable and natural — these are markdown files agents will read and write, not rigid schemas. Use example content (e.g., a fictional language snippet) to make templates concrete rather than abstract. Total file length: aim for 150-250 lines.
  </action>
  <verify>
    <automated>test -f claude-code/CLAUDE.md && test -f claude-code/references/workspace-format.md && wc -l claude-code/CLAUDE.md | awk '{if ($1 <= 200) print "CLAUDE.md OK: "$1" lines"; else print "CLAUDE.md TOO LONG: "$1" lines"}' && grep -q "PIPELINE.md" claude-code/CLAUDE.md && grep -q "model: opus" claude-code/CLAUDE.md && grep -q "workspace/" claude-code/CLAUDE.md && echo "PASS"</automated>
  </verify>
  <done>
    - CLAUDE.md exists and is under 200 lines
    - CLAUDE.md references PIPELINE.md (not duplicating its content)
    - CLAUDE.md includes model requirement (Opus 4.6 / model: opus)
    - CLAUDE.md includes workspace directory structure
    - CLAUDE.md includes agent file naming conventions
    - CLAUDE.md includes agent communication pattern (file-based handoff)
    - CLAUDE.md includes brief domain context (Rosetta Stone problems)
    - CLAUDE.md does NOT include testing guidance
    - references/workspace-format.md exists with templates for all 7 workspace file types
  </done>
</task>

</tasks>

<verification>
Run all checks to confirm the infrastructure is ready for agent development:

1. Directory structure exists:
   ```bash
   find claude-code/ -type f | sort
   ```
   Expected: 11 files (6 agents, 1 skill, 1 gitignore, 1 gitkeep, 1 CLAUDE.md, 1 workspace-format.md) plus the existing PIPELINE.md

2. All agents specify Opus 4.6:
   ```bash
   grep -c "model: opus" claude-code/.claude/agents/*.md
   ```
   Expected: each file shows count 1 (6 files total)

3. Skill is not auto-invocable and not forked:
   ```bash
   grep "disable-model-invocation" claude-code/.claude/skills/solve/SKILL.md
   grep "context: fork" claude-code/.claude/skills/solve/SKILL.md
   ```
   Expected: first grep finds `true`, second grep finds nothing

4. CLAUDE.md is concise:
   ```bash
   wc -l claude-code/CLAUDE.md
   ```
   Expected: under 200 lines

5. No JSON workspace file templates (all markdown):
   ```bash
   grep -c "\.json" claude-code/references/workspace-format.md
   ```
   Expected: 0 (or only in prose context, not as file format)
</verification>

<success_criteria>
- The `claude-code/` directory has a complete `.claude/` structure with 6 agent skeletons and 1 skill shell
- Every agent file has `model: opus` in its frontmatter
- CLAUDE.md provides project context under 200 lines, referencing PIPELINE.md
- Workspace format reference provides concrete templates for all file types
- Workspace directory is gitignored with .gitkeep tracked
- No `context: fork` on the /solve skill
- The structure is ready for Phase 21 to fill in agent system prompts
</success_criteria>

<output>
After completion, create `.planning/phases/20-infrastructure-setup/20-01-SUMMARY.md`
</output>
